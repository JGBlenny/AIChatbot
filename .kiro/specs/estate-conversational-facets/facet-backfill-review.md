# 物件領域既有知識複核補標表（任務 3.1，人工閘門）

> 產出：2026-07-04　複核基準：research.md 定案 9 項（兩軸狀態機/刪除三擋/批次 queue/地址雙層）
> 判定：**補標**（categories 追加面向）／**改寫**（口徑對真碼修正，走批次 updates）／**停用**（過時且有替代）／**不動**（他域或無衝突）

## 一、掛面向＋改寫（核心批）

| id | 知識 | 判定 | 理由與動作 |
|---|---|---|---|
| 3428 | 物件狀態 定義條件 | **改寫＋掛 `物件現況診斷`** | 現文為**單軸模型**（「刊登中＝已上架可搜尋」把兩軸混講、「合約結束後回到未刊登或刊登中」）——兩軸口徑重寫（合約軸 1/2/4/8 × 刊登軸獨立；租約中可同時刊登中） |
| 3505 | 為什麼不能新增物件 | **改寫＋掛 `物件操作引導`** | 現文一句話過薄（「系統會提示方案已滿」）——補實：額度已滿三出路＋權限不足分支 |
| 3506 | 物件為什麼突然全部下架 | **改寫＋掛 `物件操作引導`** | 過薄——補實：續約扣款失敗主因＋處理路徑＋必填欄位驗證失敗次因 |
| 3507 | 物件為什麼不能建立合約 | **改寫＋掛 `物件現況診斷`** | 「16 個必填欄位」數字未證（真碼欄位集 13-14）——改寫為兩前提（刊登中＋欄位齊）＋導個案診斷（「可以直接問我這個物件缺什麼」） |
| 3433 | 物件 VR 虛擬導覽 | **改寫（單發保留，不掛面向）** | R5.2：VR 不對話化——保留功能存在說明，拍攝/裝置/marker 操作導 iStaging 廠商 |

## 二、純補標（內容不動）

| id | 知識 | 追加 categories |
|---|---|---|
| 3429 物件刊登 上架條件 | ＋`物件操作引導` |
| 3430 物件下架 條件流程 | ＋`物件操作引導` |
| 3432 Dashboard 儀表板 | ＋`物件操作引導` |
| 3434 自訂標籤 | ＋`物件操作引導` |
| 3861 新增合約入口 刊登前提 | ＋`物件現況診斷`（保留 `建約引導` 雙掛） |
| 3862 一物件多合約 上限10份 | 設 `物件現況診斷`（原空） |
| 3894 快照原則 | 設 `物件操作引導`（原空） |
| 3895 刊登租金調整 帳單不變 | 設 `物件操作引導`（原空） |

## 三、停用

（無——原列 3357 因批次範圍外改列不動）

## 四、不動（他域/範圍外/無衝突）

**批次上傳範圍外（使用者裁定 2026-07-04 不處理）**：3431（批次匯入——不改寫不補標）、3357（舊批次筆——⚠️ 含過時口徑「須為 .xls」「3 秒顯示」，掛帳不動，若日後回收批次主題再處理）。

3338（帳務：物件帳單發票對應——帳務域另案補標候選）、3358＋3467（大房東域）、3404（帳單批次）、3456（訂閱方案——3505 改寫已引用其口徑，不重掛避免面向吸訂閱知識）、3469（社區）、3476（租客批次）、3530（合約批次續約）、3537（發票）、3596/3599（售前 prospect）、3973（帳號域團隊權限）。

## ⚠️ 待盤衝突註記（不阻塞，e2e 階段驗證）

**3861 vs 3862**：3861 說「租約中物件無法建立新合約」（真碼 pick_for_contract 強制 status=2）；3862 說「A 租約快到期先開放招租、與新租客 B 預先簽約並存銜接」——兩者表面矛盾。可能解：續約鏈/上傳既存合約不走 pick 入口。**兩筆皆不改文**，e2e 或下輪 jgb2 盤查釐清後再定；診斷面向個案回答以 API 現值（status/缺欄）為準不受影響。

## 統計

改寫 5＋純補標 8＋不動 14（含批次範圍外 2）＝ 27 筆全數審畢。
新知識/錨點見 estate-knowledge-batch.json（另一閘門件）。
