# 大綱人審回饋（2026-09-05）

- 對象：`eval/outline-20260905.md`（sha `a98af3fa…`，31 列、14 節、8007 token）。
- 業主裁定：「同意」取消重複列標記。主 session 核對後把工具建議的 7 列收窄為 **2 列（5376、5377）**——只有 3584＝5377、3610＝5376 兩對答案逐字相同（DB 實查）；5378／5379／5380 各有獨有內容（催繳／匯入／帳單格式），保留。細節與 SQL 見 `DECISIONS.md` DSP-026、`sql/unmark-outline-alias-20260905.sql`。
- 結果：大綱 29 列、7521 token、`outline_sha=90e9a987…`（容器重啟後）。
- 8 列 `categories IS NULL`（3335、3340、3342、3353、3355、3356、3357、3358）：本次不動，列入 3.6 指南「categories 對齊」待補分類。
- 工具教訓：`agent_outline_dump.py` 的 alias 判準（batch_file）對大綱用途過寬——講法列 ≠ 重複內容；建議 SQL 只能當人審輸入，⛔ 不得直接執行。
