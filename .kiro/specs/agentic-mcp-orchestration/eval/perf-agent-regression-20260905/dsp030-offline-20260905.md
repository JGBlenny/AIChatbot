# 4.4b DSP-030 規則版離線量測（2026-09-05）

- 資料：第七輪（儀表化）54 句 agent 鏈 3 rep 的 texts 旁路 → `dsp030_pairs.jsonl`（241 句；196 個 fact 句有解析後來源句）；標籤 138 句（獨立代理，逐句對「該句所引來源句」判 supported／partial／unsupported／non_fact，⛔ 無 LLM）。原文與標籤留 scratchpad，⛔ 不進版控。
- 真值定義：`grounded`＝supported；`ungrounded`＝partial＋unsupported（保守）。F-1 量詞：任一所引來源句過即過。

## 標籤分佈
| 組 | n | supported | partial | unsupported | non_fact | 結論 |
|---|---|---|---|---|---|---|
| A：被 QUOTE_NOT_COVERING 拒 | 41 | 14 | 18 | 7 | 2 | **78% 疑似誤殺** |
| B：放行 | 85 | 67 | 14 | 3 | 1 | **20% 漏放** |
| C：被 POLARITY 拒 | 12 | 5 | 2 | 5 | 0 | **58% 誤殺**（無法／不可 同義；詞表缺 不可／不得／沒辦法） |

漏放的新增成分：mechanism 31、condition 11、capability 10、actor 4、number 1、date 0 ⇒ **語義層問題，非字元層**。

## 規則變體（136 標籤句）
| 變體 | 抓到率（無據被拒） | 誤殺率（有據被拒） |
|---|---|---|
| 現行 cov≥0.5 && overlap≥4 | 53% | 13% |
| novel_chars ≤2／≤3／≤4 | 100／100／98% | **68／57／49%** |
| bigram_novel ≤0.5／≤0.6 | 94／86% | **46／32%** |
| no_novel_token（數字／期間／能力動詞） | 92% | **76%** |
| cov≥0.5 && no_novel_token | 96% | **76%** |

## 判定
1. **DSP-030 純規則版：REJECT**——沒有任何變體同時達到「抓到率 > 現行且誤殺 ≤ 現行」；字元級尺無法區分改寫與語義新增。現行覆蓋尺**維持**（⛔ 不放寬、不加嚴）。
2. **否定詞表補齊為一類（DSP-030 唯一採納項）**：加「不可、不得、沒辦法、不允許、未提供」；預期解 C 組 7/12 誤殺；離線以 12 筆 C 驗證。
3. **啟動 4.4c NLI 離線量測**（凍結計畫的觸發條件成立）：同一批 136 標籤句，量 mDeBERTa／Erlangshen 對「supported vs partial+unsupported」的抓到率、誤殺率、延遲。門檻不變：抓到率 > 53% 且誤殺 ≤ 13%、p95 ≤100 ms/句、R4 三捏造句 3/3 抓到。
4. 抽審者揭露的量尺混雜：A／B 中約 4 筆是「引用錯位」（內容對、引到不相關來源句）——這類在 NLI 下同樣會判 unsupported，屬正確拒絕（模型該引對句）。
