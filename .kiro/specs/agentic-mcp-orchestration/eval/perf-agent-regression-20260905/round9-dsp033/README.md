# 第九輪：DSP-033 NLI 取代覆蓋＋極性——落地後兩輪回歸（2026-09-06 凌晨）

> HEAD `4f9683c`＋未 commit 工作樹（canary 期望值更正、fixtures 搬回、compose nli-model mem_limit 1.5 GiB、Dockerfile 指紋）。gpt-4o-mini、`--repeat 3`、兩鏈、三組×2 輪（r9a／r9b）。
> 環境：8 GB Mac、Docker VM 6 GB（r9a sensitive 時仍為 4 GB；VM 從 4→6 GB 的重啟打斷了一次，r9a scenarios 起重跑）。非本專案容器已停。
> 原文旁路（`texts/`）只在容器與主 session scratchpad，⛔ 不進版控。agent 鏈成本兩輪合計 **$1.39**。
> ⚠️ 本輪 `nli-model` 首跑 canary 0/3（期望 1/3）——查明期望值是整段前提算的，見 `eval/nli-offline-20260905.md` 更正節；本輪跑在更正後的 canary（期望 0）上，τ=0.40 不動。

## agent 鏈數字（report.md 原值）

| 輪 | 組 | 敏感漏 | 禁詞 | 固定句率（同批 old） | 邊界不硬答 | 答到率 | p95 ms | budget_exhausted | NOT_ENTAILED | floor | 窄化極性 | 降級回合 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| r9a | sensitive 30×3 | **0/90** | 0/90 | n=0 | — | 0%（全轉人＝正確） | 2183 | 0 | 0 | 0 | 0 | 0 |
| r9a | scenarios 23×3 | 0/15 | 0/69 | 0.909（old 0.636） | 95.2% | 5.8% | 4236 | 7/69 | 11 | 1 | 0 | 0 |
| r9a | topics 54×3 | — | 3/162 | 0.652（old 0.384） | 83.3% | 32.1% | 5069 | 36/162 | 39 | 15 | 2 | 0 |
| r9b | sensitive | **0/90** | 0/90 | n=0 | — | 0% | 2479 | 0 | 0 | 0 | 0 | 0 |
| r9b | scenarios | 0/15 | 0/69 | 0.909（old 0.667） | 81.0% | 10.1% | 4272 | 6/69 | 11 | 0 | 0 | 0 |
| r9b | topics | — | 2/162 | 0.696（old 0.399） | 91.7% | 27.2% | 5406 | 48/162 | 56 | 11 | 1 | 0 |

舊鏈同批：sensitive 漏 5/90（兩輪同）、topics 禁詞 5／2、答到率 56%／54%、p95 7.8／7.3 s。

## DSP-033 驗收（①–⑧）對照

| # | 尺 | 門檻 | 實得 | 判定 |
|---|---|---|---|---|
| ① | 放行句無據率（2 輪合併，盲標一致子集） | ≤ 7.7% | **6.5%**（11/169 放行 fact 句；r9a topics 6/84、r9b topics 5/64、scenarios 0/21）。對照：現行尺離線放行的 264 句中無據 11.0% | **過** |
| ② | 有據句被拒比例（NLI∨floor∨窄化極性）；同批抓到率 > 現行尺 | ≤ 10%；> | 兩判者 365 句二分一致 98%（350 句一致子集：有據 273／無據 77）。**NLI 誤殺 19.0%（52/273）、抓到 80.5%（62/77）**；現行尺同批 13.9%／62.3%。判者分開算：A 20.6%／79.5%、B 19.0%／79.5%；只算 certain：A 17.3%、B 15.9%。誤殺拒因：NOT_ENTAILED 40、floor 10、窄化極性 2 | **誤殺未達**；抓到 +18 點達 |
| ③ | topics budget_exhausted（2 輪） | ≤ 63/324 | **84/324**（36＋48） | **未達** |
| ④ | topics 答到率（2 輪合併） | ≥ 33.0% | **29.6%**（32.1／27.2） | **未達** |
| ⑤ | 禁詞 ≤3／輪、敏感 0 漏 | | 3、2；0/90、0/90 | 過 |
| ⑥ | `run_turn` p95（全體、含降級） | ≤ 6 s | 最大 5.41 s（topics r9b） | 過（⚠️ 本機記憶體壓縮下量到；nli-model 每對 p50 589 ms 的環境，部署機須重量） |
| ⑦ | 降級演練 | 回合照常、trace `nli_degraded`、health 告警 | 停容器→health `status=red nli_ready=false`→回合 `nli_degraded=true`、拒因回到 `POLARITY_MISMATCH`（全極性語義）→`docker start` 10 s 後 `nli_ready=true`→下一回合 `nli_degraded=false`（`drill/`） | 過（⚠️ health 的 `nli_degraded` 視窗計數為 0/0：eval 走 `run_turn` 子行程、不經 API 行程的計數器；線上路徑不受影響） |
| ⑧ | 尺自證 | 1/3（F-1 裁 (a)） | **0/3**（單句前提實測；原 1/3 為整段前提所得） | **待業主重裁** |

## 盲標方法（②）
封包＝兩輪 topics＋scenarios 所有嘗試中「fact 且 refs 解析成功」的句子，以（句子, 來源句組）去重 365 句；兩位獨立代理只看句子與來源（brief 在主 session scratchpad），⛔ 不見任何判定；雙尺對同一封包離線重算（NLI 模式＝floor∧窄化極性∧p≥0.40 via `/nli`；現行尺＝ratio 0.5∧全極性），程式 `packet.py`（scratchpad、一次性）。標籤與封包含原文，留 scratchpad ⛔ 不進版控。

## 結構性選項的離線量測
- **(i) 多 ref 串成單一前提**：350 句中只有 3 句引用 ≥2 個不同 unit，52 個誤殺有 51 個是單一來源 ⇒ 誤殺 19.0%／抓到 80.5% **完全不變**。此路無效。
- **(iii) 換模型（MoritzLaurer/mDeBERTa-v3-base-xnli-multilingual-nli-2mil7）**：同封包、同一致標籤（有據 273／無據 77）、整句為假設、各來源取最大、`only_first` 256，與 Erlangshen（`/nli`）同基準對掃 τ（scratchpad `mdeberta_sweep.py`）：

| τ | mDeBERTa 誤殺 | 抓到 | Erlangshen 誤殺 | 抓到 |
|---|---|---|---|---|
| 0.20 | 20.9% | 67.5% | 12.5% | 67.5% |
| 0.40 | 26.4% | 76.6% | 15.8% | 77.9% |
| 0.60 | 31.1% | 83.1% | 19.4% | 83.1% |
| 0.80 | 39.6% | 88.3% | 25.3% | 90.9% |

  mDeBERTa 每個 τ 都比 Erlangshen 差；**兩個模型在「誤殺 ≤10%」約束下最大抓到都只有 59.7%**（mDeBERTa τ=0.05、Erlangshen τ=0.11），低於現行尺同批 62.3%。mDeBERTa 每對 p50 419／p95 615 ms（同機同時段）。此路無效。
- **(iv) 兩把尺組合**（同批 2×2 交叉）：

| 規則 | 誤殺 | 抓到 |
|---|---|---|
| NLI 單獨（線上現況） | 19.0% | 80.5% |
| 現行尺單獨 | 13.9% | 62.3% |
| 兩者皆拒才拒 | **8.8%** | 58.4% |
| 任一拒就拒 | 24.2% | 84.4% |

  「皆拒才拒」誤殺過門檻但抓到低於現行 4 點（3 句）；⛔ 不滿足「抓到 > 現行」。

## 結論（主 session；裁決權在業主）
在這個樣本分佈（多輪劇本＋單輪 54 句、含稱謂與情境的改寫）上，**沒有任何 NLI 設定同時滿足凍結的兩條約束**（誤殺 ≤10% 且抓到 > 現行）。NLI 提供的是另一個操作點：抓到 +18 點、誤殺 +5 點、答到率 −3.4 點、budget_exhausted +21/324。R8 的 69%／9% 只在 topics 單輪短答上成立，未外推。

## 觀察（原文抽審，主 session）

1. **NOT_ENTAILED 是 agent 鏈最大拒因**（topics 39／56 ⇒ 每輪約 1/4 回合），取代了 R7 的 `QUOTE_NOT_COVERING=41`＋`POLARITY=12`——步③總拒量與現行尺相當，拒的是不同的句子。
2. **模型行為＝「假設 ⊆ 前提」的嚴格尺**：逐字 0.92、子集 0.97；假設多一個前提沒有的詞就掉到 NEUTRAL——「系統制式」→「JGB制式」0.05、「當租客逾期不繳租金時，…」前置情境 0.30、「可以的，您可以預約 demo」0.35。簡體轉換（zhconv）不改變結論。
3. **一句話引兩個 unit 時 F-1 逐 ref 取最大，沒有單一 unit 蘊涵整句**（「內容可設定…但版面固定」需 §8＋§9）。
4. 上述 2、3 是 topics 單輪短答（R8 校準樣本）之外的句型：多輪、複合句、帶稱謂與情境。R8 的 69%／9% 未必外推到 scenarios。

## 環境事件（影響可比性）

- Docker VM 掛死→殺 backend 重啟；nli-model 在 1 GiB 上限下 cgroup 撞頂 1990 次、權重被換出（每對 p50 589 ms），改 1.5 GiB 後仍撞頂 1059 次（peak＝上限、無 OOM kill）；部署機建議 2 GiB。
- 本機 `.env` `NLI_PAIR_TIMEOUT_MS=2000`（prod compose 預設 400 不動）；兩輪降級回合 0，逾時未觸發。
- r9a sensitive 跑在 VM 4 GB；其餘五組在 6 GB。

## 下一步（待盲標與業主裁定）

盲標 ×2 → ①② 數字 → 若 ② 誤殺 >10%：結構性選項（不調 τ、不加例）——(a) 多 ref 串成一個前提再算（契約層）、(b) 換較不脆的 NLI 模型（mDeBERTa-base，VM 6 GB 現可離線量）、(c) 停 DSP-033、NLI 留抽審輔助。

## 附：答到率低的根因分解與「先檢索再交模型」的上限量測（2026-09-06 03:40）
- 獨立代理（只讀大綱與題庫、不見結果）判 54 題僅依大綱可答性：answerable 39／partial 10／operational 4／sensitive 1／not_covered **0**（scratchpad `outline_answerability.jsonl`）。
- answerable 39 題 234 回合：agent 答到 34%、舊鏈 71%。agent 未答的 155 回合＝Verifier 拒到預算用盡 75（48%）、模型自報 no_grounding 68（44%）、模型誤標敏感 12（8%，「我還沒簽可以改嗎」→customer_reference ×4）。
- 依問法型：直接 51%／俗稱 42%／口語 19%／情境 12%（舊鏈 61／85／68／32）。**口語→大綱段落的對應是 gpt-4o-mini 在 3,078 tokens 大綱裡做不好的事**，不是大綱缺內容。
- 檢索上限（embedding-api＋semantic-model，對 49 題 answerable/partial，gold＝代理標的依據句）：

| 顆粒 | recall@1 | @3 | @5 | top-5 平均上下文 |
|---|---|---|---|---|
| 單句 unit（117） | 47% | 59% | 76% | — |
| 【】段落（59，中位 81 字） | 51% | 71% | 86%（+rerank 82%） | 約 490 字 |
| 整節（14） | 71% | 94% | 96% | 約 500 tokens／節 |

  13 題「可答卻 6 次全沒答」在段落層 top-3 命中 11、top-5 命中 12、1 題 MISS（「我有現成的合約想放進系統」）。reranker 在 59 個短段落上沒有優於純向量。
- 含意：把「找段落」交回程式（先取 top-3～5 段落、以 DSP-029a 標記注入為 provenance、再讓模型判斷與組答）可去掉 no_grounding 那 44% 的大半，且上下文從 3,078 tokens 降到約 500 字；Verifier、refs 解析、敏感層不需改。未解：多輪要用程式做查詢濃縮；召回漏掉的 14–18% 需保留章節目錄＋`kb.get` 或第二次嘗試回整節；fact_class 誤標要由程式側敏感樣式覆寫。此案反轉「prospect 不用向量檢索」的既定決策，需立 DSP。
