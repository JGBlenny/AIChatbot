# Plan：第一刀——Verifier 極性／覆蓋尺在 gpt-5-mini 材料上的誤殺量測（離線、$0）

> 位置：DSP-035 後續三刀之一（業主 2026-09-07「先完成主線再優化」）。目的只有一個：**判定 4.4 的 `budget_exhausted` 是尺誤殺還是模型講超過來源**，用預登記門檻決定下一步要不要動尺。⛔ 本 Plan 不改 `verifier.py`、不改 rules json、不動模型、不打 OpenAI API。
> 依據：探針 51–54 四輪，被極性／覆蓋／無引用尺拒且引用可解析的句子共 **140**（51：30、52：42、53：42、54：26；SCHEMA／HANDOFF_WORD 拒因不在本案——那是形狀，不是尺）。DSP-034 重提條件：**凍結 holdout、誤殺 ≤10% 且抓到 > 現行尺同批**才討論改尺。前次（NLI）在 4o-mini 材料上誤殺 19% 出局；本案材料換成 gpt-5-mini（`low`／預設），是新分佈。

## 0. 命題與推翻條件（跑前預登記，⛔ 事後不改）

| # | 命題 | 尺 | 推翻條件 ⇒ 結論 |
|---|---|---|---|
| M1 | 現行尺在 gpt-5-mini 材料上的**誤殺**（被拒句其實有據）是 `budget_exhausted` 的主因 | 被拒句盲標 grounded 比例（一致子集） | 誤殺 < 40% ⇒ 主因是模型外推，**本刀收束、不動尺**，下一刀轉正本缺口；≥ 40% ⇒ 開「改尺設計」子案（仍受 DSP-034 條件） |
| M2 | 極性尺的誤殺集中在「否定框架改寫」（來源否定句被寫成肯定／反之，語義相同） | 極性拒的 grounded 句中，判者標 `negation_reframe` 的比例 | < 50% ⇒ 極性尺沒有單一可修的形狀，改尺子案不以極性為切入 |
| M3 | 覆蓋尺的誤殺集中在「合併兩行來源」（句子有據但依據分散在被引用的兩行） | 覆蓋拒的 grounded 句中，`sources_needed ≥ 2` 的比例 | < 50% ⇒ 同上 |
| M4 | 現行尺的**抓到率**在此材料上不低於 4o-mini 材料的 62%（DSP-034 同批值） | 抓到＝被拒無據句 ÷（被拒無據句＋放行無據句）；放行無據句取 H4 一致子集（52：0、53：7、54：4） | 抓到 < 50% ⇒ 尺對新模型的無據形狀失效，列 P1 交業主 |

## 1. 材料（凍結）

- 來源：`rag-orchestrator/.probe-run-{51,52,53,54}/…/texts/outline-probe.jsonl` 的 `attempts[*]`，取 `verdict.ok == false` 且 `reason ∈ {POLARITY_MISMATCH, QUOTE_NOT_COVERING, UNCITED_ASSERTION}` 的**那一句**（`verdict.sent` 索引）與它的 `refs`。
- 每句解析 refs ⇒ 來源行（用與 `probe_analyze.py` 封包器同一把 `_REF_RE` 與正本 `content_units`；解析不到 ⇒ 排除並計數）。
- 封包欄：`pid`（sha 前 10）、`sentence`、`sources`（被引用細目**標題＋被引用行＋同細目其餘行**，與 H4 封包同形；判者另標 `sources_needed`＝支撐該句最少需要幾行）、`reason`（⛔ 不給判者；判者不知拒因、不知輪次、不知模型）。
- 凍結：封包 sha 寫進本檔 §4 後才派判者；⛔ 派後不增刪句。
- 對照組（正對照，防「尺瞎」）：混入 20 句 **已放行且 H4 一致判 grounded** 的句子（不標記），判者對它們的 grounded 率須 ≥ 90%，否則判者尺壞、整批作廢。

## 2. 判者

- 兩位 Claude Code 子代理，互不可見，brief 沿 `judge-brief-h4.md` 加兩欄：`negation_reframe: bool`（句子與來源同義但否定／肯定框架互換）、`sources_needed: int`。
- 一致子集才計；分歧 > 10% ⇒ 第三判者只判分歧句。

## 3. 產出

- `inputs/cut1-verifier-ruler-measure-20260907.md`：M1–M4 表、按拒因分項（極性／覆蓋／無引用各自的誤殺率）、按模型分項（4o-mini 30 句 vs gpt-5-mini 110 句）、誤殺句形狀 top-3（描述，⛔ 不進提示詞）。
- 封包與判者輸出留 scratchpad（含原文，⛔ 不進版控）。
- tasks 4.4 回寫一行；DECISIONS 若觸發改尺子案才新列。

## 4. 凍結紀錄（派判者前填）

- 封包句數：**157**（被拒 **137**＋正對照 20）；排除（refs 解析不到／無引用）：**15**（含 `UNCITED_ASSERTION` 全部——該拒因本來就是「無引用」，不在尺誤殺範圍）；被拒分佈：覆蓋 78／極性 59；輪次 51：30、52：42、53：40、54：25；sha256：`7f540c8e5a75c32f44cc1308fbae82f4089c3c0c462059714972d0e65b233739`。
- 判者派出時間：2026-09-07（業主「核准」後即派）。M1 門檻計算母體＝被拒 137 句的一致子集。

## 5. 預算與停損

- API $0；判者兩位各一次（約 160 句）。
- 停損：正對照 grounded < 90% ⇒ 作廢重派；判者一致 < 80% ⇒ 第三判者；材料 < 100 句可解析 ⇒ 交業主決定是否補跑一輪探針取材（$0.2）。

## 6. 非目標

改尺、調 τ／ratio、加規則、動提示詞、重跑探針；「退回改追問」「方案結構脫離 pricing」「轉人前先收集」三個岔題（業主已列待裁、不排主線）。
