# M-a 乾跑報告：可答性 Workflow 2 格（任務 1.4，2026-09-06）

> 受測物：`.claude/workflows/outline-curation.js`＠`e5d95f1c`；args 由 `answerability_args.py --cell-ids C01,C02` 產（候選 39＝21 列 prospect kb＋18 筆草稿，judgePrompt 各 9,088／9,093 字元）；判者 `effort: low`、模型＝session 模型 `claude-fable-5-1`（未指定 `model`，繼承）。
> 目的：證 pipeline 跑得通、schema 強制生效、`resumeFromRunId` 續跑行為、量出真實 token——⛔ 2 格的一致率不是 rubric 品質證據（分母太小）。

## 1. 三次執行的事實

| 次 | 形式 | agent 數 | 實跑 | subagent token（harness 計） | wall | 結果 |
|---|---|---|---|---|---|---|
| 1 | fresh run `wf_44d75ae7-af1` | 6 | 6 | 338,295 | 221 s | C01 三判者 partial／answerable／answerable ⇒ answerable；C02 answerable／partial／answerable ⇒ answerable；一致率 0/2 ⇒ `needs_rubric_revision=true` |
| 2 | resume，同 args | 4 | **2**（stage 1 兩個 judge1 重跑，key 變）| 112,766 | 54 s | judge2 兩個命中快取；judge1 重跑後 C01 兩者皆 partial、C02 皆 answerable ⇒ 一致率 2/2 |
| 3 | resume，同 args | 4 | **0** | **0** | **29 ms** | 與第 2 次逐位元相同 ⇒ **100% 快取命中** |

- 判者 prompt 逐位元相同（agent transcript 首則 user 訊息 sha256 兩兩比對：C01 `fb88344f…`、C02 `d31fc784…` 三次一致）。
- **done ④（`resumeFromRunId` 續跑快取命中 100%）**：resume→resume 成立；fresh→第一次 resume **不成立**（stage 1 全部重跑、stage 2 全命中，原因在 harness 的 key 計算，非本 spec 可控；已記 Claude Code 回饋）。**對 1.5 的意義**：第一次續跑要預期 55 個 judge1 重跑（≈55×56k token），或改為分批 fresh run、只在必要時 resume。
- schema 強制生效：6 個判者輸出皆通過 `VERDICT_SCHEMA`（`fine_id` ∈ 39 個 enum、`provisional=true`），無重試痕跡（每 agent 恰 2 次 API 呼叫＝首呼＋StructuredOutput 工具回合）。
- 判者隔離：每個 agent 只收到 judgePrompt（journal 與 transcript 無 v1 傳入 judge2／3 的痕跡）。

## 2. 真實 token（transcript `usage` 逐筆加總，第 1 次 6 判者）

| 項 | 值 |
|---|---|
| 每判者呼叫次數 | 2（首呼＋工具回合）|
| 每呼叫 context | ≈56,000 token（judgePrompt ≈7,000＋workflow 子代理系統脈絡與工具 schema ≈49,000）|
| cache_creation | 182,736（前綴首次建立，之後判者共享）|
| cache_read | 493,812 |
| input（非快取） | 24 |
| output | 2,669 |
| harness `subagent_tokens` | 338,295 |
| usd | **未知**——session 內無 Fable 牌價；`cost.json` 先記 0，1.5 前由業主給單價或改判者模型 |

**55 格推估**（2 判者＋不一致第 3；本次 2 格皆不一致，先以 100% 觸發第 3 判者估上界）：165 判者 × ≈112k context token ≈ **18.5M context token**（其中 ≥97% 為 cache read）；harness 計法 ≈ 165 × 56k ≈ **9.3M**。代理數 165 ≤ 步 4 上限 180 ✅；**usd 是否 ≤$3 無法判**——取決於 Fable cache-read 單價，且判者用的是 session 最貴模型。

## 3. 對 1.5 的三個決定（業主裁）

1. **判者模型**：`agent()` 加 `model:'haiku'`／`'sonnet'`（design 元件 2 允許 `effort:'low'`，未指定模型；換模型＝換尺，須先在同 2 格跑對照看 label 是否一致）。
2. **候選內容瘦身**：judgePrompt 7k token 中候選全文佔 ≈6k；改「標題＋首句」可砍 ≈70%，但 rubric 的 `evidence_unit`（第幾句）判定會失去依據——⛔ 不建議，除非 rubric 改。
3. **分批**：11 批 × 5 格 fresh run，避免「第一次 resume 重跑 stage 1」的成本；批間用 `finalize_answerability.py` 累加。

## 4. rubric 觀察（給業主審 rubric 用，⛔ 不是結論）

- C01「你們系統適合我嗎」三判者都選 `tmp:kb:3585`（「想知道適不適合您，先讓我了解一下您的狀況」），但 label 在 answerable／partial 間分歧——這句是**反問**不是回答，rubric 四值沒有涵蓋「候選是反問／導流句」的情況。1.5 前值得在 rubric 加一條定義（例：反問句不構成回答 ⇒ 非 answerable），改了＝新 readiness epoch。
- C02「收租常常忘記」：judge1／3 選 3601（帳務管理）或 5378（催繳提醒），fine_id 不一致但 label 多數一致——`fine_id` 一致率要另計，目前 Reconcile 只算 label。

## 5. 產物

- StepEnvelope：`.claude/skills/outline-curation/runs/2026-09-06T00-00-00Z/answerability-dryrun-2cells.json`（第 1 次結果；`finalize_answerability.py` 依設計 exit 2 因 `needs_rubric_revision=true`）
- journal（gitignored）：`.claude/skills/outline-curation/journal/wf_44d75ae7-af1.json`
- `session.json.answerability.needs_rubric_revision=true` 寫入後，本 session 回合結束時 **Stop hook 確實擋下**（Claude Code 回饋原文：`answerability.needs_rubric_revision=true（rubric 待修）`）⇒ done ② 的 Stop 判定取得線上正對照；乾跑非正式試作，隨後手動移除該鍵放行
- Workflow transcript：`~/.claude/projects/-Users-lenny-jgb-AIChatbot/158ab224-5fb1-4cb1-8d8a-8a4a3d250f04/subagents/workflows/wf_44d75ae7-af1/`

## 6. rubric 0.2.0（業主核可）模型對照（同日續作）

rubric 0.2.0 sha `3d113747…`（補「什麼不算回答」：反問／要資訊／導流句不構成證據）。同 2 格、同 args，只換判者模型：

| 格 | Fable `wf_80b7b6d2-c5f`（4 判者、226,154 token、122 s） | sonnet `wf_f5d56fc2-940`（5 判者、283,342 token、220 s） | label 一致 |
|---|---|---|---|
| C01 | partial×2（3585 第 1 句；信心 low／medium） | no_source×2＋partial（3596） | ✗ |
| C02 | answerable×2（3601） | answerable×2（5378／3596） | ✓ |

事實：
- 新條款生效：兩模型五個 C01 判者**沒有一個**再拿 3585 第 2 句（反問）當證據；Fable 改引第 1 句、sonnet 多數判無可答。
- 每判者 context 兩模型皆 ≈56.7k token——換模型省的是單價，不是 token。
- 模型間 label 一致 1/2；模型內：Fable 2/2、sonnet 1/2（C01 觸發第 3 判者）。n=2，⛔ 不作模型優劣結論。
- C01 的分歧是 rubric 邊界：「系統把物件／合約／收租集中管理」對「適合我嗎」算 partial 還是 no_source——這類「泛適配題」在 55 格裡還有多格（C01–C05 定位類），1.5 一致率會反映它；⛔ 不在 1.5 前再改 rubric（改＝新 epoch），先量。
- fine_id 一致率仍未計（Reconcile 只算 label）。

**1.5 建議**：判者用 `sonnet`（成本），分批 fresh run（11×5 格），一致率 <0.90 的出口＝回修 rubric（design 附錄 C ③）。
