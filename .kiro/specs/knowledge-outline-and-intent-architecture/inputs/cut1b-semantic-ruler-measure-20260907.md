# 子案 1b 報告：兩把語義尺在凍結 338 句上的量測（2026-09-07）——**兩把都不達 DSP-034 門檻，改尺路線再次收束；NLI 最佳點誤殺 26%／抓到 59%，cross-encoder 不是蘊涵訊號**

> Plan：`inputs/plan-cut1-verifier-ruler-measure-20260907.md` §7（預登記）。材料 sha `65fff9fc…`：有據 306（被拒 108＋放行 198）／無據 32（被拒 21＋放行 11）；前提＝線上形狀（被引用 unit 行）。現行尺同批：**誤殺 35.3%／抓到 65.6%**。門檻：誤殺 ≤10% **且** 抓到 > 65.6%。$0；NLI 容器 `aichatbot-nli-model`（sha `405d2ae0…`）、semantic-model `/rerank`。

## 1. 結果

| 尺 | 最佳操作點 | 誤殺 | 抓到 | 判定 |
|---|---|---|---|---|
| 現行（ratio 0.5 ∧ 詞表極性） | — | 35.3% | 65.6% | 基準 |
| NLI（單句前提，τ 掃 0.10–0.90） | τ=0.40（DSP-033 舊值） | 26.1% | 59.4% | 未達（誤殺 ≤10% 的點不存在：τ=0.10 仍 19.9%） |
| NLI | τ=0.65 | 29.1% | 68.8% | 抓到過線、誤殺差 19 點 |
| cross-encoder（rerank 分數） | θ=0.81 | 8.2% | 25.0% | 誤殺過線、抓到只剩 1/4 ⇒ 不是蘊涵訊號 |
| 皆拒才拒（現行 ∧ NLI<τ） | τ=0.3 | 11.8% | 40.6% | 誤殺近線、抓到塌陷 |
| 參考：NLI 以細目全部行當前提（非線上形狀） | τ=0.70 | 23.2% | 65.6% | 同天花板 |

## 2. 為什麼還是同一個天花板

- 被拒的 108 句有據句，NLI 最高 p_ent 中位數 0.79（p25 0.04、p75 0.98）：三分之二能被 NLI 救回（τ=0.4 救 67/108），但剩下三分之一 NLI 也看不出蘊涵——短句改寫「可以，系統支援線上建約。」對一行長來源，模型給的蘊涵分數很低。
- 32 句無據句的 NLI 分佈是雙峰：13 句 ≥0.4（「多講一點」型：來源說可追蹤、句子多說可開單），NLI 判蘊涵。**同義改寫與多講一點在 NLI 眼裡是同一種東西**，這與 DSP-033／034 在 4o-mini 材料上的結論一致（`project_nli_replacement_failed_frozen_constraints`），換材料沒有換掉天花板。
- cross-encoder 量的是「相關」不是「蘊涵」：無據句與來源高度相關，θ 越高放行越多無據句。

## 3. 結論與待業主裁

1. **改尺子案依預登記收束**：兩把語義尺都不達門；⛔ 不調 τ 以外的東西（預登記），⛔ 不看結果改材料。DSP-034 重提條件仍有效，本輪不新列決策。
2. **4.4 的 `budget_exhausted` 門在現行尺下結構性過不了**：尺誤殺 35%（DSP-034 定義）是固定成本，探針每輪 13–42 回合耗盡多數是它。剩下的路只有兩條，都不是量測而是設計，交你裁：
   - (a) **接受現行尺，4.4 以縮窄主張收案**：「候選層通過（gold 在前五 0.9、零故障）；回答層在現行尺下答到率 43%（`low`），誤殺為已知固定成本」，把 `budget_exhausted` 門從 4.4 拿掉、改列觀察；主線推進 5.2／5.3／5.5、7.x。
   - (b) **改輸出契約（DSP-034 (c)）**：要求模型的事實句貼近來源句法（近逐字＋允許截斷／換序，由程式以現行尺量），等於用「少改寫」換「少誤殺」；代價是語氣變生硬，且 `minimal` 已示範照抄會伴隨 JSON 碎片——需另立 Plan 並用同一 338 句離線先量誤殺變化才做。
3. 材料與分數已封存 `~/jgb-archive/agentic-mcp-holdout-20260907/`（含原文，⛔ 不進版控）。
