# Plan R：agentic-MCP 結構性整理（2026-09-10；業主「依序處理」第 ① 項；第 4 稿＝r1 六條＋r2 四條＋r3 兩條措辭全處置；審查輪次達上限，r3 兩條為一致性修正、不再送審，主線裁定可派）

> 依據：帳本 §1t 兩份獨立審查（修正性質：44% 機制／49% 有債／5% 補丁；架構對目標：安全邊界 ✅、結構債三處）。目標是**行為不變的結構整理**，讓後續每輪走查不再往同一支 1,228 行的函式加補丁。
> 紀律：⛔ 不改任何判定與文案（純搬移＋收斂）；每片獨立可驗、可回退；程式一筆／文件一筆；不 push；⛔ 不部署（業主另裁）。
> 現況量測：`runtime.py` 4,274 行、76 個 def；`_run_turn_body` 1,228 行（L3021 起）；`_run_confirm_segment` 216、`_run_select_segment` 146、`_begin_pending_confirm` 129、`_finish_confirm_turn` 116；出口閘四支模組級純函式（`_apply_scope_exit`／`_apply_handoff_without_lookup`／`_apply_ask_target_gate`／`_apply_handoff_data_exits`）；`state["agent"]` 11 個鍵、五種生命週期；`verifier.py` 805 行，三個維度（受眾鍵／`document_turn` 布林／模式集合）各用不同機制。

## 0. 程式封套（三片，序列；每片獨立驗收、獨立可回退）

| 片 | 目標形態 | 非目標 | 預算 | 停止條件 |
|---|---|---|---|---|
| R1 runtime 四段管線 | `_run_turn_body` ≤200 行；四段各自模組級可測：`ProgramSegments`（模型前機器值：文件禁寫固定句、照片三終止、確認兌現、清單點選、handoff 快取重播）→ `TurnContext.inject()`（九段資料注入收成一支，保留 id 由它派生）→ `ModelLoop`（工具迴圈＋Verifier＋兩條迴圈內改寫）→ `ExitGates`（四道閘依序表＋三個狀態寫點＋快取＋dialog＋trace，`_finalize` 升為模組級） | ⛔ 不改任何閘的條件、順序、文案；⛔ 不改 `run_turn` 簽名；⛔ 不動 verifier／facade | 1 executor＋1 verifier | 任一既有測試改語義（只准改 import 路徑）即停；smoke 對照任一指標退步 >1 即停 |
| R2 Verifier 規則自帶屬性 | 規則檔每條 `{id, pattern, class∈{safety,contract,grounding}, audiences, turn_types}`；`verify(out, …, ctx: VerifyContext | None = None)` **加法**新增（r2 #1／r3 #1）：`ctx` 有值以它為準；`audience=`／`document_turn=` 兩個舊 kwarg **原樣保留、語義不變**（轉接成 `VerifyContext`）；⛔ 本片不刪舊 kwarg、⛔ 不改 `runtime.py` 呼叫點、⛔ 不改任何既有測試（`grep -rn "audience=\|document_turn=" rag-orchestrator/tests` 命中檔一律零改動）；廢除舊 kwarg 另立 R2b（R3 之後）；`_is_observed` 查 `class × mode`；`term_id` 改穩定 id（廢 1000／2000 基底）；`VerifierVerdict.observed_counts`（類別→次數，只列舉值；寫進 trace 的落點由 R1b 提供，DSP-040 絆線） | ⛔ 不改任何規則的字面與判定結果；⛔ 不動 pm／prospect 的作用域（現行值原樣搬進屬性）；⛔ 不刪除舊 kwarg、⛔ 不改 `runtime.py` 呼叫點 | 1 security-executor＋1 verifier | fixtures 三組（enforce／grounding_observe／缺值方向）任一翻轉即停 |
| R3 `AgentSession` | 一個類包住 `state["agent"]`（JSON 形狀不變、不遷移）；每鍵宣告 `lifetime∈{turn, once, until_scope_exit, fifo(n), stamp}`；`begin_turn()`／`end_turn(result)` 唯一寫點（取代 `_finalize`／`_finish_confirm_turn`／快取重播三處）；`prompt_segments(scope)` 唯一讀點（取代四段各自的 `if scope_estate_id is None`）；順手修 L8（`state_store.DEFAULT_CONFIG_KEY`） | ⛔ 不改任何鍵的生命週期數值（FIFO 5／20／50、一次性、釘住不寫） | 1 executor＋1 verifier | 任一鍵的序列化形狀改變即停 |

## 0b. 擁有權與先後（plan-verifier r1 #4）

| 片 | 執行者 | 獨佔檔案（其他片 ⛔ 不碰；`runtime.py` 由 R1→R1b→R3 序列接手、與 R1 平行的只有 R2 且 R2 不碰它） | 隔離 |
|---|---|---|---|
| R1 | security-executor | `services/agent/runtime.py`、新模組 `services/agent/turn_segments.py`（模型前程式段）、`services/agent/turn_context.py`（注入＋回合可變狀態容器）、`services/agent/exit_gates.py`（四道閘＋`finalize`）、`tests/unit/agent/test_pipeline_order_req.py`（新）、`tests/unit/agent/test_pipeline_golden_req.py`（新）、`tests/fixtures/agent/pipeline_golden.json`（新）、`tests/unit/agent/test_question_sensitivity_req.py`（僅 §1.1 列明的三條字面斷言改受測物） | worktree |
| R1b（R1 收尾後的小片，同一執行者接續） | security-executor | `runtime.py`：`TurnTrace` 新欄位 `verifier_observed_counts: dict[str,int]`、**verdict→trace 的賦值行**（在 `self.verifier.verify(...)` 取得 verdict 處以 `Counter(verdict.observed)` 累加，⛔ 不依賴 R2 的 `observed_counts`；`TurnTrace` 四個建構點（`runtime.py` 內 grep `TurnTrace(`）一律以 `default_factory=dict` 取預設、只有取得 verdict 的一般出口寫入實值）、`_emit_agent_decision` 白名單加該鍵（值只列舉類別名→整數）；測試：`tests/unit/agent/test_candidate_trace_req.py`（白名單鍵數 24→25 的等值斷言與「新鍵為 `verifier_observed_counts`」＋「白名單仍無原文鍵」正對照；§1.1 例外表第 2 條）、`test_runtime_req.py` 的模組層常數 `_ALLOWED_AGENT_DECISION_KEYS`（frozenset 字面量加 `verifier_observed_counts` 一鍵；⛔ 不動該檔其他斷言；r3 #2）、新 `test_verifier_observed_counts_req.py`（`grounding_observe` 回合的計數與 `verdict.observed` 一致；enforce 下為空；拿掉賦值行必紅） | 序列於 R1 |
| R2 | security-executor | `services/agent/verifier.py`、`services/agent/output_schema.py`（`VerifierRules`／`VerifierVerdict`／`VerifyContext`）、`config/agent_verifier_rules.json`（2.0.0）、`tests/fixtures/agent/known_*.json`、`tests/fixtures/agent/rules_1_6_2.json`（新，term_id 對照）、`tests/unit/agent/test_verifier_req.py`／`test_*_audience_req.py`／`test_document_turn_forbid_req.py`／`test_polarity_pairs_req.py`／新 `test_rule_attributes_req.py`；**⛔ 不含 `runtime.py`、⛔ 不含任何 R1／R1b 擁有的測試**——R2 是加法，呼叫點不需改；日後改用 `ctx=` 是 R2b 的事 | worktree，與 R1 平行 |
| R3 | executor | `services/agent/agent_session.py`（新）、`services/agent/completed_actions.py`、`services/agent/state_store.py`（L8）、`runtime.py` 中對 `agent_state` 的讀寫呼叫點（R1 完成後）、`tests/unit/agent/test_agent_session_req.py`（新） | worktree，序列於 R1／R1b 之後 |

先後：R1 → R1b → 主線接線 → R3；R2 與 R1 平行（檔案交集為空：對照上表逐檔比對）。每片預算：1 executor＋1 fresh verifier，工期上限 R1 三個工作天、R2 兩天、R3 一天；超過即停下交業主。

## 0c. R1 回合可變狀態契約（plan-verifier r1 #2）
`_run_turn_body` 的閉包 `_finalize`／`_build_fixed` 捕獲的可變名稱一律收進**單一容器** `TurnAccumulator`（dataclass，R1 定義在 `turn_context.py`）：`counters`（既有 `BudgetCounters`）、`scope_counts: dict`、`violations: list`、`tool_call_records: list`、`verifier_verdicts: list`、`messages: list`、`tool_results_by_id: dict`、`llm_calls: int`、`prompt_tokens: int`、`completion_tokens: int`、`attempt_no: int`、`cache_key`、`nonce`、`trace_id`、`start`。另有七個在 `_run_turn_body` 內產生、被 `_build_fixed`／`_finalize` 寫進 `TurnTrace` 的名字（r2 #4）：`candidate_ids`、`winning_key_kind`、`miss_kind`、`has_context`（碼上是 `bool(entry_text)`，⚠️ 對帳時以 `entry_text` 為準）、`has_recent_refs`、`pre_lookup`（碼上是 `pre_lookup_trace`）、`doc_trace`（四鍵）——收進同一容器的 `snapshot: TurnInputsSnapshot`（dataclass，段落產生後寫入、之後唯讀）；模組級 `finalize` 需要的 `verifier.rules`／`clock`／`cache`（`agent_state["handoff_cache"]`）由參數傳入。規則：整數計數在迴圈中**改為對容器欄位就地加**，⛔ 不再重新綁定區域變數；list／dict 一律傳同一物件、⛔ 在段邊界複製；四段只接收這一個容器＋不變的輸入（identity／user_message／agent_state／outline）。完整性檢查：R1 執行者以 `inspect`／AST 列出 `_finalize`／`_build_fixed` 的自由變數，逐一對到容器欄位或參數，清單附在 commit 訊息。驗收：新測試「模型迴圈各加一筆計數與 violation 後走固定句出口，`trace.llm_calls`／`violations` 反映最新值」＋ golden 對照 `trace` 各數值欄逐位相同。

## 1. 行為不變的驗收法（每片相同，先定標準）
1. **單元**：`scripts/run-tests.sh unit tests/unit/agent tests/unit/audit` 全綠（基準 2285）；既有測試檔只准改 import 路徑，⛔ 不准改斷言。**唯一例外（plan-verifier r1 #1，列明）**：`tests/unit/agent/test_question_sensitivity_req.py` 以 `inspect.getsource(rt.AgentRuntime._run_turn_body)` 逐字斷言四道閘呼叫寫法與 `_qs_rules = getattr(self.verifier, "rules", None)` 的三條字面斷言——搬移後改掃 `exit_gates` 模組（受測物換成 `inspect.getsource(exit_gates.finalize)` 或等價入口），三條斷言**語義不變**（四道閘依序、`_qs_rules` 由 verifier.rules 取得、傳進 `_apply_handoff_data_exits`／`_apply_handoff_without_lookup`），並加變異正對照：拿掉任一 `_qs_rules` 參數該檔必紅。**例外表（逐檔逐條，皆附變異正對照）**：(1) `test_question_sensitivity_req.py` 三條源碼字面斷言（上述）；(2) `test_candidate_trace_req.py` 的 `len(_ALLOWED_AGENT_DECISION_KEYS) == 24` → `25`＋新鍵名斷言（R1b）；(3) `test_runtime_req.py::_ALLOWED_AGENT_DECISION_KEYS` 常數定義加一鍵（R1b；該檔 `set(captured[0].keys()) == _ALLOWED_AGENT_DECISION_KEYS` 等值斷言與 `test_candidate_trace_req.py` 的 25 鍵斷言須同時綠，拿掉 `_emit_agent_decision` 該鍵必紅）。除此三處，`tests/` 下只准改 import 行。
2. **管線順序表格測試**（新，R1）：對四道出口閘各一個最小 `TurnResult` 形狀，斷言恰好命中一道、其餘不動；再一組「同回合先後」：scope_exit 觸發後其餘三道皆為 no-op、NO_JUDGEMENT 設 `ask_target=confirm_intent` 後 `_apply_ask_target_gate` 不動它。
3. **回放等價**（新，R1／R3；plan-verifier r1 #3）：golden 產生腳本內建於 `tests/unit/agent/test_pipeline_golden_req.py`（`--regen` 由整理前的碼產生 `tests/fixtures/agent/pipeline_golden.json`，整理後比對）。**決定性**：固定 `new_nonce`（monkeypatch 回常數）、`uuid.uuid4`（常數）、`_clock`（`FakeClock`）；`violations` 比對前正規化 `replayed_from:*` → `replayed_from:<id>`；整理前連跑兩次 golden 必逐位相同（否則先修不決定性）。**覆蓋矩陣**（缺一不得收案，腳本輸出矩陣）：九個保留 id（`img-`／`done-`／`entry-`／`aff-`／`ctx-`／`pre-`／`ref-`／`doc-`／`est-`）各 ≥1 條、四道閘各 ≥1 條、handoff 快取重播 ≥1 條、確認兌現／取消／清單點選各 ≥1 條 ⇒ ≥17 條。比對欄位（r2 #4）：`kind`／`outcome`／`answer`／`quick_replies`／`TurnResult.handoff` 全鍵／**`TurnTrace` 全欄位**（以 `dataclasses.fields(TurnTrace)` 逐欄，含非數值；`violations` 正規化後；`trace_id` 固定）／`agent_state` 鍵集合與各鍵序列化值；`--regen` 輸出比對欄位清單，並以變異正對照證明任一欄漏傳（例如 `pre_lookup=None`）必紅。
4. **smoke 對照**（真模型，統計性；plan-verifier r1 #5）：計分尺先升格進 repo `scripts/smoke/`（`mcp_smoke.py`、`b4_score.py`、`liff_score.py`；記 sha）；整理前後同模型（gpt-5.6-luna none）各跑 lb2 三輪＋線③一輪＋b6 一輪。**停止門檻（單位寫死）**：lb2 三輪平均分退步 ≥1.0 分（/13）、線③出卡會話數退步 ≥1、阻擋判決總數增加 ≥1、p50 增加 >1.0 s、p95 增加 >2.0 s——任一命中即停止並回退該片。⚠️ `b4_score.py` 看不見複誦型病灶（帳本 §1o），該類以 golden 與 verifier 守，不靠 smoke。
**整理前基準（2026-09-10，HEAD `73a4cd97` 映像，gpt-5.6-luna none，smoke 容器上限拉高 `AGENT_TURN_CAP=1000`／`RATE_PER_MIN=600`／`KB_GET_CAP=3000`——⚠️ 預設 60 次工具／分鐘會被 luna 的 3 秒回合撞到，帳本 §1u）**：lb2 三輪 11／11／13（平均 11.67）、p50 3.4–4.1 s、p95 6.2–9.0 s、轉人 1；線③ 48 回合 12/12 出卡、0 洩漏、0 轉人、p50 3.2 s、p95 5.6 s；b6 8/8 全接上、p50 4.5 s、p95 6.4 s；阻擋判決 0。結果檔 scratchpad `smoke/base_lb2_r{1,2,3}.jsonl`、`base_liff4.jsonl`、`base_b62.jsonl`。
5. fresh verifier 每片一次；R2 另加 `self_test` 三向 fixtures 與 `rules_sha` 進健檢的驗證。
6b. **R1b 驗收**：`test_verifier_observed_counts_req.py` 三案（observe 模式計數一致／enforce 為空／拿掉賦值行必紅）＋白名單 25 鍵＋無原文鍵正對照；`decision_snapshot` 不變量 30 仍綠。
6. **`make audit` 與模組落點**（plan-verifier r1 #6）：R1／R3 新模組一律落在 `services/agent/**`（不變量 27／30／32 以此 glob 靜態掃描，⛔ 不另立頂層套件）；驗收含 `make audit`——先 `docker compose -f docker-compose.dev.yml build rag-orchestrator` 並重建常駐 dev 容器再跑，不變量 3 若仍紅須列成因與正對照；整理後 `check_27`／`check_30`／`check_32` 掃到的檔數 ≥ 整理前（腳本輸出檔數）。

## 2. 回退
每片一筆 commit，回退＝revert 該筆；R1 的檔案搬移用 `git mv` 保留歷史；R2 規則檔改版時保留 1.6.2 副本於 `tests/fixtures/agent/rules_1_6_2.json` 供 term_id 對照測試。

## 3. 之後（依序 ②③④，另立 Plan）
② 寫入面機器值下限：`agent.turn` 訊息接受封閉機器值 `action:<name>`（同 `select:`／`confirm_*:` 慣例），程式段直接組候選或卡、不經模型；line-bot 的「延期」「報修」鍵送它。③ 房東管家敏感類：正本 E 加定義＋`SENSITIVE` 以受眾分集合（候選：租金異動、租客個資、法律／糾紛建議）。④ 延遲命題：規格 p95 ≤12 s（quality）／程式預設 20 s／部署 45／60 s 三值對齊成一張表寫進架構文 §6，並加逐次模型呼叫毫秒進 `agent_turn` log。
