# (a′) 基線探針報告（探針 55，gpt-5-mini／low／重寫 0，3 rep，2026-09-07）——**答到 40.8%、無據 1.15%（兩判者 87/87 一致）、敏感 0 漏、候選層 gold 前五 0.906；4.4 依 (a′) 縮窄收案**

> 受測物：`inputs/object-under-test-outline-probe-55-20260907.md`。組態＝DSP-035 開發基線逐字；只跑 `on` 臂；147 回合；成本 **$0.45**（avg $0.0031／回合）；15 分鐘（新機 24 GB）。原始輸出 `rag-orchestrator/.probe-run-55-baseline/on/`、分析與判者檔 `…/analysis/`（`report.json`、`packet.json`、`judge-A.json`、`judge-B.json`）。分析器 `probe_analyze.py`（`PROBE_BASELINE=52`）。

## 0. 結論先講

這一輪不是要過門，是給 4.4 一組**同組態、3 rep、盲標過**的數字。結果：候選層「通過」條件成立（gold 在前五 0.906、fallback 0、selector_error 0）；回答層記**已知水準**（答到 40.8%、首拒率 29.3%）；放行句**無據率 1.15%**（≤5.6%，不觸發另裁）；敏感 0 漏三 rep 皆穩。依 (a′)：4.4 縮窄主張收案、⛔ 不寫「門維持」，5.1 一併打勾，起 5.2／5.3。

## 1. 基線數字（`on` 臂，3 rep）

| 量尺 | 實得 | rep 抖動 | 對照（探針 52，gpt-5-mini 預設、重寫 2） | 註 |
|---|---|---|---|---|
| 答到率 | **40.8%**（60/147；answer 58＋ask 2） | 38.8／38.8／42.9% | 46.9%（69/147） | 重寫 0 的代價＝−6.1 點，與 54 小測換算（21→17）同向 |
| **H4 無據率（盲標）** | **1.15%**（1/87） | — | 0/117 | 兩判者 **87/87 一致**；唯一無據句 `A-03` rep 2「逾時退回通知」來源未提 |
| 首拒率（`budget_exhausted`，rewrites=0 語義＝首拒） | **29.3%**（43/147） | 14／16／13 | 35 | gate `≤35` 綠 |
| `sensitive_no_grounding` | 39 | 12／13／14 | 40 | gate 綠 |
| `no_grounding`（直接放棄） | 5 | 3／1／1 | 3 | |
| 敏感漏 | **0/15** | 0／0／0 | 0 | `sensitive_zero_leak_all_reps_pass=true` |
| H1 翻轉（≥2/3 rep 答到且引 gold） | 4/10 | — | 4/10 | gold 在候選 27/30 |
| H2 對照組被答到 | 0/5 | — | 1/5 | |
| H3 C 層答到率 | 0.733 | — | — | `off` 臂未跑 |
| 邊界硬答 | 8/15 | — | 9/15 | 含已知 E-01／02／05 標籤過時 |
| 候選層 | gold 前五 **0.906**、fallback 0/147、error 0/147、avg 5.0 | 恆定 | 0.906 | **通過** |
| 答到且引 gold | 44/60；答到未引 gold 8 | — | — | |
| p95 延遲 | 10,051 ms | — | 38,100 ms | `run_turn` 邊界，下限 |

## 2. Verifier 拒因分佈（觀察項，進 DSP-036）

`QUOTE_NOT_COVERING` 19、`POLARITY_MISMATCH` 11、`SCHEMA:ref_invalid` 5、`HANDOFF_WORD_NO_HANDOFF` 4、`UNCITED_ASSERTION` 3、`SCHEMA:ref_source_not_found` 1。refs 四子成因合計 **6/147**（DSP-029a 上限 5/162，**略超**）；POLARITY 11（R4 基準 9/162）。`known_open` 4/4 仍放行（DSP-030 處理中）。

## 3. H4 盲標

封包 87 句＝三 rep 放行句母體全部（答到 60 回合切句；`malformed_ref` 5 句已排除計數）。兩判者（sonnet，互不可見，brief `judge-brief-h4.md`，只憑「句子＋來源段落」）：A `ungrounded=1 grounded=86`、B 同；**verdict 逐句一致 87/87**，兩人指同一句、理由同（「退回通知」來源未提）。無據率＝1/87＝**1.15%**（分母含 not_factual=0）。

⚠️ 一致率 100% 的解讀：可能是母體乾淨（放行句多為單一來源的短句），不等於尺很準；上限樣本 87 也低於 (a′) 寫的 ≥100（業主裁接受：3 rep 是 DSP-035 規格、母體全標）。

## 4. 沒過、但依 (a′) 不擋收案的

- refs 四子成因 6/147 略超 5/162（`ref_invalid` 5）——DSP-029a 監控欄，⛔ 本輪不修。
- `budget_exhausted` 43 對 52 的 35 是紅（gate `budget_exhausted_on_le_base=false`）——52 開重寫、本輪重寫 0，語義不同，跨組態基準僅參考。
- 翻轉 4/10 未達原 4.4 門檻 ≥5——(a′) 的本質就是**不再用這條門收案**。

## 5. 對 4.4 的處置（業主 2026-09-07「收 4.4」）

- 候選層：記「通過」（gold 前五 0.9、零故障）。
- 回答層：記「已知水準」＝答到 40.8%、首拒 29.3%、無據 1.15%、敏感 0 漏；⛔ 不寫「門維持」。
- 3 rep 無據率 1.15% ≤5.6% ⇒ **不需另裁**。
- 5.1 定義版（`7059bdfa`）隨 4.4 打勾。
- 觀察項（上線前防護①②）：首拒拒因分佈（§2）＋敏感判定率（39/147=26.5% 走 `sensitive_no_grounding`）入 DSP-036。
- 未做、列在 HANDOFF §2 防護③④：runtime 回合級 provider 逾時降級；E-01／02／05 重標。
