# Plan 程式封套 KOIA-M-d：回合接線——候選注入（4.1）、身分槽位（4.2）、評估工具兩臂（4.3）（2026-09-07）

> 狀態：**S1 收案（2026-09-07）**：executor＋fresh verifier CONFIRMED（15 探針全過；unit 1049／integration 210／`make audit` PASS）。P4 三條 defer：形狀違規在 `from_selection` 即以 `candidate_selector_error` 收掉（fail-closed 仍成立、只是標籤不同）；`_select_outline` 的 except 內再呼叫降級建構子、若它自身 raise 會外拋（無可達輸入）；`app` 啟動 fail-soft 列印 `{e}` 全文（觀察到的訊息乾淨）。取捨：主 session 重建本機 audit 容器（不變量 3 需要；本機非線上）。executor 額外動了 `test_agent_router_unit_req.py`（3 案 monkeypatch `agent_configured=False`，因本機 `.env` 有 `AGENT_SHADOW_AUDIENCES` 使新紅規則在無關測試觸發）與 `test_trace_view_req.py`（閉集加 `candidates`）。**下一步：S3（§3）送 plan-verifier → 業主核 → mech-executor；再 S2（§4）security-reviewer → plan-verifier → 業主核 → security-executor。** 業主核准（2026-09-07，§5 六項照建議；§5-6 選 (a) 歸 S2）。前一輪：收尾審查回 1 P2＋2 P3，主 session 逐條 FIX 後交業主核——P2：b2c 模式 prospect 接線後零內容 ⇒ 加驗收案 §2.4-11、加 violation `candidate_none_visible`、列 §5-6 裁決；P3：`agent_configured()` 判準逐位元搬移（`0`／`false` 為未啟用）；health／fine_index 的「不致紅」docstring 同步改寫。前史（第 2 輪 REVISE 2 條皆 FIX：§2.4-1 合成正本改 ≥6 可見已審細目、`len(candidate_ids)==K`、`ZZUNSELECTED` 加正對照；§2.4-9 裸 id 對 `kb_get` 是 `INVALID_INPUT`、對 `resolve_canon_section` 才是 `NO_MATCH`，§2.3 加「不改 `kb_get`」。第 1 輪 REVISE 4 條皆 FIX：政策句明講 `outline:` 前綴＋新增前綴／裸 id 正反測試；`CandidateSelector` 加唯讀 `index` property；health 紅條件改用新公開 `mcp_facade.agent_configured()`；prepare 逾時狀態為 `absent` 非 `not_ready`）。核准前 ⛔ 不寫碼。本輪只送 **封套＋S1（4.1）** 審 READY；S2（4.2）屬身分／信任邊界，**先過唯讀 `security-reviewer`、再 plan-verifier**，在 S1 收案後送；S3（4.3）在 S1 收案後送 plan-verifier（它依賴 S1 的介面）。4.4 步 2 探針另立 Plan（4.4a 定義凍結交業主審）。前置：3.7 收案 `b61d8dd7`（`FineIndex` 三鍵）、3.3 `CandidateSelector`、3.2 `canon_visible`／`build_canon_toc`／`resolve_canon_section`。

## 0. 結論先講

回合前用 3.3 的 `CandidateSelector` 選 K=5 個可見細目，組成 `CandidateOutlineDoc`（K 細目節＋每回合依身分算的 `outline:toc`）當作既有 `outline` 參數餵給 `build_messages` 與 `_seed_outline_provenance`（同一物件）；選不到／索引不可用／例外 ⇒ 降級為「**可見**細目全集＋toc」（不是未過濾的整份），trace 記 `candidate_fallback_full_outline` 或 `candidate_selector_error`；`decision_snapshot.agent` 加 `candidate_ids`／`winning_key_kind`／`miss_kind` 三鍵（封閉白名單同步、id 形狀守門），影子與 `trace_view` 鏡射。主／影子共用同一個已 `prepare` 的 selector。身分槽位（S2）與評估工具兩臂（S3）各自一片，順序執行：**S1 → S3 → S2**（三片都動 `runtime.py`／同一批測試檔，平行寫入的合併成本高於節省；tasks 的「(P)」是相依關係不是義務）。

## 1. 封套（program envelope）

| 項 | 內容 |
|---|---|
| 目標 | 回合真的只看被選的細目、用入口身分作答；評估工具能跑「有候選 vs 無候選」兩臂，供 4.4 探針量翻轉率 |
| 切片 | S1＝4.1 候選注入（`executor`／effort 高）；S3＝4.3 `agent_eval` 兩臂（`mech-executor`）；S2＝4.2 身分槽位（`security-executor`；句型表先交業主核） |
| 順序與相依 | S1 先（定義 `TurnTrace` 三欄、`build_runtime(candidate_selector=…)`、`CandidateOutlineDoc`）；S3 依 S1 介面；S2 只依 S1 的 `run_turn` 版位（`_slots_for_prompt` 之後），與 S3 無關 |
| 驗證 | S1 fresh `verifier`（主張見 §2.7）；S3 [自驗]＋併入 S1 verifier 的整合邊界（JSONL 鍵集合＝runtime trace 欄位）；S2 fresh `verifier`（security 主張） |
| 非目標（封套） | 不做 4.4；不改 Verifier 尺；不動 `PromptAssembler.build()` 簽名（R11.5 `inspect.signature` 白名單）；不動 `build_visibility_predicate`；不接 reranker；不調 K；不做 5.x 對話邏輯（CTA／handoff／`_POLICY_TEXT` 三段定義）——**唯一例外**：S1 改政策文一句（§2.1-9，R5.9 第二次機會需要） |
| 回滾 | 各片獨立 revert；S1 revert 後 runtime 回到整份正本（現況） |
| 預算 | 全程假 embedding／假模型，$0；產線啟動多一次 `prepare`（358 鍵、45 批） |
| 停損 | 任一片 verifier REFUTED 兩次 ⇒ 停；S1 的「不可見細目內容不得進 text」若在降級路徑做不到 ⇒ 停下交裁，⛔ 不以「反正 prospect 全可見」放行 |

## 2. 切片 S1（4.1）：`CandidateOutlineDoc`＋runtime 注入＋trace 三鍵

### 2.1 結果（outcome）
1. **`services/agent/outline.py`：`CandidateOutlineDoc`**（pydantic `BaseModel`，與 `OutlineDoc` 同型別家族；滿足 `OutlineDocLike`）：欄位 `audience`／`version`／`sha256`／`token_count`／`sections: list[OutlineSection]`／`text`／`token_count_approx`。兩個建構子：
   - `from_selection(full: OutlineDoc, sel: Selection, toc: OutlineSection)`：`sections`＝依 `sel.candidate_ids` 順序從 `full.sections` 取（id＝fine id；找不到的 id ⇒ `ValueError`，⛔ 不靜默跳過）＋末尾 `toc`；`audience`／`version`／`sha256` **一律複製自 `full`**（Plan v3 P1-2：⛔ 不填 `identity.resolved_audience()`，否則 `test_outline_audience_mismatch_fails_closed` 恆真）；`text` 用與 `_build_doc` 同一組裝式（`【id】title\ntext` 以 `_SECTION_SEP` 接）；`token_count` 用 `_build_doc` 同一估算器。
   - `from_visible(full, visible_ids: frozenset[str], toc)`：降級用——`sections`＝`full.sections` 中 id ∈ `visible_ids` 者（正本序）＋`toc`。這是 3.2 verifier P3 債的修補點：**降級也只給可見細目**，⛔ 不是未過濾整份。
   - 兩者都不含 `outline:toc` 以外的非細目節；`full` 自帶的啟動期靜態 toc **不沿用**（它是靜態身分算的）。
2. **`services/agent/runtime.py`：`AgentRuntime.__init__` 加 `candidate_selector: Optional[CandidateSelector] = None`**（`build_runtime` 的 `**runtime_kwargs` 已透傳，⛔ 不改 `build_runtime` 簽名本體；`test_build_runtime_passes_through_runtime_kwargs` 可延伸）。`run_turn` 在 `outline = agent_state.get("outline")` 之後、`_seed_outline_provenance` 之前插入 `outline, sel_meta = await self._select_outline(identity, outline, user_message, dialog, violations)`：
   - 適用條件：`self._candidate_selector is not None` 且 `outline` 非 None 且 `get_canon(outline.audience)` 有註冊且 `outline.audience == identity.resolved_audience()`；不適用 ⇒ 原 `outline` 原樣、`sel_meta = None`（`miss_kind=None`、`candidate_ids=[]`、無 violation——這**不是**降級，是「這條路沒有候選機制」，例如評估工具 `--candidates off` 或非 prospect 受眾）。
   - 查詢＝`user_message` ＋ `dialog` 中最後一則 `role=="user"` 的 `content`（無則單句），單一空格連接（Plan v3 程式規則）；⛔ 不 log 查詢。
   - `sel = await selector.select(canon, identity, query, vendor_business_types=frozenset())`（prospect 正本走 b2b 分支，業態集合不參與）。⚠️ 事實：`canon_visible` 下 **b2c 身分對 prospect 正本不可見**；但**現況回合**拿到的是整份大綱（`build_outline` 未套 `canon_visible`＝3.2 債）。而 `mcp_facade.parse_identity` 對缺漏／非法 `mode` 一律落 `b2c`（`_DEFAULT_MODE`）、`audience_of` 對 `target_user=="prospect"` 不看 `mode` ⇒ `Identity(target_user="prospect", mode="b2c")` 可達且會進選取，接線後由「整份」變「零內容」（`none_visible` ⇒ 空 toc）。這是產品行為改變，列 §5-6 業主裁；本片使其**可觀測**：`none_visible` 除記 `miss_kind` 外另記 violation `candidate_none_visible`（與 `candidate_fallback_full_outline`／`candidate_selector_error` 三者互斥、S3 報表分列）。
   - `sel is None` ⇒ `visible = selector.index.visible_subset(identity, canon, vendor_business_types=frozenset())`——**`CandidateSelector` 現無公開 `index`**（只有 `self._index`）：本片在 `candidate_selector.py` 加一個唯讀 `@property index -> FineIndex`（比照 `FineIndex.backend` 的唯讀 property 先例，⛔ 無 setter、不改任何邏輯）；`visible_subset` 不依賴索引狀態（3.3 文件明寫「未 prepare 照樣算得出」），故降級時可用、`outline = CandidateOutlineDoc.from_visible(full, visible, toc)`、`violations.append("candidate_fallback_full_outline")`、`miss_kind="index_unavailable"`、`candidate_ids=[]`。
   - `sel["miss_kind"] == "none_visible"` ⇒ `from_visible`（空）＋ `miss_kind` 照記＋ `violations.append("candidate_none_visible")`（⛔ 不記 fallback 字串——不是機制故障，但必須在 trace 大聲）；`"no_candidate"`（ready 下不可達）⇒ `from_visible`（全可見）＋ `miss_kind` 照記、不記 violation。
   - `hit` ⇒ `from_selection(full, sel, toc)`；`candidate_ids=sel["candidate_ids"]`、`winning_key_kind=sel["winning_key_kind"]`、`miss_kind="hit"`。
   - 整段包 `except Exception`（⛔ 不吞 `CancelledError`）⇒ 同 `sel is None` 路徑但 violation 記 `candidate_selector_error`（Plan v3 P2-2）、`logger.warning` 只印例外型別名。
   - `toc = build_canon_toc(canon, identity, vendor_business_types=frozenset())`（每回合、依身分；3.2 已驗它套 `canon_visible`）。
   - **同一個 `outline` 物件**接著餵 `_seed_outline_provenance(outline)` 與 `self.assembler.build_messages(identity, outline, …)`（unit 以 `is` 斷言：monkeypatch 兩處記下收到的物件）。
3. **`TurnTrace` 加三欄**：`candidate_ids: list[str] = []`、`winning_key_kind: dict[str, str] = {}`、`miss_kind: Optional[str] = None`；`run_turn` 三個 `TurnTrace(...)` 構造點（handoff 快取重播、`_build_fixed`、正常路徑）都帶入（重播路徑 `[]`／`{}`／`None`）。
4. **`_emit_agent_decision` 字面 dict 加三鍵**（不變量 30 靜態掃描要字面量）；同步 `tests/unit/agent/test_runtime_req.py::_ALLOWED_AGENT_DECISION_KEYS` 加三鍵（白名單 13→16；「多一鍵必紅」自證測試保留）。**形狀守門**在 `_emit_agent_decision` 前：`_CANDIDATE_ID_RE = re.compile(r"[a-z_]+/[A-Z]/[a-z0-9-]+")`；任一 id 不 `fullmatch`、或 `len > K`、或 `winning_key_kind` 的鍵不在 `candidate_ids` 內、或值不在 `{"title","phrasing","content"}` ⇒ 該回合 `candidate_ids=[]`、`winning_key_kind={}`、`violations += ["candidate_ids_shape_invalid"]`（fail-closed、⛔ 不 raise 進熱路徑）；unit 正對照塞一個違規 id 必見該 violation。`miss_kind` 值域封閉 `{hit, none_visible, no_candidate, index_unavailable, None}`。
5. **`services/agent/shadow.py::_trace_to_dict`** 與 **`services/agent/trace_view.py::render_trace`** 鏡射三鍵（`render_trace` 放 `"candidates": {"ids": …, "winning_key_kind": …, "miss_kind": …}`；`_assert_no_verbatim` 照跑——值只有 id／列舉）。
6. **`app.py::_init_agent_runtime`**：`build_prospect_outline` 之後 `canon = get_canon("prospect")`；`index = FineIndex(EmbeddingUtilsBackend())`；`await asyncio.wait_for(index.prepare(canon), PREPARE_TOTAL_TIMEOUT_S)`（新常數 `services/agent/canon/fine_index.py::PREPARE_TOTAL_TIMEOUT_S = 60.0`，整份 prepare 的硬上限）；⚠️ 事實：`wait_for` 逾時是取消協程、索引狀態**停在 `absent`**（`prepare` 只在完成或 `_discard` 時改狀態；`_entries` 只在末段整批指派，取消不留半份），後端層級失敗才是 `not_ready`——兩者對 selector 都是「非 ready ⇒ 回 `None`」、對 health 都是紅；app 捕 `TimeoutError`／`Exception` 後只印一行、⛔ 不掛啟動、⛔ 不另加 API 把 `absent` 改寫成 `not_ready`；`register_index("prospect", index)`；`selector = CandidateSelector(index)`；主 runtime 與影子 factory **傳同一個 `selector` 實例**（`candidate_selector=selector`）。`FineIndex.prepare` 失敗不是啟動紅（design 錯誤處理表：`not_ready` ⇒ 回合走整份正本、health 紅）。
7. **`services/agent/health.py`**：`_index_state` 不變；`compute_agent_health` 的 `red` 條件加一項：`mcp_facade.agent_configured()` 為真且 `canon.index.prospect.state != "ready"`（含 `absent`、`not_ready`）⇒ 紅（design 錯誤處理表「health `not_ready`／`absent` 紅」；3.3 時不致紅是因為尚未接線，接線後「索引不在」就是產線降級中）。**唯一權威判定**：新增公開函式 `services/agent/mcp_facade.py::agent_configured() -> bool`，判準**逐位元搬自** `app.py::_agent_configured`（三個 env `AGENT_AUDIENCES`／`AGENT_SHADOW_AUDIENCES`／`AGENT_TURN_ENABLED` 任一 `strip()` 後不在 `{"", "0", "false", "False"}`；⛔ 不是「任一有值」，`AGENT_TURN_ENABLED=0` 仍是未啟用），`app.py::_agent_configured` 改為呼叫它的薄別名、名稱保留給既有測試；同步改寫 `health.py::_index_state` 與 `fine_index.py::index_registry_states` docstring 的「⛔ 不致紅」語句為「4.1 接線後：agent 開關任一開且非 ready ⇒ 紅」；`health.py` 已 `from services.agent import mcp_facade`，以模組屬性呼叫 `mcp_facade.agent_configured()`（⛔ 不 `from … import`，讓 monkeypatch 生效；⛔ 不 import `app`——`routers/agent.py` import health、`app.py` import router，會成環）。⚠️ `mcp_facade.agent_turn_enabled()` 只看 `AGENT_TURN_ENABLED`，語義不等價、⛔ 不拿它當紅條件。unit：既有「三態不致紅」測試改為「`agent_configured()` 假 ⇒ 不致紅；真 ⇒ `not_ready`／`absent` 紅、`ready` 不紅」。
8. **`tools/agent_eval.py::_set_outline_on_state` 不動**；S3 再接兩臂。
9. **`services/agent/agent_rules.py::_POLICY_TEXT`** 只改一句（Plan v3 P1-1）：「⛔ 不需要、也不要為了引用大綱去呼叫 `kb.get`。」→「資料段是與問題最相關的幾個章節；若都不相關，可用 `kb.get` 讀目錄列出的章節整節後再引用，id 形狀為 `outline:` 加上目錄那一行的章節 id。」（定義、不寫例子）；unit：政策文不含原禁止語、含允許語且含 `outline:` 形狀說明。⚠️ 事實：目錄行是**裸** fine id（`build_canon_toc` 的 `f"{fine.id}｜{title}"`），而 `kb_get` 只把 `outline:` 前綴交給 `resolve_canon_section`、裸 id 走整數路徑必失敗、`resolve_canon_section` 對裸 fine id 亦回 `NO_MATCH`——所以政策句必須明講前綴；⛔ 不改 `build_canon_toc` 的行格式（3.2 收案物）、⛔ 不改 `resolve_canon_section`。

### 2.2 現況事實（主 session 2026-09-07 實查；查證指令附）
- `run_turn(self, identity, user_message, state)`：`slots = _slots_for_prompt(state)` → `dialog = agent_state.get("dialog", [])` → `outline = agent_state.get("outline")` → `_seed_outline_provenance(outline)` 進 `tool_results_by_id[OUTLINE_TOOL_CALL_ID]` → `self.assembler.build_messages(identity, outline, slots, dialog, tool_specs, nonce)`；`violations: list[str]` 在回合開頭建立；`TurnTrace(...)` 三個構造點（重播、`_build_fixed`、正常）。`grep -n "agent_state.get(\"outline\")\|_seed_outline_provenance(outline)\|TurnTrace(" rag-orchestrator/services/agent/runtime.py`
- 大綱進 `state["agent"]["outline"]` 的三個入口：`services/agent/mcp_facade.py`（`_app_state(deps, "agent_outline")`，進場前塞、存檔前 pop）、`routers/agent_entry.py`（同法）、`tools/agent_eval.py::_set_outline_on_state`。都塞**同一個行程級 prospect `OutlineDoc`**，不分身分。`grep -rn "agent_state\[\"outline\"\]" rag-orchestrator/services/agent/mcp_facade.py rag-orchestrator/routers/agent_entry.py rag-orchestrator/tools/agent_eval.py`
- `build_prospect_outline`：`load_canon_or_die` → `register_canon("prospect", canon)` → `build_outline(canon)`（**未套 `canon_visible`**，逐細目一節、`citable=fine.reviewed_by is not None`、未審者 `text=""`）＋ 啟動期以靜態身分 `Identity(vendor_id=0, target_user="prospect", mode="b2b")` 算一次 `build_canon_toc` → `_build_doc` → `check_budget`。`sed -n "$(grep -n 'def build_prospect_outline' rag-orchestrator/services/agent/outline.py | cut -d: -f1),+40p" rag-orchestrator/services/agent/outline.py`
- `app.py::_init_agent_runtime`：主 runtime `build_runtime(db_pool, provider, _mcp_registry, outline_doc=…)`；影子 `ShadowRunner(lambda readonly_view: build_runtime(…, outline_doc=outline_doc, readonly_view=readonly_view), db_pool)`；無任何 index／selector；失敗只在 `_agent_configured()` 時升啟動紅。`grep -n "_init_agent_runtime\|build_runtime(\|ShadowRunner(" rag-orchestrator/app.py`
- `bootstrap.build_runtime(db_pool, provider, registry, *, outline_doc=None, budget=None, rules_path, fixtures_dir, **runtime_kwargs)` 同步函式，`**runtime_kwargs` 直傳 `AgentRuntime(...)`；`AgentRuntime.__init__` 現有 `readonly_view`、`attempt_sink`、`clock`、`stage`。`grep -n "def build_runtime" -A12 rag-orchestrator/services/agent/bootstrap.py`
- `_emit_agent_decision` 以字面 dict 呼叫 `usage_metering.set_agent_decision`，13 鍵；白名單常數在 `tests/unit/agent/test_runtime_req.py::_ALLOWED_AGENT_DECISION_KEYS`（測試端），`test_agent_decision_snapshot_keys_match_closed_whitelist_exactly` 攔截呼叫比對「恰好相等」；不變量 30 `scripts/audit/checks/agent_boundary.py::check_30_decision_snapshot_no_verbatim` 只擋 `DECISION_BANNED_KEYS={answer,quote,text,user_message}`（加 id 鍵不觸發）且要求 agent 路徑至少一個呼叫。`grep -n "_ALLOWED_AGENT_DECISION_KEYS = " -A16 rag-orchestrator/tests/unit/agent/test_runtime_req.py`
- `shadow._trace_to_dict` 逐欄位字面量（無 `asdict`）；`trace_view.render_trace` 回封閉鍵 dict 後 `_assert_no_verbatim(view)`。`grep -n "def _trace_to_dict\|def render_trace" rag-orchestrator/services/agent/shadow.py rag-orchestrator/services/agent/trace_view.py`
- `PromptAssembler.build(identity, outline, slots, dialog, tool_specs, nonce)`：outline 只用 `.sections`（`id`／`title`／`text`／`citable`）、`.sha256`／`.version`／`.audience`；audience ≠ identity 受眾 ⇒ `ValueError`（`test_outline_audience_mismatch_fails_closed`）；簽名由 `test_build_signature_has_no_channel_for_tool_results` 鎖。`OutlineSectionLike`／`OutlineDocLike` 為 Protocol（`services/agent/prompt_assembler.py`）。
- `CandidateSelector.select(doc: CanonDoc, identity, query, *, vendor_business_types=frozenset()) -> Optional[Selection]`；`Selection = {candidate_ids, winning_key_kind, scores, miss_kind}`；`K=5`；`FineIndex.visible_subset(identity, doc, *, vendor_business_types)`；`build_canon_toc(doc, identity, *, vendor_business_types)` 回 `OutlineSection(id="outline:toc", citable=False)`；`get_canon(audience)`／`register_index`／`get_index` 皆已存在。
- `_POLICY_TEXT` 含「⛔ 不需要、也不要為了引用大綱去呼叫 `kb.get`。」。`grep -n "kb.get" rag-orchestrator/services/agent/agent_rules.py`
- 測試夾具：`test_runtime_req.py` 的 `FakeProvider`／`FakeRegistry`／`FakeVerifier`／`FakeAssembler`／`_runtime(...)`／`_identity(...)`；`test_agent_turn_unit_req.py`、`test_agent_entry_req.py`、`test_mcp_facade_unit_req.py` 為入口層測試；3.3 的假 embedding 後端樣式在 `tests/unit/agent/test_candidate_selector_req.py::_FakeBackend`（明示向量、未列文字正交）。
- 已知不一致（不在本片解）：`services/agent/outline.py` 的 `OutlineDoc` 為 pydantic、design 元件 7 草圖寫 `@dataclass(frozen=True)`——本片採 pydantic（與 `OutlineSection` 同家族、`_build_doc` 可重用）。

### 2.3 非目標（S1）
不動 `PromptAssembler.build` 簽名；不改 `candidate_selector.py`／`fine_index.py` 邏輯（`candidate_selector.py` 只加唯讀 `index` property；`fine_index.py` 只加 `PREPARE_TOTAL_TIMEOUT_S` 常數）；不做身分槽位（S2）；不做 `agent_eval` 兩臂（S3）；不做 CTA／handoff／三段定義（5.x）；不改 Verifier；不改 `resolve_canon_section`；⛔ 不改 `services/agent/tools/kb.py::kb_get` 的路由與錯誤碼；不做視窗告警；不改 `mcp_facade`／`agent_entry` 的大綱塞入方式（仍塞整份，選取在 runtime 內做）。

### 2.4 驗收（容器內；`scripts/run-tests.sh unit tests/unit/agent/ tests/unit/_meta/ -rA`、`integration tests/integration/agent/`、`make audit`）
1. **候選路徑一回合**（合成正本 **≥K+1＝至少 6 個**對 prospect 可見且已審的細目——selector 無分數門檻、一律取前 K=5，可見已審 ≤5 時候選＝全部、「未選」斷言恆真；假 embedding 後端：5 個細目各有一把鍵與查詢向量分數遞減、第 6 個（內文帶 `ZZUNSELECTED`）分數最低；假模型回一句引用候選細目的 fact）：`len(candidate_ids)==K`、順序＝分數序；`tool_results_by_id["outline"]` 的 provenance 恰為這 5 個 id＋`outline:toc`；`outline.text`、system 資料段、provenance 都不含 `ZZUNSELECTED`；**正對照**：同一材料改走 `candidate_selector=None` ⇒ `ZZUNSELECTED` 出現在 prompt（證明斷言看得見它）；`winning_key_kind` 值域三值、`miss_kind="hit"`、無 fallback violation。⛔ 不改 `K` 常數；若需小 K 案，monkeypatch `candidate_selector.K` 並在測試內註明。
2. **降級路徑一回合**：(a) 後端 raise ⇒ `sel is None` ⇒ outline＝可見細目全集＋toc、violations 含 `candidate_fallback_full_outline`、`miss_kind="index_unavailable"`、`candidate_ids=[]`；(b) selector 本身 raise（monkeypatch `select`）⇒ violations 含 `candidate_selector_error`（且不含 fallback 字串）；(c) `candidate_selector=None`（評估基線）⇒ outline 原樣、無 violation、`miss_kind=None`。正對照：計數器「跑過」（先故障一回合再正常一回合，violation 1→0）。
3. **同一物件**：monkeypatch `_seed_outline_provenance` 與 `assembler.build_messages` 記下 outline 參數，`is` 相等；候選與降級兩路徑都斷。
4. **不可見細目內容不得進 text**（3.2 債）：合成正本含一個 `target_user=[property_manager]` 細目（對 prospect 不可見）且其內文帶標記 `ZZHIDDEN`；候選路徑與降級路徑的 `outline.text`、system prompt、provenance **都不含** `ZZHIDDEN`；正對照：把該細目改成 prospect 可見 ⇒ 降級路徑 text 含它。
5. **toc**：每回合 `build_canon_toc` 依身分算；toc 節 `citable=False`；假模型引用 toc 的 ref ⇒ Verifier `SOURCE_NOT_CITABLE`（既有語義，沿 `test_runtime_req` 現有 toc 案）。
6. **trace 三鍵**：`_ALLOWED_AGENT_DECISION_KEYS` 16 鍵恰好相等；多一鍵必紅自證保留；形狀守門正對照（塞 `outline:bad#p1` ⇒ `candidate_ids=[]`＋`candidate_ids_shape_invalid`；長度 6 ⇒ 同）；`shadow._trace_to_dict` 與 `render_trace` 含三鍵且 `_assert_no_verbatim` 綠；不變量 30 `make audit` PASS。
7. **bootstrap／app**：`build_runtime(candidate_selector=obj)` 透傳到 `AgentRuntime._candidate_selector`（沿 `test_build_runtime_passes_through_runtime_kwargs`）；`_init_agent_runtime` 以假 provider／假 embedding 跑一次：`get_index("prospect").state=="ready"`、主 runtime 與 shadow factory 建出的 runtime 持有**同一個** selector（`is`）；後端永不回應 ⇒ 在 `PREPARE_TOTAL_TIMEOUT_S` 內返回、`get_index("prospect").state in {"absent","not_ready"}`（永不回應 ⇒ 實際為 `absent`）、啟動不 raise、selector 對該索引 `select` 回 `None`（以縮短的 timeout 注入測；斷言耗時上界）。
8. **health**：monkeypatch `services.agent.mcp_facade.agent_configured` 為假 ⇒ 三態不致紅（既有差分斷言）；為真 ⇒ `not_ready`／`absent` 紅、`ready` 不紅；另一案：三個 env 各自單獨設值 ⇒ `agent_configured()` 真、全清 ⇒ 假、`AGENT_TURN_ENABLED=0`／`false` ⇒ 假（正對照）。
9. **政策文與第二次機會**：政策文不含原禁止句、含允許句與 `outline:` 形狀說明；**新測試**（`tests/unit/agent/`）：對 prospect 身分 `kb_get("outline:<可見已審 fine id>")` 回該細目內容且 `citable=True`；`kb_get("<裸 fine id>")` ⇒ `INVALID_INPUT`（`kb_get` 對非 `outline:` 前綴且非純數字的 id 回 `INVALID_INPUT`，這是既有路由）、`resolve_canon_section(canon, identity, "<裸 fine id>")` ⇒ `NO_MATCH`（兩條分開斷言；正反對照，⛔ 不寫成「既有測試」、⛔ 不為了讓測試綠而改 `kb_get` 的錯誤碼）；`kb_get("outline:toc")` 回目錄 `citable=False`。
10. 隱私：候選與降級全流程 `caplog`＋`capsys` 無查詢字串、無細目文字（正對照先證擷取器活著）。
11. **b2c 模式的 prospect 身分**：`Identity(vendor_id=1, target_user="prospect", mode="b2c")` 跑一回合 ⇒ `miss_kind="none_visible"`、violations 含 `candidate_none_visible`、`candidate_ids=[]`、provenance 只有空 toc、prompt 不含任何細目內文；**正對照**：同材料改 `mode="b2b"` ⇒ 候選路徑正常（`hit`、無該 violation）。此案釘住的是 §5-6 裁決前的**過渡行為**（可觀測的零內容），裁決若選 (a) 入口正規化則本案期望改為「與 b2b 相同」。
12. 既有 `test_runtime_req`／`test_prompt_assembler_req`／`test_agent_turn_unit_req`／`test_agent_entry_req`／`test_mcp_facade_unit_req`／integration agent 全綠；`make audit` PASS。

### 2.5 回滾／預算／停損
revert `outline.py`／`runtime.py`／`app.py`／`shadow.py`／`trace_view.py`／`health.py`／`agent_rules.py`／`fine_index.py`（常數）與測試。$0。停損：§2.4-4 在降級路徑做不到 ⇒ 停；verifier REFUTED 兩次 ⇒ 停。

### 2.6 對 design 的偏離（待回寫）
- 降級＝「可見細目全集＋toc」（`from_visible`），不是 design 寫的「整份正本」——為了 3.2 債（tasks 4.1 前置債明列）；prospect b2b 身分下兩者內容相同。
- `miss_kind=None` 代表「這條路沒有候選機制」（`candidate_selector=None` 或受眾無正本），與 `index_unavailable`（機制故障）分開；`none_visible` 記 `candidate_none_visible`（可觀測、非故障）；`no_candidate` 不記 violation。
- b2c 模式的 prospect 身分：接線後從「整份大綱」變「零內容＋大聲 violation」（§5-6 待裁；現況整份是 3.2 債的副作用，不是設計）。
- `CandidateOutlineDoc` 為 pydantic 非 dataclass；toc 每回合依身分重算，不用啟動期靜態 toc。
- health 紅條件在 agent 啟用時生效（3.3 的「不致紅」限於未接線期）。
- 政策文一句在本片改（design 把政策文歸 5.1）。

### 2.7 fresh verifier 主張（S1）
「候選路徑只有被選細目＋toc 進 prompt／provenance，降級路徑只有可見細目＋toc；不可見細目內容在兩路徑都不出現；同一 outline 物件餵兩側；trace 三鍵值域封閉、白名單恰好 16 鍵、形狀違規 fail-closed；主／影子共用同一 selector；prepare 逾時不掛啟動（索引留 `absent`、回合降級）；agent 開關任一開時索引非 ready ⇒ health 紅；無查詢／細目文字外洩；既有測試與 audit 全綠。」

### 2.8 派工紀律（進 brief）
⛔ 禁 `git stash`／`checkout --`／`reset`／`clean`／`add`／`commit`；⛔ 不讀 `.env`、不印金鑰；⛔ 不動別窗檔（`docs/INDEX.md`、`.kiro/specs/testing-traceability/`、`docs/presales-assistant-quality-audit-20260902.md`、`scripts/review_coverage.py`、`.claude/DECISIONS.md.lock`）；⛔ 不動 `rag-orchestrator/canon/*`；⛔ 不重建產線容器（本機 audit 容器已於 3.7 重建；若 `make audit` 不變量 3 因映像過期而紅，回報而非自行重建）；log／assert 訊息不得帶正本或查詢文字。

## 3. 切片 S3（4.3）：`agent_eval` 兩臂（`mech-executor`；狀態：**收案 2026-09-07**——verifier CONFIRMED 22 探針、unit 1064；執行中兩次補刀：真 provider on 臂 selector 未接線（原稿順序錯）、索引先於 key 檢查會在缺 key 時打 embedding API；P4 三條留：`--chain old` 仍印 candidates 節、`--chain both` 的 fallback 分母含 old 列（`avg_candidates`／`gold_in_candidates_rate` 分母正確）、真臂 off 省略 kwarg 而假臂傳 None）

### 3.1 結果（outcome）
> 第 1 輪 plan-verifier REVISE 2 條 P2 皆 FIX（2026-09-07）：報表三計數改由 `EvalRecord.candidate_violations` 承載（不進 JSONL）；exit 5 走 `_build_prospect_index` 注入縫。第 2 輪 REVISE 2 條 P2 皆 FIX：exit 5 以模組層例外 `IndexNotReady` 傳到 `main()`（⛔ 不改 agent 鏈 tuple 回傳契約）；縫自行取得 canon（不依賴 `build_real_runtime`），§3.4-4 改為允許替身但 exit 由受測碼發出。兩輪自動 REVISE 已到上限 ⇒ 主 session 逐條處置後開一次收尾審查。**收尾審查再回 2 P2 皆 FIX**（三個 chain 函式的 `candidates` 參數與函式層預設 `off`／`main()` 顯式傳值；`meta` 承載 `candidates_mode`／`candidates_backend` 經 `render_report_md(candidates_meta=)`、`run_old_chain(candidates_mode=)` 回填）——依規則不再開審查輪，交業主核（核准＝本輪讀證）。

1. **`--candidates {on,off}`（CLI 預設 `on`）**：評估專用依賴注入、⛔ 不是線上開關、⛔ 不讀 env。**簽名與預設（寫死）**：`run_agent_chain(scenarios, *, set_name, provider_kind, …, candidates: str = "off")`、`_run_agent_chain_openai(…, candidates: str)`、`_run_agent_chain_fake(…, candidates: str)`——**函式層預設 `"off"`**（既有以 library 方式呼叫的測試 `test_build_real_runtime_is_monkeypatchable`／`test_provider_openai_without_key_refused_before_any_network` 不進索引縫、不建 `EmbeddingUtilsBackend`），`main()` **一律顯式傳入 CLI 值**（CLI 預設 `on`）。`_run_agent_chain_*` 回傳的 `meta` dict 增 `candidates_mode`（CLI 值）與 `candidates_backend`（值域 `{"fake","embedding","none"}`：`off` 臂＝`none`；`on`＋假 provider＝`fake`；`on`＋真 provider＝`embedding`）；`main()` 取出後以新 kwarg `candidates_meta: dict` 傳給 `render_report_md(...)`（簽名加一個關鍵字參數、預設 `None` ⇒ 不印 `candidates` 節）。`run_old_chain(...)` 增 `candidates_mode: str` 參數，old 鏈列的 `candidates_mode` 填 CLI 實際值、`candidate_ids=[]`、`miss_kind=None`、`gold_in_candidates=None`、`candidate_violations=()`（28 鍵對所有列成立）。`off` ⇒ 每個 runtime 以 `build_runtime(..., candidate_selector=None)` 建（S1 語義：`miss_kind=None`、無 violation）。`on` ⇒
   - 假 provider 路徑（`--provider fake`）：`canon = get_canon("prospect")` 若未註冊則 `load_canon_or_die(resolve_canon_dir(), "prospect")`＋`register_canon`；`index = FineIndex(_DeterministicBackend())`（決定性假向量：`sha256(NFKC(text))` 前 4 位元組正規化，樣式沿 `tests/unit/agent/test_fine_index_req.py::_vector_for`，⛔ 不用 `hash()`）；`await index.prepare(canon)`；`selector = CandidateSelector(index)`；**同一個 selector** 傳給該次 run 的每個 `build_runtime(candidate_selector=selector)`。假向量下的候選只證「機制接上」，不證品質——report 標 `candidates_backend=fake`。
   - 真 provider 路徑：**注入縫**＝新模組層 `async def _build_prospect_index(canon=None, *, backend=None) -> FineIndex`：`canon is None` ⇒ **與假 provider 路徑同一式**取得（`get_canon("prospect")`，未註冊則 `load_canon_or_die(resolve_canon_dir(), "prospect")`＋`register_canon`——⛔ 不依賴 `build_real_runtime` 有沒有先登記）；`backend=None` ⇒ `EmbeddingUtilsBackend()`；內含 `await asyncio.wait_for(index.prepare(canon), PREPARE_TOTAL_TIMEOUT_S)`，逾時／例外只回 `absent`／`not_ready` 的索引、⛔ 不 raise。**exit 5 的唯一傳遞形式**：新模組層例外 `class IndexNotReady(RuntimeError)`；`_run_agent_chain_openai` 在 `candidates=="on"` 時**先**呼叫縫、若 `index.state != "ready"` ⇒ `raise IndexNotReady(state)`，就緒才 `selector = CandidateSelector(index)` 並以 **`build_real_runtime(candidate_selector=selector)`**（新關鍵字參數、預設 `None`、透傳到 `build_runtime(..., candidate_selector=...)`；⛔ 這是本片唯一動到 `build_real_runtime` 簽名的地方）建 runtime——**順序＝`_require_openai_key()`（自 `build_real_runtime` 抽出的同一前置檢查）→ 索引 → 就緒閘門 → `build_real_runtime(candidate_selector=)`**——執行中第二次發現：索引若先於 key 檢查，CLI 預設 `on` 會在缺 key 時先打 embedding API（`test_provider_openai_without_key_refused_before_any_network` 在本機仍綠只因容器連得到 embedding-api）；已修，該測試加 `_build_prospect_index` 呼叫計數 0 的斷言。**索引先於 runtime、selector 必須真的接進 runtime**（2026-09-07 執行中發現：原稿寫「取得 build_real_runtime 結果之後」導致 mech-executor 只做就緒閘門、selector 未接線、真 provider on 臂 `miss_kind` 恆 None；已修）（⛔ 不改 `_run_agent_chain_openai`／`run_agent_chain` 的 `tuple[list[EvalRecord], dict]` 回傳契約——回 int 會在 `main()` 的 `agent_records, agent_meta = run_agent_chain(...)` 解包處炸）；`main()` 把 `run_agent_chain(...)` 呼叫包在 `try/except IndexNotReady` ⇒ `return EXIT_INDEX_NOT_READY`（新常數 `= 5`；3／4 已占）**在任何 JSONL／report 寫出之前**；⛔ 不以降級臂充當 on 臂（那會把「機制壞了」量成「候選沒用」）。report 標 `candidates_backend=embedding`。
   - 也要把 outline 塞進 state：假 provider 路徑現在 `outline_doc=None`（`_run_scenario_agent(outline_doc=None)`）⇒ S1 的適用條件（`outline` 非 None）不成立、`miss_kind` 恆 `None`。故 `on`／`off` 兩臂在假 provider 路徑都要 `outline_doc = build_outline(canon)`＋靜態 toc（沿 `build_prospect_outline` 的組法但不打 DB；抽一個不需 pool 的 helper 或直接呼叫 `build_prospect_outline(None)`——它的 `db_pool` 已不使用，executor 對碼確認）。
2. **`--set outline-probe`**：`--set` choices 加 `outline-probe`；新 loader `load_outline_probe_v1(path, manifest) -> list[Scenario]`，檔形狀 `{"_meta": {"rule_sha256": str, "frozen_at": str, "n": int}, "items": [{"id": str, "q": str, "stratum": str, "gold": [fine_id…], "expect_kind": "answer"|"handoff"}]}`；每 item 一個單輪 `Scenario`（`sensitive=False`；`stratum=="sensitive"` 者 `sensitive=True`、`expect_kind="handoff"`）；`gold` 進 `Turn`（新選填欄位 `gold_fine_ids: tuple[str,...]`，⛔ 其他 loader 不填）。manifest（`.kiro/specs/agentic-mcp-orchestration/eval/samples-manifest.json`）新增 `outline-probe` 項：`{"available": false, "path": null, "sha256": null, "note": "4.4a 凍結後補值"}` ⇒ 現行 UNAVAILABLE 分支退出碼不變（同 `traffic`）。
3. **JSONL 加四鍵**：`candidate_ids: list[str]`、`miss_kind: str|null`、`candidates_mode: "on"|"off"`、`gold_in_candidates: bool|null`（`gold_fine_ids` 非空時 `bool(set(gold) & set(candidate_ids))`，否則 `null`——4.4「gold 不在候選對照」的直接量）。同步 `tests/unit/agent/test_agent_eval_req.py::_EXPECTED_JSONL_KEYS`（24→28，恰好相等）；`_NO_VERBATIM_KEYS` 不變。**`EvalRecord` 另加承載欄 `candidate_violations: tuple[str, ...]`**（`_build_agent_record` 從 `result.trace.violations` 取 `{candidate_fallback_full_outline, candidate_selector_error, candidate_none_visible, candidate_ids_shape_invalid}` 四個字面；old 鏈列為 `()`），**⛔ 不進 `to_jsonl_dict`**（維持 28 鍵）；報表三個計數一律由此欄算、⛔ 不由 `miss_kind` 反推——事實：S1 對「selector 回 None」與「selector 例外」都記 `miss_kind="index_unavailable"`，只有 violations 分得開。
4. **report 加**：`candidates` 節：`candidates_mode`、`candidates_backend`、`candidate_fallback`＝violations 含 `candidate_fallback_full_outline` 的回合數／n、`candidate_selector_error` 回合數、`candidate_none_visible` 回合數、`avg_candidates`（只算 `miss_kind != null` 的回合）、`gold_in_candidates_rate`（只算 `gold_in_candidates != null`）；`--repeat` 摘要對 `candidate_fallback` 與 `gold_in_candidates_rate` 加 mean/min/max。`--chain both` 時 old 鏈列**不帶**三鍵值（`candidate_ids=[]`、`miss_kind=null`、`candidates_mode` 仍填，供對照）。
5. 兩 arm 報表須標 `candidates_mode=on` 為 production 組態、`off` 為評估基線（Plan v3 §9.4）。

### 3.2 現況事實（Scout D 2026-09-07＋主 session 對照）
- `tools/agent_eval.py`：`--set` choices `["topics","scenarios","sensitive","traffic"]`（required）、`--chain {old,agent,both}`、`--provider {fake,openai}`、`--repeat`、`--manifest`（預設 `DEFAULT_MANIFEST_PATH`＝`.kiro/specs/agentic-mcp-orchestration/eval/samples-manifest.json`，**存在**、5.9 KB，`sets` 有 topics／scenarios／sensitive／traffic(available:false)）；loader `load_topics_scenarios`／`load_scenarios_v1`／`load_sensitive_v1`；`verify_manifest` sha 不符 exit 3（`EXIT_SAMPLES_CHANGED`）、`available:false` exit 4（`EXIT_SAMPLE_UNAVAILABLE`）；假 provider 路徑 `build_runtime(db_pool=None, provider=ScriptedFakeProvider, registry=build_fake_registry(), **runtime_kwargs)`（`runtime_kwargs` 只有 `attempt_sink`）；真 provider 路徑 `build_real_runtime` 先驗 `OPENAI_API_KEY` 再 `build_prospect_outline` → `build_runtime(..., outline_doc, readonly_view=True)`；`_run_scenario_agent(runtime, identity, turns, *, outline_doc=None, attempts_buffer=None)` 每輪 `_set_outline_on_state`、回合後 pop；`EvalRecord.to_jsonl_dict` 24 鍵、`_NO_VERBATIM_KEYS` 凍結；report `render_report_md` 有 hardlines／supplementary／DSP-028／repeat 四節。`grep -n "add_argument\|^def load_\|EXIT_\|def to_jsonl_dict\|def render_report_md" rag-orchestrator/tools/agent_eval.py`
- `test_agent_eval_req.py::_EXPECTED_JSONL_KEYS` 硬寫 24 鍵、直接與 `row.keys()` 比對；`test_sha_mismatch_exits_3`、`test_traffic_unavailable_exits_4`；夾具 `_write_manifest`。
- S1 介面（已收案 `191fc1a5`）：`TurnResult.trace.candidate_ids: list[str]`、`.winning_key_kind`、`.miss_kind: Optional[str]`、`.violations`；`build_runtime(candidate_selector=…)` 透傳；`miss_kind=None` ⇔ 無候選機制。
- ⚠️ Scout D 曾誤報 manifest 不存在；主 session 已以 `ls` 對照推翻。

### 3.3 非目標
不改 runtime／selector；不做 4.4a 選題（`outline-probe` 樣本檔由 4.4a 產生並凍結，本片只接 loader 與 manifest 佔位）；不改既有三個 set 的 loader 與 sha；不動 `_NO_VERBATIM_KEYS`；不加 env 開關。

### 3.4 驗收
1. `--candidates off`＋fake：每列 `candidates_mode=="off"`、`miss_kind` 全 `null`、`candidate_ids==[]`；`on`＋fake：`candidates_mode=="on"`、每列 `miss_kind in {"hit","none_visible","index_unavailable"}`、prospect 身分下至少一列 `hit` 且 `1 ≤ len(candidate_ids) ≤ 5`；report `candidates` 節存在且 `candidate_fallback==0`、`candidate_selector_error==0`。**兩格互為正對照**：(a) 假後端在查詢時 raise ⇒ `candidate_fallback==n` 且 `candidate_selector_error==0`；(b) monkeypatch `CandidateSelector.select` raise ⇒ `candidate_selector_error==n` 且 `candidate_fallback==0`。
2. `_EXPECTED_JSONL_KEYS` 28 鍵恰好相等；`_NO_VERBATIM_KEYS` 測試仍綠。
3. `--set outline-probe`：manifest `available:false` ⇒ exit 4；夾具寫一份可用的 outline-probe 檔＋manifest sha ⇒ 正常跑、`gold_in_candidates` 為 bool；sha 改動 ⇒ exit 3。
4b. 真 provider `on` 臂**真的接線**：以替身 `build_real_runtime` 捕捉 kwargs，斷言 `candidate_selector` 是 `CandidateSelector` 且其 `.index` 為縫回傳的同一物件（`is`）；`off` 臂 ⇒ `candidate_selector is None`；索引未就緒 ⇒ `IndexNotReady` 在 `build_real_runtime` **被呼叫之前**拋出（替身計數為 0）。
4. 真 provider 路徑（不設 `OPENAI_API_KEY`、不觸網、不觸 DB）：**允許**沿既有先例把 `agent_eval.build_real_runtime` 替換為回假 runtime handle 的替身（只為越過 key 檢查與 pool；替身⛔ 不發 exit、⛔ 不登記 canon），canon 由 `_build_prospect_index` 自行從 `resolve_canon_dir()` 取得（容器內 `/app/canon/prospect.md` 存在）；以 monkeypatch `agent_eval._build_prospect_index` 的 `backend` 預設（或包一層傳 `backend=`）注入**永遠回 `None` 的假後端** ⇒ `agent_eval.main([...]) == agent_eval.EXIT_INDEX_NOT_READY == 5`、`out_dir/<set>.jsonl` 不存在（exit 由受測的 `_run_agent_chain_openai`／`main` 發出，不是替身）；正對照：同一注入點給可用的決定性假後端 ⇒ 回傳 0 且 JSONL 寫出、`candidates_backend` 值照縫的實際後端型別記；既有 `test_build_real_runtime_is_monkeypatchable`／`test_provider_openai_without_key_refused_before_any_network` 不改仍綠。
5. grep：`tools/agent_eval.py` 與 `services/` 無讀取 candidates 相關 `os.environ`。
6. 既有 `test_agent_eval_req.py` **不改** `test_build_real_runtime_is_monkeypatchable`／`test_provider_openai_without_key_refused_before_any_network` 的情況下全綠，且以 monkeypatch 計數器證明這兩案執行中 `_build_prospect_index` **未被呼叫**（正對照：`--candidates on` 路徑會呼叫）；`scripts/run-tests.sh unit tests/unit/agent/test_agent_eval_req.py -rA`；`make audit` PASS。
7. `--chain both --candidates off`（假 provider＋假 old 鏈）跑完：`<set>.jsonl` 每列（含 `chain=="old"`）鍵集合恰為 28 鍵且 `candidates_mode=="off"`；report `candidates` 節 `candidates_backend=="none"`；`--candidates on`＋fake ⇒ `"fake"`；注入永遠回 `None` 的後端 ⇒ exit 5、無 report。

### 3.5 待業主核（S3）
1. 真 provider 路徑 `prepare` 失敗 ⇒ exit 5 而非降級（避免把故障量成「候選沒用」）。
2. 新增 `gold_in_candidates`（4.4 的直接量）——tasks 4.3 未列，屬小幅加寬。

## 4. 切片 S2（4.2）：身分槽位（**業主核准 2026-09-07（§4.4 四項照建議）→ plan-verifier（第 1 輪 REVISE 3 P2 皆 FIX：`_STATS` 三處同名欄位／鍵、`derive_identity_source` 落點與逐形狀驗收、S2a／S2b 切法）→ 第 2 輪 REVISE 2 P2 皆 FIX（既有 `build_messages` slots 恰好相等斷言逐檔列名改法；§4.3-8／9 歸 S2a）→ 收尾審查回 1 P2 FIX（MCP 側先正規化 target_user 再決 mode；加 list 形狀案）→ 依規則不再開審查輪、以業主既有核准為讀證 → `security-executor`（S2a）**；security-reviewer 唯讀前置審 2026-09-07 已完成，13 條發現逐條處置如下）

### 4.1 結果（outcome）
1. **入口正規化（§5-6 業主裁 (a)）**：新增純函式 `services/agent/identity.py::normalize_entry_mode(target_user, mode) -> str`（與 `audience_of` 同層、同一判準來源）：`target_user == "prospect"` ⇒ 一律 `"b2b"`（含缺漏、非法、**明送 `"b2c"`**——F-3：`audience_of` 對 prospect 本就不看 mode，放著會有「受眾說 prospect、謂詞說 b2c」的分裂身分；⛔ 不 400（DSP-011：入口是上游可信輸入，不在線上端點新增外顯失敗）、⛔ 不放著（失敗形狀是靜默零內容）；其餘 `target_user` 維持原值／預設 `b2c`。**兩個入口都呼叫**：`services/agent/mcp_facade.py::parse_identity` 與 `routers/agent_entry.py::build_identity`。**時序約束（收尾審查 P2）**：MCP 側必須先 `tu = _normalize_target_user(payload.get("target_user"))`（payload 合法形狀含 list，`_effective_target_user` 對 `["prospect"]` 回 `"prospect"`），再 `mode = normalize_entry_mode(tu, payload.get("mode"))`、`Identity(target_user=tu, mode=mode, …)`——⛔ 不把 raw payload 值餵給 `normalize_entry_mode`（raw list 對 `== "prospect"` 為 False ⇒ 留下 F-3 要消滅的分裂身分）；REST 側 `build_identity` 的 `target_user` 已是字串、順序同理——F-1 事實：`routers/chat.py::VendorChatRequest.mode` pydantic 預設 `'b2c'`，`agent_entry` 現行 `("b2b" if target_user == "prospect" …)` 只在明送 null／空字串才觸發，**現況 REST 未帶 mode 的 prospect 落 b2c**，S1 的 `candidate_none_visible` 零內容在產線可達。**可觀測**：正規化實際改寫了 mode 時計數——事實：`mcp_facade._STATS` 是 `_PremiseStats` **dataclass 實例**（固定欄位、走屬性不走鍵）、`premise_stats()` 回字面 dict、`health.compute_agent_health` 的 `"premise"` 是字面鍵表 ⇒ 三處都要加：`_PremiseStats` 新欄位 `identity_mode_normalized: int = 0`、`premise_stats()` 新鍵同名、`health.py` `"premise"` 區塊新觀測值同名（**不進 `red_flags`、不致紅**；`services/agent/health.py` 因此列入本片可動檔）；另 `logger.info` 一行只印 target_user／原 mode／新 mode（⛔ 不印 payload）。REST 入口沒有 `_STATS` ⇒ REST 側只 log 不計數（明列取捨）。
   - **資料範圍變更逐字說明（F-2，ACCEPT、業主知情）**：prospect 由 b2c 改 b2b 後，`build_visibility_predicate`／`canon_visible` 走 b2b 分支＝`business_types && ['system_provider']`（⛔ 無 `IS NULL` 放行）且 `target_user` 參數不含 `'all_users'` ⇒ **多看到**全域 `system_provider` 池、**少看到**業態 NULL／`all_users` 列；跨業者隔離由 `vendor_ids && [vendor_id]` 條件不變維持；受眾／工具可見性／jgb2 個資閘不變（`audience_of` 對 prospect 先行返回；`_identity_gate_ok` 看雙證）。⚠️ 影響及於**所有** MCP 工具（`kb.get` 的 `fetch_visible_row` 跟著換池），不只 `agent.turn`。與 memory「prospect 一律 b2b 謂詞（31 列）」一致。
2. **身分派生（F9）**：`run_turn` 在 `slots = _slots_for_prompt(state)` 之後**強制覆寫**：`slots["identity"] = identity.resolved_audience()`、`slots["identity_source"] = derive_identity_source(identity)`。⛔ 不用 `setdefault`；⛔ 不回寫 `state`（`_slots_for_prompt` 回的是新 dict，覆寫只影響本回合 prompt）；⛔ 不落 DB。**`identity_source` 形狀對照表（F-7，依 `docs/jgb2-chat-integration.md` §3／§4／§8 契約）**：

   | 身分形狀 | 來源 | `identity_source` |
   |---|---|---|
   | pm／tenant：帶 `role_id`（＋`user_id`），`vendor_id` 由 AI 側解 | REST／MCP | `entry` |
   | prospect：`mode=b2b`、不帶 `role_id`／`user_id`；REST `vendor_id=0`、MCP `vendor_id`＝key 所屬業者 | REST／MCP | `anonymous` |

   | MCP 未知／缺 `target_user`（`_normalize_target_user` ⇒ `tenant`）且無 `role_id`／`user_id` | MCP | `anonymous` |

   規則＝`"entry" if (identity.role_id is not None or identity.user_id is not None) else "anonymous"`，落點 **`services/agent/identity.py::derive_identity_source(identity) -> Literal["entry","anonymous"]`**（與 `normalize_entry_mode`、`audience_of` 同層）——⛔ 不看 `vendor_id`（MCP 的 vendor_id 是 key 的業者、不是使用者身分；本 Plan 舊稿「jgb2 面板 prospect 帶 vendor_id>0 ⇒ entry」與契約衝突，**採契約**、舊稿作廢）。事實：`AGENT_TURN_SPEC` stage 只開 prospect ⇒ `entry` 分支今天無活流量，契約測試以 pm／tenant 身分構造並在測試 docstring 註明。偽造：REST 呼叫端可送任意 `role_id` 把它翻成 `entry`（F-6，ACCEPT）——因 F10 護欄它只影響對話控制、不進任何謂詞，`role_id` 的信任由上游承擔（DSP-011）；Plan 明寫「不是認證訊號」。
3. **三道守門（F-4）**：(1) `_slots_for_prompt` 內 deny `identity`／`identity_source`（來自儲存側的同名鍵一律丟棄、`logger.warning` 只印鍵名）；(2) `tools/session.py::write_slot` 首行 `key in SLOT_KEYS` 否則 raise（它是 `collected_data.slots` 唯一 DB 寫入點、現無自驗）；(3) `identity`／`identity_source` ⛔ 不入 `SlotKey`（`parse_slot_key` 回 None ⇒ `INVALID_INPUT`）。
4. **`SlotKey` 加四值**（票 B）：`identity_detail`／`team`／`pain`／`interested`；`SLOT_KEYS` 由 enum 反射、schema 自動同步。同步 `tests/unit/agent/test_session_confirm_tools_req.py::test_slot_key_enum_is_closed_six_values`（六→十，⛔ 維持「恰好相等」、不得改成子集斷言，F-12）。**已知取捨（F-9）**：四鍵是模型自由文字（≤120 字）、長期落 jsonb 並每回合回灌 prompt，`identity_detail` 字面邀請寫入人名／公司——本片以政策文定義句約束值域（下條），保存期限與最小化留為已知債（§4.4）。
5. **政策文一句（F-8 選 (a)，design「身分反問禁令」列本就把它放 `agent_rules.py`）**：`_POLICY_TEXT` 加一句定義（⛔ 不寫例子）：「`identity` 槽位由系統依入口填入、代表對方受眾，`identity_source=entry` 時不得再詢問對方身分；`identity_detail` 只填角色子類（如個人房東／包租代管），⛔ 不填姓名、公司名、聯絡方式。」unit：政策文含此定義句。
6. **AST 護欄（F10／F-5）**：**新寫**掃描器（新測試檔內、與 `find_retired_symbol_usages` 並存、⛔ 不改它——它只看 Import／Attribute／Name，原理上看不到 `slots["identity"]`）：① taint 種子＝對 `slots`（或任何名為 `slots`／`flat` 的 dict）的 `Subscript(Constant ∈ {identity, identity_source, identity_detail})` 與 `.get("…")`，單層區域變數傳遞；② sink 封閉表：`dataclasses.replace(identity, …)`／`dataclass_replace`／`Identity(...)` 重建身分、`build_visibility_predicate`、`fetch_visible_row`、`canon_visible`、`build_canon_toc`、`resolve_canon_section`、`FineIndex.visible_subset`、`CandidateSelector.select`、`registry.call`／`ToolRegistry.call`、`jgb2.*` 五個 `query_*`、`kb_get`／`kb_search`；③ 走訪 sink 呼叫的 `args`／`keywords` 子樹找 tainted 名；④ **每種 sink 形狀各一段假碼正對照必紅**（含 `dataclass_replace` 那條——codebase 已有 `mcp_facade.namespaced_identity` 用它）、乾淨樣本必綠；⑤ 沿 `_iter_agent_service_files` 的「目錄非空否則大聲失敗」。掃描範圍 `services/agent/**`。
7. **`tests/unit/agent/test_identity_no_reask_req.py`（R4.2／R4.3）**：`identity_source=entry`（pm 身分）回合輸出不得匹配「身分反問句型表」；正對照：`anonymous`＋候選細目依子身分分叉（R4.3）的正例必須匹配得到；**非空守門**：先斷言每欄位句型數 ≥1（空表下否定斷言恆真，F-11）；清空表 ⇒ 正例必紅；比對**兩側**都 NFKC；主張縮為「**agent 路徑內**不重問」（F-10：`fallback_old_chain` 後舊鏈讀 `collected_fields` 不讀 `slots`，仍會重問——列已知債）。
8. **句型表**：`services/presales_gate.py` 同層常數 `IDENTITY_REASK_PATTERNS`（依 `inputs/kb3645-presales-dialogue-rules-20260906.md` 五欄位 identity／scale／team／pain／interested 草擬、每欄位 ≤6 句型、NFKC、只用於測試與掃描）；**⛔ 不進 prompt**：新增掃描斷言 `agent_rules.py`／`prompt_assembler.py`／`outline.py` 不得引用該符號（F-11：`presales_gate.SENSITIVE` 已有被 `agent_rules._SENSITIVE_LINE` 反射進 `_POLICY_TEXT` 的先例）。

**執行切法（業主閘門在中途 ⇒ 兩個可獨立收案的單位）**：
- **S2a**（`security-executor`，一次派工）：§4.1-1～6、9 全部程式改動＋§4.3-1～5、7、8、9 驗收＋fresh verifier；**末件**＝句型表草稿寫成 `inputs/identity-reask-patterns-draft-20260907.md`（五欄位各 ≤6 句型、NFKC 後文字、每句附一行「為何算反問」），⛔ 不寫 `IDENTITY_REASK_PATTERNS` 常數、⛔ 不寫 R4.2 測試，交回主 session 即停。
- **業主閘門**：主 session 呈句型表草稿，業主核可（可刪改）。
- **S2b**（`security-executor`，第二次派工，閘門後才起）：把核可後的表落成 `IDENTITY_REASK_PATTERNS`＋§4.1-7 `test_identity_no_reask_req.py`＋不進 prompt 掃描＋§4.3-6 驗收＋fresh verifier。
9. **不進 decision snapshot（F-13，DEFER）**：`identity_source` ⛔ 不加進 trace 白名單（不變量 30 抓不到這類鍵，唯一守門是測試端白名單＋影子／trace_view 閉集；影子 `_emit_metering` 已記 mode／target_user／role_id／user_id，事後可重算）。

### 4.2 非目標／已知取捨
可動檔：`services/agent/identity.py`、`services/agent/mcp_facade.py`、`routers/agent_entry.py`、`services/agent/runtime.py`（只 `_slots_for_prompt` 與其呼叫點）、`services/agent/tools/session.py`、`services/agent/health.py`（只 `premise` 觀測值）、`services/agent/agent_rules.py`（一句）、`services/presales_gate.py`（S2b）、`inputs/identity-reask-patterns-draft-20260907.md`（S2a 末件）；測試：新檔（identity／AST／no-reask）＋既有需同步者逐檔列名——`tests/unit/agent/test_agent_turn_unit_req.py::test_next_turn_reads_the_slot_written_by_the_tool`（現斷 `seen["slots"] == {"unit_count": "600"}`，覆寫後多兩鍵：改為「`unit_count` 仍攤平可見（保留正對照：拿掉 `_slots_for_prompt` 攤平必紅）＋ `identity`／`identity_source` 為派生值」，⛔ 不得改成子集斷言）、`tests/unit/agent/test_session_confirm_tools_req.py::test_slot_key_enum_is_closed_six_values`（六→十恰好相等）、`test_mcp_facade_unit_req.py`／`test_agent_entry_req.py`／`test_agent_router_unit_req.py`（`_STATS` 新欄位若影響既有 `setattr` 夾具）；其餘既有測試（`test_runtime_req.py`、`test_candidate_*`、integration agent）⛔ 不改、必須仍綠。不改 `build_visibility_predicate`／`canon_visible`（只改入口 mode）；不改 `_slot_blocks` 包裝（F-9 結論：欄位層 `sanitize_slot_value` 剝 `<>{}[]`＋段落層 `sanitize_for_data_block` 已足以擋破框）；不做 CTA／handoff（5.2）；不動舊鏈 `collected_fields`。已知債：新四鍵的個資最小化與保存期限；`fallback_old_chain` 後舊鏈重問。

### 4.3 驗收（容器內）
1. `normalize_entry_mode`：prospect × {缺、非法、`"b2c"`、`"b2b"`} ⇒ 全 `"b2b"`；pm／tenant × 缺 ⇒ `"b2c"`；MCP `parse_identity` 與 REST `build_identity` 各一案（REST 案用 pydantic 預設，⛔ 不明送 null）；**MCP list 形狀案**：payload `"target_user": ["prospect"]`＋缺 mode ⇒ `ident.target_user == "prospect"` 且 `ident.mode == "b2b"` 且 `_STATS.identity_mode_normalized` +1（在「餵 raw payload」的實作下必紅）；正對照 `"target_user": "Prospect"`（非已知值）⇒ `tenant`、`mode == "b2c"`、計數不加；MCP 正規化發生 ⇒ `_STATS.identity_mode_normalized` +1（正對照：prospect 明送 b2b 不加）。
2. 契約測試：模型經 `session.slots.set` 寫 `identity_detail`＝任意值後，下一回合 prompt 的 `identity` 段仍為派生值；`slots.set(key="identity")` ⇒ `INVALID_INPUT`；儲存側預塞 `{"identity": {...}}` ⇒ `_slots_for_prompt` 丟棄且 warning 只有鍵名。
3. 回合後 `state["slots"]` 與 `store.save` 收到的 `collected_data` 皆不含 `identity`／`identity_source`；正對照：同一斷言對 `identity_detail` 必須看得到。
4. `write_slot` 對非 enum key raise（正對照：enum key 寫入成功）。
5. AST 掃描器：每種 sink 形狀正對照必紅、乾淨樣本綠、真 `services/agent/**` 綠。
6. 句型表非空守門、正例匹配、清空必紅、不進 prompt 掃描。
7. 政策文含定義句；`test_slot_key_enum_is_closed_*` 十值恰好相等；`test_next_turn_reads_the_slot_written_by_the_tool` 依 §4.2 改法仍紅於「沒攤平」；**既有全綠清單**：`scripts/run-tests.sh unit tests/unit/agent/ tests/unit/_meta/ -rA`（含 `test_runtime_req`、`test_agent_turn_unit_req`、`test_candidate_*`）與 `integration tests/integration/agent/`；`make audit` PASS。
8. **`derive_identity_source` 逐形狀**：{pm 帶 role_id、tenant 帶 role_id＋user_id、prospect 無兩者（REST vendor_id=0 與 MCP vendor_id>0 各一）、prospect 偽送 role_id（F-6：⇒ `entry`，測試 docstring 註明只影響對話控制）、MCP 未知 target_user 無兩者} 回值逐一斷言；正對照：同一身分拿掉 `role_id`／`user_id` ⇒ 由 `entry` 翻成 `anonymous`（把函式改成恆回其一必紅）。
9. `_STATS` 計數：`F._STATS.identity_mode_normalized` 與 `premise_stats()["identity_mode_normalized"]` 各斷一次；health `premise` 節含該鍵且 `red_flags` 不含它。

### 4.4 待業主核（S2，S3 收案後一併呈）
1. 明送 `mode="b2c"`＋prospect 也正規化為 b2b（F-3；替代：只補缺漏，明送者照送＝分裂身分）。
2. `identity_source` 依契約形狀定義（不看 vendor_id）；`entry` 分支今天無活流量，測試以 pm／tenant 構造。
3. 政策文再取一句定義（F-8 (a)）。
4. 已知債兩條（新四鍵個資最小化／舊鏈重問）留紀錄不做。

## 5. 待業主核（本輪只需裁 S1 相關）
1. 降級路徑改為「可見細目全集＋toc」而非未過濾整份（§2.6 第 1 條）。
2. health 紅條件：三個 agent 開關（`AGENT_AUDIENCES`／`AGENT_SHADOW_AUDIENCES`／`AGENT_TURN_ENABLED`）任一開且 prospect 索引非 `ready`（`absent` 或 `not_ready`）⇒ 紅（§2.1-7）；判定函式搬到 `mcp_facade.agent_configured()`。
3. 政策文一句在 4.1 改（§2.1-9），不等 5.1。
4. 執行順序 S1 → S3 → S2（不平行寫入）。
5. `PREPARE_TOTAL_TIMEOUT_S = 60.0`（Plan v3 曾用 20；3.7 後 358 鍵 45 批，本機估 5–10 s）。
6. **b2c 模式的 prospect 身分**（`mcp_facade` 對缺漏／非法 `mode` 預設 `b2c`，`agent.turn` 只開 prospect）：接線後從整份大綱變零內容。選項 (a)（**建議**）：在入口把 `target_user=="prospect"` 且 `mode` 缺漏／非法者正規化為 `b2b`（與 `routers/agent_entry.py` 現行做法、memory「prospect＝b2b＋無 role_id」一致）——這是身分／信任邊界改動，**歸 S2、先過 security-reviewer**，S1 先以 `candidate_none_visible` 讓它可觀測；選項 (b)：接受零內容為正確行為（b2c 身分本就不該看 b2b 池），只留可觀測性。⛔ 不選「沿用整份」——那是 3.2 債。
