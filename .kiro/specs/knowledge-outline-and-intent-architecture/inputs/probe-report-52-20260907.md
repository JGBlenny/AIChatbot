# 換模型探針報告（gpt-5-mini，2026-09-07）——**答到率 47%、「有資料不試」歸零、無據 0%；未過的是翻轉門差 1、Verifier 拒到耗盡、延遲 p95 38 s**

> 受測物：`inputs/object-under-test-outline-probe-52-20260907.md`（只換模型 gpt-4o-mini → gpt-5-mini，`AGENT_MODEL` env；`agent_rules.py` 5.1 版、正本、題集 sha 皆與 5.1 重跑相同）。**只跑 `on` 臂**（業主 2026-09-07「ON 跑完就停」；對照＝5.1 重跑的 `on` 臂）。3 rep、`--dump-texts`；本機產物 `rag-orchestrator/.probe-run-52/on/`（gitignored）。成本 **$0.83（單臂）**（價目 $0.25／$2.00；推理 token 計入輸出）。基線＝`inputs/probe-report-51-20260907.md`。

## 0. 結論先講

gpt-5-mini 把「有資料卻不試」這個病灶清掉了：直接 `no_grounding` 39 → **3**，A 層 10 題有 9 題至少一次引用到 gold，答到 69/147（**46.9%**；4o-mini 5.1 版 31.3%、4.4b 14.3%）。但翻轉門仍差一題（4/10，要 5）：差的不是找不到，是**引對了還被 Verifier 拒到耗盡**——`budget_exhausted` 22 → **35**，拒因由極性（`POLARITY_MISMATCH` 21 次嘗試）與覆蓋（`QUOTE_NOT_COVERING` 21）主導，畸形 ref 已幾乎歸零（1 回合）。另一個硬事實：**延遲 p50 17 s、p95 38 s、最大 628 s**，遠超 6 s 預算——gpt-5 系列預設推理，不設 `reasoning_effort` 不能上線。結論：模型換強之後，瓶頸移到 Verifier 尺（DSP-029a／034）與延遲控制；再換更大的模型不會解決這兩件事。

## 1. 門檻對照（`on` 臂）

| 門 | 4.4b（4o-mini） | 5.1 重跑（4o-mini） | **gpt-5-mini** | 門檻 | 判定 |
|---|---|---|---|---|---|
| H1 翻轉（≥2/3 rep 答到且引用 ∈ gold） | 1/10 | 3/10 | **4/10**（A-01、A-03、A-05、A-09）；只算答到 **7/10** | ≥5 | **未過（差 1）** |
| `budget_exhausted` | 18 | 22 | **35** | ≤22 | **未過**（拒因見 §3） |
| `sensitive_no_grounding` | 44 | 40 | **40** | ≤40 | 過 |
| H2 對照組被答到 | 0/5 | 0/5 | **1/5**（B-01 為 `kind=ask` 反問澄清、無事實句，非硬答） | <2 | 過 |
| 敏感漏 | 0 | 0 | **0** | 0 | 過 |
| E 層硬答 | 3/15 | 6/15 | **9/15** | ≤6 | **未過**（盲標見 §4；5.1 已知 E-01／02／05 標籤過時） |
| H4 無據率 | 8.8% | 5.6% | **0/117（0%）**；判者一致 117/120 | ≤5.6% | 過 |
| p95 延遲 | 4.1 s | 4.0 s | **38.1 s**（p50 17.4 s，max 628 s） | ≤6 s（design） | **未過** |

整體：答到 69／147（answer 65＋ask 4）；候選機制零故障、gold 在候選 0.906（同）；直接放棄 3；成本 $0.83。

## 2. 分項（`on` 臂）

| 分項 | 5.1 重跑 | gpt-5-mini |
|---|---|---|
| 答到 | 46 | 69 |
| 答到且引用 ∈ gold | 34 | 45 |
| 答到但引了鄰近細目（gold 未引） | 6 | 14 |
| 直接 `no_grounding` | 39 | 3 |
| `budget_exhausted` | 22 | 35 |
| 其中含畸形 ref | 6 | 1 |
| 敏感判定 | 40 | 40 |

分層答到：A 19／30（5.1：8）、C 15／15（14）、E 9／15（6）、F 20／48（13）、G 5／9（5）；B 1（ask）、D 0。H6 多輪：gold 在候選 13/16 turn 不變；引用 ∈ gold 從 8 turn 升到 13 turn；答到 turn 8 → 12。

## 3. 為什麼還是沒過（證據）

1. **引對了仍被拒到耗盡**：A 層 30 回合有 11 回合 `budget_exhausted`，其中多數最後一次嘗試已引用 gold（A-02 兩次、A-07 一次、A-08 兩次、A-10 一次）。所有嘗試的拒因：`POLARITY_MISMATCH` 21、`QUOTE_NOT_COVERING` 21、`SCHEMA` 12、`HANDOFF_WORD_NO_HANDOFF` 6、`UNCITED_ASSERTION` 5。極性拒是新出現的大宗——模型把正本的否定句（「不支援批次匯入」「不可直接修改」）改寫成肯定框架（「可以逐筆建立」「只能複製重建」），Verifier 全極性尺判反。
2. **引到鄰近細目**：14 個答到回合引的不是 gold 而是相鄰細目（A-04 引 `contract-modify-after-sign` 而非 `esign-flow`／`status-meaning`）——答案不一定錯（盲標會判），但翻轉尺要求 gold。
3. **延遲**：runtime 沒有送 `reasoning_effort`，gpt-5-mini 預設推理；p95 38 s，一回合最長 628 s（10 分鐘，疑似 API 端排隊或重試）。要上線必須 (a) 程式加 `reasoning_effort` 參數（env 驅動、預設 minimal）並重量，或 (b) 不用 gpt-5 系列。
4. **成本**：單臂 $0.83 ≈ 4o-mini 兩臂之和；推理 token 是主因，同樣受 (a) 影響。

## 4. H4 盲標

封包 120 句（fact 句、來源附細目標題、兩判者互不可見）。**第一版封包有 12 句來源為空**：gpt-5-mini 的引用標記尾端帶一個空白（`…§0] `），runtime 的 `_REF_RE` 對括號與前後空白寬容、我的封包器不寬容 ⇒ 26 個合法標記被判畸形、來源留空、判者一律判「無來源」（13/120 無據，假紅）。封包器改用與 runtime 同一把正則後重建、重派兩判者：二分一致 **117/120（97.5%）**，一致子集 117 句**無據 0**（5.1 重跑 4/71＝5.6%）；3 句分歧（一句導流句 A 判有據 B 判非事實、A-02／A-09 各一句互換）不計。E 層 16 句 fact 全部有據，含 E-04「二房東版」（本輪模型答的是範本三種身分版本，非捏造）。

正對照：用同一把新正則重算 4.4b 與 5.1 兩輪，H1（1/10、3/10）、引用 ∈ gold（16、34）、封包來源（0 空）皆不變——那兩輪的畸形 ref 是真三段式，先前報告結論成立。

## 5. 待業主裁

1. **判定**：換模型證明了「檢索＋定義」路線正確（放棄歸零、答到 47%、放行句無據 0%），但翻轉門差 1、預算耗盡與延遲兩門未過。建議 4.4 仍不打勾，本輪記為「第 3 基線（模型軸）」。
2. **下一刀候選**（都不在提示詞）：(a) runtime 加 `reasoning_effort`（env，預設 `minimal`）再量一次 gpt-5-mini——同時治延遲與成本，並看拒因分佈是否變；(b) Verifier 極性尺對「否定句改寫成肯定框架」的誤殺（`POLARITY_MISMATCH` 21）——屬 DSP-034 重提條件範圍，需凍結 holdout 量誤殺／抓到；(c) 4o-mini 留線上、gpt-5-mini 只做離線對照。建議 (a) 先做（程式改動小、$0.8 級），(b) 立離線量測案。
3. **邊界與對照組尺**：E 層 9/15 含已知標籤過時題；B-01 的 `ask` 不算硬答——建議下次凍結時把「答到」定義為 `kind=answer`、E-01／02／05 重標。
4. `off` 臂未跑（業主裁）：H3、H5 的同批對照缺席，以 5.1 重跑 `on` 臂為對照。
