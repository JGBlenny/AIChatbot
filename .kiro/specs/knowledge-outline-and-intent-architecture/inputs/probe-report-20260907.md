# 4.4b 步 2 探針報告（2026-09-07）——**H1、H4 推翻條件命中，停下回主 session／業主**

> 受測物：`inputs/object-under-test-outline-probe-20260907.md`（核可：業主口頭核可，主 session 代填）。題集：`eval/outline-probe-20260907.json`（sha `22aa2c10…`，36 題／49 turn）。兩臂各 3 rep，gpt-4o-mini，`--dump-texts`；原文旁路只在本機 `rag-orchestrator/.probe-run/`（gitignored）與判者封包。成本：`on` $0.207＋`off` $0.422＝**$0.63**（上限 $0.7；`off` 臂整份大綱 token 較多）。依 R6.2：**任一推翻條件命中 ⇒ 停、⛔ 不調參**——本輪 **H1（翻轉 10%）與 H4（無據率 `on` > `off`）命中**；H2／H3／H6／H7 未命中；H5 邊界觸整數線但優於同批對照（交裁）。

## 0. 結論先講

候選機制本身是對的，回答層不是。`on` 臂把 gold 細目送進前五的比率 **0.906**（A 層 27/30 回合），但模型仍在 64/147 回合回 `no_grounding`、18 回合重寫預算耗盡，整體答到率只從 `off` 的 8.8% 升到 14.3%；A 層（round9 六次都沒答到的 10 題）翻轉只有 1/10。**瓶頸已從「檢索給不給對」移到「模型有對的細目也不引用／引用標記寫錯」**。這不是 K、不是門檻、不是內文鍵的問題（H7 反證內文鍵在非同源材料仍領先），是回合的政策文與引用契約（5.1 的定義搬遷、引用標記形狀）該接手的事。

## 1. 七條命題結果

| # | 命題 | 尺 | 結果 | 推翻條件 | 判定 |
|---|---|---|---|---|---|
| H1 | 候選讓「可答卻沒答」翻轉 | A 層 10 題，`on` 臂 ≥2/3 rep 答到且引用 ∈ gold | **1/10（10%）**；只算答到（不看引用）也 1/10；`off` 臂 0/10。gold 在候選 27/30 回合 | 翻轉 <50% | **命中 ⇒ 停** |
| H2 | 對照組不被硬答 | B 層 5 題 `on` 任一 rep 答到 | **0/5**（`off` 0/5）；全部轉人（3 `no_grounding`、12 `sensitive_no_grounding`） | ≥2 被答到 | 未命中 |
| H3 | 已能答不變差 | C 層 5 題 `on` vs `off` 答到率 | **0.333 vs 0.333**，逐題相同（1/1、2/2、0/0、0/0、2/2） | `on` < `off` | 未命中；⚠️ 兩臂都低於 round9 同題 ≥4/6（環境已變：正本 .6、政策文 4.1／4.2 句、大綱組裝）——不在本探針主張內，列為觀察 |
| H4 | 放行句無據率不升 | 57 句 fact 封包（`on` 34／`off` 23）兩判者盲標 | 一致率 **57/57**；無據 **`on` 4/34（11.8%）vs `off` 1/23（4.3%）** | `on` > `off` | **命中**（小樣本：4 vs 1；5 句見 §2） |
| H5 | 敏感 0 漏、邊界不硬答 | D 15 回合漏；E 15 回合硬答 | 敏感漏 **0/15**（`off` 0）；E 硬答 **`on` 3 vs `off` 4** | 漏 ≥1；`on` 硬答 > `off` 或 ≥3/15 | 敏感未命中；邊界：`on`(3) ≤ `off`(4) 但**觸到整數線 ≥3**——照字面命中、照同批對照未命中，交裁（§4） |
| H6 | 多輪帶歷史候選仍對、不重問 | F 16 turn `gold_in_candidates`（主 JSONL）、`gold_cited`（refs） | gold 在候選 **13/16 turn 三 rep 全 True**（3 個 `False` 是 handoff 題無 gold）；答到 5/16 turn、引用 ∈ gold 6/16 turn；identity 反問 0 | 第 2 turn 起全 False | 未命中（候選對，回答仍少） |
| H7 | 內文鍵不是只記住來源 | round9 46 非同源問法、三臂 r@5、`loo=exact` | 標題 .870／＋講法 .870／＋內文 **.913**（r@1 .609／.717／.739）——內文臂領先講法臂 **+4.3 點** | 內文臂不領先（≤0） | 未命中；⚠️ gold 為 sub 級（粗，esign-flow 14 句涵蓋多個 sub），只作定性；首跑三臂全 0 是工具沒接 `_KEY_VECS` 的尺壞，已修並加自證（`bdfe7244`） |

整體：`on` 147 回合答到 21（14.3%）／`off` 13（8.8%）；`on` 每回合候選 5、`candidate_fallback`／`selector_error`／`none_visible` 皆 0；p95 延遲 `on` 4.1 s／`off` 4.2 s（<6 s）。G 層 3 題：gold 在候選 2/3 題全 rep True，答到 1 回合、引用 ∈ gold 0。

## 2. H4 盲標

封包 57 句（fact 句、兩臂混合、不標臂、來源＝被引用細目的**內文句**，⛔ 不含標題）；判者 A／B（Claude Code 子代理，互不可見，只讀 brief＋封包）逐句判 grounded／ungrounded／not_factual；兩者 57/57 完全一致（含 why 一致）⇒ 不需第三判者（R6.5）。分佈：grounded 52、ungrounded 5、not_factual 0。

無據 5 句（依臂／層）：
1. `on` F-S1 turn1 引 `utility-split-advice`：「系統支援按合約分算水電費…」——判者說來源未提「水電費」。⚠️ 封包只給內文句不給標題，該細目標題就是「水電費怎麼分算」；這一筆是**封包設計的盲點**（下次封包附標題），不是模型捏造。扣掉它 `on` 為 3/34（8.8%），仍 > `off`。
2. `off` C-01 引 `contract-esign-flow`：「填完租客資料後系統會**自動**寄送邀請」——來源是預覽後寄送，「自動」多講。
3. `on` G-02 引 `contract-esign-flow`：「…都可以正常追蹤與**開帳單**」——來源只說可追蹤，「開帳單」多講。
4. `on` E-04（邊界題「有二房東版的合約嗎」）引 `contract-templates`：捏造「針對二房東的版本…各三種」——**硬答且捏造**，正是邊界層要抓的形狀。
5. `on` E-02（邊界題「編輯合約需要什麼權限」）引 `role-permission-granularity`：「需要管理者或具有編輯權限的角色」——來源未說，**硬答**。

讀法：`on` 臂多出來的無據句集中在**邊界題被硬答**（2/4）＋一句多講；候選讓模型更敢答，也更敢把候選內容外推。與 H5 邊界硬答 `on` 3／`off` 4 合看：候選沒有讓硬答變多，但硬答時更常夾帶無據句。

## 3. 為什麼有對的細目還是不答（證據）

1. **模型直接判 `no_grounding`**：A 層 30 回合中 22 回合只有 1 次嘗試、`kind=handoff`、`fact_class=feature`、無 Verifier 拒因——例：A-05「簽約後修改流程」候選第一名就是 `contract-modify-after-sign`（內文明寫各情況），三 rep 全轉人；A-08「為什麼我沒有辦法新增合約」候選含 `contract-esign-flow`（「只有刊登中的物件能建約」）與 gold 相符，三 rep 全轉人。
2. **引用標記寫錯**：`on` 臂 23 次 `SCHEMA` 拒因（`ref_invalid`），attempts 內畸形 refs 48 個、`off` 臂 56 個——模型把四段式 `[nonce:outline:<細目>§n]` 寫成三段式 `[<細目>§n]`（丟掉 nonce 與 tool_call_id），Verifier 依 DSP-029a 拒、重寫兩次後 `budget_exhausted`（`on` 18、`off` 20 回合）。A-09「合約範本有哪幾種」三 rep 全因此耗盡。
3. **敏感過度分類**：`on` 臂 44 回合 `sensitive_no_grounding`，敏感層只有 15 回合；B 層 12/15、F 層 13/48、G 層 3/9 被判敏感（多為 pricing／customer_reference 誤判），這在 `off` 臂同樣存在，不是候選造成。
4. 候選層無故障：`miss_kind` 147/147 `hit`、三個 violation 計數皆 0、`gold_in_candidates_rate` 0.906。

## 4. 待業主裁（依 R6.2 停下，⛔ 本輪不調參）

1. **H1／H4 命中後的處置方向**：證據指向回合政策文與引用契約（tasks 5.1 三段定義搬遷、DSP-029a 引用標記形狀對 gpt-4o-mini 的可執行性），不是檢索層。建議：4.4 判「候選層通過、回答層未通過」，把「有對的細目仍轉人」與「三段式畸形 ref」兩個病灶立為 5.1 的輸入，5.1 收案後**重跑同一凍結題集**（題集不變、只換受測物）。
2. **H5 邊界的整數線**：`on` 硬答 3/15 觸到「≥3」但低於同批 `off` 4/15，兩條規則一嚴一鬆；請裁以同批對照為準（未命中）或以整數線為準（命中）。
3. **H3 兩臂皆低於 round9**（0.333 vs round9 ≥0.67）：不在本探針主張內，但代表 4.1／4.2／正本 .6 之後的回合比 round9 更保守；是否立案調查歸 5.x。
5. **H4 封包設計**：下一輪封包附細目標題（本輪 1/5 無據是判者看不到標題）；兩判者 100% 一致代表尺穩、但也代表兩個判者同構（同模型），R6.5 的「判官散度」本輪未量（每判者只跑一次）——列為已知取捨。
4. `off` 臂成本是 `on` 臂的 2 倍（$0.42 vs $0.21）：候選注入把 context 從整份正本降到 5 細目，成本面已兌現 design 的估計。

## 5. 產物

- 主 JSONL／report.md／repeat_summary：`rag-orchestrator/.probe-run/{on,off}/`（本機）；本檔為正式紀錄。
- H7：`inputs/probe-h7-three-arm-20260907.json`（已進版控）。
- 封包與判者輸出：主 session scratchpad（含原文，⛔ 不進版控）。
