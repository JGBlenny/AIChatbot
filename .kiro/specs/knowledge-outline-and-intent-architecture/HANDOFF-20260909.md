# HANDOFF 2026-09-09：LINE OA「JGB 房東管家」demo 版**已上線**（agentic-mcp 線，大改版收案）

> 讀這一份就知道現在線上跑的是什麼、怎麼起、給誰什麼、還缺什麼。⛔ 只索引與摘要，事實以各正本／程式為準。取代 `HANDOFF-20260908.md`（其 §1／§2 仍對，但範圍已擴）。架構總覽：`docs/architecture/AGENTIC_MCP_ARCHITECTURE.md`。

## 1. 現況一句話
正式站 `jgb2-ai-chatbot` 於 2026-09-08 換成 demo 版：main＝feat/agentic-mcp（部署碼 `5eb9cbb6`，其後皆文件），DB 由 dev dump 取代，整站替身，`/mcp` 對 line-bot 開放（key `line-bot-oa-demo`）。線上 health ok、audit PASS、三段真對話走通（帳本 §1h「線上部署」）。

## 2. 這次改版包含（2026-09-08 一天內落地、皆經 fresh verifier CONFIRMED）
| 塊 | 內容 | commit（程式） |
|---|---|---|
| 正本 delta2–4 | 文字口述直接出卡、未表明急迫＝非緊急不反問、聊天版收尾；A 節去「拍照」；工具描述改定義；分類／急迫缺值由程式補（歸「其他」／1） | `5b03bdeb`、`173487e7`、`17621359`、`b86e7fde`（閘） |
| W8 (1) 清單點選 | `select:<type>:<id>` 程式攔截直答（bill／contract／repair）、最小揭露 face、槽位、作廢無 receipt 的待確認 | `b86e7fde` |
| W8 (3) 未結單提示 | `jgb2.query.repairs` `estate_id`、`open_repairs` 注入、卡外 hint | `b86e7fde` |
| W8 (5) 會話過期 | `last_turn_at`＞1800 s 先關舊列再開、`session_expired` 第六鍵 | `b86e7fde` |
| 未知 pid | 機器值 pid 不在 session ⇒ 固定句不進模型（過期後舊鍵曾外洩 pid） | `3bf28e79`、`3100c781` |
| L15 別戶邊界 | select 後以 `estate_id` 等值程式判定：候選過濾、單列替換、接句、寫入閘；contracts／bills description 定義句 | `95b511c0` |
| 規則兩句 | pm／tenant 政策文【判準】：一句多題先答完再指路、自述數值一律現查 | `84f7b0ce` |
| W8 (2) 照片線 | 服務端自抓 relay 簽章網址（硬邊界）、縮圖去 EXIF、≤10 張每 5 張一批、辨識決定性驗證、`run_turn(image=ImageTurnInput)`、健檢鍵、gpt-5 參數 | `56331d51`、`123fd6cc` |
| 正本落檔 | DSP-042（`session_expired`、`select:` 契約、落檔清單）；R3.7／design 元件 3、4；契約照片列 | `ace24b46`、`19df2c14` |

## 3. 業主裁示（全在帳本 §1b；決策正本 `.claude/DECISIONS.md`）
R1–R9 同前｜R10／R10-c LIFF 只做入口、點選以機器值｜**R11 DSP-042 三件都採**｜**R12 照片辨識 demo 用 gpt-4o**（真線路 6/6 vs gpt-5-mini 5/6）｜L15-04 session 級釘住、L15-08 邊界＝物件｜delta4 採、5.1 兩句採｜照片上限「可以超過三張」「超過五張分批送」⇒ 10 張、內部 5 張一批。

## 4. 數字（帳本 §1h）
- line-bot 21 個可跑案例：線③出卡 2/12→**12/12**、建單 1→8、內部識別名外洩 9→**0**、索取照片 8→**0**、perf 10/10、p50 11.2→8.8 s；同版本三輪穩定度線③ 12 案 3/3，線⑤ 7 案 3/3、2 案 2/3（模型自判 no_grounding）。
- select 進場 8/8 程式直答；過期真 DB 探針達；L15 六情境機制 2/2；W6 四門檻過（轉人 19%、反問 0%、收尾 4/4、p50 7.3 s）。
- 照片真線路六張：gpt-4o 6/6（約 2 s／張）、gpt-5-mini 5/6。

## 5. 文件地圖（給誰）
| 檔 | 給誰 | 內容 |
|---|---|---|
| `docs/architecture/AGENTIC_MCP_ARCHITECTURE.md` | 工程／稽核 | 元件、回合路徑、寫入鏈、邊界、資料落點、組態、測試層、已知限制 |
| `inputs/line-bot-integration-sheet-20260908.md` | **line-bot** | 端點 `/rag-api/mcp`、header、`agent.turn` 輸入輸出、機器值、select、照片、限制、自測 |
| `inputs/mcp-client-contract-line-bot-20260907.md` | line-bot | 契約細節（含照片列、select 列、語音列） |
| `inputs/demo-ledger-line-oa-20260907.md` | 我們 | 帳本：§1b 裁示 R1–R12、§1h 各輪回測與部署、§2／§3 待辦 L1–L15 |
| `inputs/plan-agent-write-tools-demo-20260908.md` | 我們 | Plan：W0–W8、§5–§5h 安全處置、§13–§16e 審查處置 |
| `docs/deployment-runbook.md` §19／§20 | 業主 | key 發行、demo 部署逐條（§20-0 換庫、實跑修正） |
| `docs/api/mcp-facade.md` | 串接方 | `/mcp` 閘與 header（本次改版對碼修正見該檔頁首） |
| `.kiro/specs/agentic-mcp-orchestration/{requirements,design}.md` | 我們 | R3.7 六鍵、元件 3／4 |
| `rag-orchestrator/canon/property_manager.md`（2026-09-08.4） | 系統 | pm 正本 |

## 6. 未完與後續
| 項 | 誰 | 狀態 |
|---|---|---|
| LIFF 五個照片案例經 `/mcp` 端到端 | 我們＋line-bot | 等 relay 簽章網址（或本機 https 替身＋`IMAGE_URL_ALLOWLIST` 覆寫） |
| W8 (4) 催繳草稿 `dunning.draft` | 業主給模板 | 語氣分級與模板文字未給 |
| 線⑤「有工具結果仍自判無依據」 | 我們（5.1 定義文） | 5.1 主驗收三門未過、停下交裁 |
| W6-b3 Verifier 正式參數（取代過渡旗，機敏類恢復照擋） | 我們 | 排程 |
| W7 語音 | 我們 | 審查暫停；抓檔硬邊界可複用 `image_fetch.py` |
| `scope_exit` 機器可讀鍵、A1 confirm 卡回合不接指路句、A2 `$` 非 fullmatch、A3 streak | 我們 | P4 延後 |
| JGB：`agent/v1` 存取、`PATCH bills`、`update` 權限、冪等、C1′ 標籤 | JGB／業主 | 需求文 §B′／§E |
| 既有壞測試 `tests/unit/backtest/test_verdict_ruler_req.py`、integration `test_confirm_token_is_bound_to_the_namespaced_session`（「水電」不在樹） | 我們 | 與本線無關 |

## 7. 查證
```bash
git log --oneline 43e4781f..5eb9cbb6 -- rag-orchestrator      # 本次改版程式鏈
rg -n "session_expired|image_urls|minLength" rag-orchestrator/services/agent/mcp_facade.py
rg -n "SCOPE_EXIT_TEXT|_run_select_segment|ImageTurnInput" rag-orchestrator/services/agent/runtime.py
./scripts/run-tests.sh unit tests/unit/agent -q                  # 1419 passed
scripts/audit/check_invariants.sh | tail -3                      # OVERALL: PASS
```
