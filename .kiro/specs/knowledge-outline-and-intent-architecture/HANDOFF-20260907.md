# 交接（2026-09-07 收尾，換機開發用）——knowledge-outline-and-intent-architecture／agentic-mcp 線

> 讀者：下一台機器上的主 session。⛔ 本檔是**快照**，寫於 commit `df8ae241`（分支 `feat/agentic-mcp`）；與程式或 DECISIONS 衝突時以 git 為準。所有「查證」欄都是可重跑指令。

## 1. 現在停在哪

| 項 | 狀態 | 查證 |
|---|---|---|
| 主線 | tasks 1–3 全收、4.1–4.3 收、**4.4 未打勾**、5.1 程式已落（`7059bdfa`）驗收綁 4.4、5.2 起未動 | `grep -nE "^- \[( \|x)\] [0-9]+\.[0-9]+" tasks.md` |
| 開發基線（DSP-035） | gpt-5-mini＋`AGENT_REASONING_EFFORT=low`＋`AGENT_BUDGET_REWRITES=0`＋`OPENAI_TIMEOUT_S=60`；bootstrap 已放行 0（`fb3ffa14`） | `grep -n "DSP-035" .claude/DECISIONS.md`；`docker compose -f docker-compose.dev.yml run --rm -e AGENT_BUDGET_REWRITES=0 rag-orchestrator python3 -c "from services.agent.bootstrap import budget_from_env; print(budget_from_env().max_rewrites)"` 應印 `0` |
| 對話規則版本 | `services/agent/agent_rules.py` sha256 前 16 `5e392c00c57253fd`（5.1 定義版） | `shasum -a 256 rag-orchestrator/services/agent/agent_rules.py` |
| 工作樹 | 本線無未 commit 變更；`docs/INDEX.md`、`.kiro/specs/testing-traceability/`、`docs/presales-assistant-quality-audit-20260902.md`、`scripts/review_coverage.py`、`.claude/DECISIONS.md.lock` 屬**別的視窗**，⛔ 不動 | `git status --short` |

## 2. 待業主裁（開工第一件事）

**4.4 出路二選一**（依據 `inputs/cut1b-semantic-ruler-measure-20260907.md` §3）：
- (a) 接受現行 Verifier 尺，4.4 **縮窄主張**收案：候選層通過（gold 在前五 0.906、零故障、翻轉曾達 6/10）、回答層答到率 43%（`low` 小測）、無據／敏感門維持、`budget_exhausted` 改列觀察項（尺誤殺 35.3% 為固定成本）。動作＝改 tasks 4.4 主張與驗收並打勾、DECISIONS 新列 DSP-036、design 同步、快照更新；不動程式。之後起 5.2。
- (b) 改輸出契約（DSP-034 (c)）：另立 Plan，先在同批 338 句離線量誤殺變化。

主 session 建議 (a)。

## 3. 今天定案與收束的事（證據索引）

| 事 | 結論 | 文件 | commit |
|---|---|---|---|
| 探針 53（gpt-5-mini minimal） | 翻轉 6/10 首次過，但 SCHEMA 碎片 37、敏感漏 1、無據 10.4% ⇒ 不上線 | `inputs/probe-report-53-20260907.md` | `6d7b2083` |
| 探針 54 小測（low，1 rep，$0.20） | SCHEMA 1/66、敏感 0 漏、無據 4/25、p50 7.1 s；⛔ 無正式報告（業主裁只報數字），數字記在 DSP-035 | `.claude/DECISIONS.md` DSP-035 | `3ff0d1e0` |
| 模型組態定案 | 開發基線（非上線核可） | DSP-035 | `3ff0d1e0` |
| 選項式追問 Plan | 三輪審後 **業主否決**（「給選項的追問我不要」）⛔ 不再提 | `inputs/plan-clarify-on-reject-20260907.md`（標頭已標否決） | `fb3ffa14` |
| 第一刀：尺誤殺量測 | 被拒 137 句誤殺 83.7%（DSP-034 定義 35.3%）；極性＝詞表級、覆蓋＝字面重疊級；抓到 63–66% | `inputs/plan-cut1-…`＋`inputs/cut1-verifier-ruler-measure-20260907.md` | `3759a4ec` |
| 1b：語義尺量測 | NLI 最佳 26%/59%、cross-encoder 8%/25%、皆拒才拒 12%/41% ⇒ 皆不達 DSP-034 ⇒ **不啟用 NLI**，改尺收束 | `inputs/cut1b-semantic-ruler-measure-20260907.md` | `df8ae241` |
| 第二刀：正本缺口盤點 | 凍結題集無真缺口（未答成因＝尺誤殺 6、費用類判報價 2、檢索漏 1）⇒ 收束；新缺口要換材料 | 同上 §（tasks 4.4 註記） | `df8ae241` |
| 工具韌性 | agent_eval 單回合 provider 例外記列；`OPENAI_TIMEOUT_S`；`AGENT_REASONING_EFFORT` | — | `3842fe3b`、`55fd6fa7`、`450c44fd` |

## 4. 岔題待裁（⛔ 不排主線，業主「先完成主線再優化」）

1. 費用類／方案結構問題被模型判 `pricing` 轉人（實跑證據：「我想了解方案的規則」「方案有哪幾種」皆 `fact_class=pricing`；探針 F-S1「簽約要另外收費嗎」正本明寫不另收費仍轉人）——是判準定義邊界＋正本方案章節內容，需業主裁「方案結構算不算報價」。
2. 敏感轉人前先收集基本資訊——前提：確認使用者按「找真人」後業務端看不看得到會話（現行 handoff 只帶固定句與 `channel`，不帶槽位）。
3. 模型生成追問——先定「追問合理」的尺再實驗。
4. 已否決：選項式追問。

## 5. 本地素材（不在 git）

| 素材 | 位置 | 大小 | 說明 |
|---|---|---|---|
| 封存包 | `~/jgb-archive/agentic-mcp-holdout-20260907/`（`MANIFEST.md`） | 15 MB | `probe-runs/.probe-run*`（隱藏目錄，`ls -a`）＝五輪探針原文旁路；`scratchpad/`＝盲標封包、兩判者輸出、1b `pairs.json`／`scores.json`、分析腳本、判者 brief。**含模型原文與正本句，⛔ 不進 git** |
| 探針產物原位 | `rag-orchestrator/.probe-run{,-51,-52,-53,-54}/` | 13 MB | gitignored；`.probe-run-54/show_prompt.py`、`one_turn.py` 為一次性工具（封存包 `scratchpad/` 已有副本，見 §7） |
| embedding 快取 | `.claude/skills/outline-curation/raw/` | 26 MB | 可重算（打 embedding API） |
| NLI 映像 | `aichatbot-nli-model:latest` | 1.16 GB | 離線工具（DSP-034）；可 `docker rmi`、由 `nli_model/Dockerfile` 重建 |
| Claude 記憶 | `~/.claude/projects/-Users-lenny-jgb-AIChatbot/memory/`（含 `snapshots/agentic-mcp.md`） | 小 | **必搬**；新機專案路徑需同為 `/Users/<user>/jgb/AIChatbot` 才對上目錄名 |

## 6. 新機驗證三步（環境等價才開工）

```bash
scripts/run-tests.sh unit tests/unit/agent/ -q          # 預期全綠（今日最後一次：bootstrap＋runtime 41 案綠、eval 67 案綠）
make audit                                              # 預期 PASS（invariant 33/34 含）
docker compose -f docker-compose.dev.yml run --rm rag-orchestrator python3 /app/.probe-run-54/show_prompt.py "簽約後修改流程"
# 預期：候選 5 個、system 訊息約 9,000 字、tools 4 個（需 DB 與 embedding-api 起著、OPENAI_API_KEY 在 .env）
```

## 7. 一次性工具（gitignored；封存包 `scratchpad/` 有副本，若沒有就重寫，各 ≤60 行）

- `rag-orchestrator/.probe-run-54/show_prompt.py`：對單一問句跑候選選取＋prompt 組裝（不打 LLM），印 messages。
- `rag-orchestrator/.probe-run-54/one_turn.py`：對單一問句實跑一回合（打 LLM），印 kind／handoff／候選／attempts。
- 封存包 `scratchpad/*.py`：探針分析、第一刀封包、1b pairs 與計分。

## 8. 探針流程速查

```bash
docker compose -f docker-compose.dev.yml run -d --name probeNN \
  -e AGENT_MODEL=gpt-5-mini -e AGENT_REASONING_EFFORT=low -e AGENT_BUDGET_REWRITES=0 -e OPENAI_TIMEOUT_S=60 \
  rag-orchestrator sh -c 'python3 tools/agent_eval.py --set outline-probe --chain agent --provider openai --candidates on --repeat 1 --dump-texts --out /app/.probe-run-NN/on; echo on_rc=$?; echo ALLDONE'
# 1 rep ≈ 49 回合／8 分鐘／$0.2；3 rep ≈ 25 分鐘／$0.6。Bash 背景有 10 分鐘上限 ⇒ 一律 run -d ＋ Monitor。
# 分析：PROBE_BASELINE=52 python3 probe_analyze.py <repo> <repo>/rag-orchestrator/.probe-run-NN <out>（run_dir 需有 on/）；盲標 brief：judge-brief-h4.md，兩位判者互不可見。
```
