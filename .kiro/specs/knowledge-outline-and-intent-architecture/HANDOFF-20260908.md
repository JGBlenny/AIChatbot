> ⚠️ **已被 `HANDOFF-20260909.md` 取代**（2026-09-08 當日稍晚 demo 版即上線，範圍擴至 W8 (1)(2)(3)(5)、L15、`outcome`）。本檔保留為當時快照：§1 定義、§2 裁示 R1–R9、§3 起法仍可讀，⛔ 但「未 push」「demo key 未發」「部署未執行」等狀態欄已過期。

# HANDOFF 2026-09-08：LINE OA「JGB 房東管家」demo 版（agentic-mcp 線）

> 讀這一份就知道 demo 版是什麼、怎麼起、給誰什麼、還缺什麼。⛔ 本檔只索引與摘要，事實以各正本／程式為準；每段附查證方式。前一份 `HANDOFF-20260907.md` 的 §1／§2（4.4 收案、demo 優先序）仍有效。

## 1. demo 版定義

| 項 | 值 |
|---|---|
| 分支 | `feat/agentic-mcp`（⛔ 未 push；push 由業主） |
| HEAD | 以 `git log -1 feat/agentic-mcp` 為準（程式最後一筆＝`d94bd6ac`；compose 透傳 `0d286c83`；其後皆文件） |
| 範圍 | LINE OA 文字對話經 `/mcp` `agent.turn`（受眾 `property_manager`）＋查詢（帳單／合約／物件／電錶／修繕）＋寫入（延到期日、開修繕單；確認卡→按鈕→Runtime 兌現）；後端整場替身（role 20151 真資料子集、已遮罩） |
| 不含 | 語音（W7，審查暫停）、LIFF 線③④⑤ REST 面向、真 JGB 寫入（等 JGB 開 `agent/v1`＋`PATCH bills`）、Verifier 正式參數（W6-b3，demo 用實驗旗） |
| 部署 | 正式站直接換 demo 版（業主：無線上使用者）、DB 由 dev dump 取代；逐條指令 `docs/deployment-runbook.md` §20 |

## 2. 業主裁示（全在 `inputs/demo-ledger-line-oa-20260907.md` §1b；決策正本 `.claude/DECISIONS.md`）

R1 demo 先於 JGB 開 API、整場替身｜R2 替身讀寫一份 JSON、完整可運作｜R3 寫入工具 demo 必做｜R5 完成度要應付真實用戶（W6）｜R6 JGB 端新規格業主親自溝通、預設會有｜R7 尺分兩類（DSP-039，備援）｜**R8 r3 為正式組態：Verifier 引用類只記錄、機敏類照擋（DSP-040）——⚠️ demo 用的過渡旗 `AGENT_VERIFIER_OBSERVE_ONLY` 連機敏類也不擋（敏感題由模型自行轉人，實跑 0 洩）；W6-b3 落地才分類**｜R9 語音 W7 用 `gpt-4o-mini-transcribe`（DSP-041）。DSP-037 agent.turn 開 pm｜DSP-038 寫入工具四件。

## 3. 起法（帳本 §0；正式站見 runbook §20-2）

`UVICORN_WORKERS=1 AGENT_STAGE=M1 AGENT_TURN_ENABLED=true USE_MOCK_JGB_API=true USE_SEMANTIC_RERANK=false AGENT_MODEL=gpt-5-mini AGENT_REASONING_EFFORT=low RAG_API_AUTH_ENFORCE=true AGENT_BUDGET_DEADLINE_S=45 AGENT_TURN_TIMEOUT_S=60 AGENT_VERIFIER_OBSERVE_ONLY=1 AGENT_WRITE_TOOLS_ENABLED=true`（與帳本 §0、runbook §20-2 逐字同源）；⛔ 不帶 `AGENT_BUDGET_REWRITES=0`、不帶 `AGENT_ATTEMPT_LOG_PATH`。compose 已透傳（`0d286c83`）。demo 不需要 `semantic-model`；正式站舊鏈仍需要。

## 4. 文件地圖

| 檔 | 給誰 | 內容 |
|---|---|---|
| `inputs/demo-ledger-line-oa-20260907.md` | 我們 | 帳本：§0 起法、§1 劇本、§1b 裁示、§1c W6 量測（r1 52%／r2 48%／r3 7%）、§1d R-寫、§2 D-BLOCK 1–3／W1–W5／H1、§3 後面要改 L1–L11、§4 line-bot、§5 JGB |
| `inputs/plan-agent-write-tools-demo-20260908.md` | 我們 | Plan v3：W0–W7、§5 安全處置（寫入 13 條、W6-b1 10 條、W7 15 條）、§6 回退／預算／停止、§7–§12 審查處置 |
| `inputs/line-bot-worklist-demo-20260908.md` | **line-bot** | 一頁交辦 B1–B6＋六條自測劇本 |
| `inputs/mcp-client-contract-line-bot-20260907.md`（v1） | **line-bot** | 精確形狀：header、回應鍵、`quick_replies` 值 `confirm_*:<pending_id>`、錯誤碼、逾時 60 s、語音列（W7） |
| `inputs/demo-data-sheet-line-oa-20260908.md` | **line-bot** | 可演的真資料：5 物件／4 合約／7 帳單／1 電錶／2 修繕單＋六條建議句 |
| `inputs/jgb-api-needs-line-oa-demo-20260907.md` | **JGB** | §A 查詢免開、§B／B′ 建立與 `PATCH bills`、§E jgb2 逐條盤查（`date_expire`、無冪等、無 `update`、C1 真值 2＝緊急／External 標籤反） |
| `inputs/line-bot-scenarios-delta-20260908.md` | 我們／line-bot | 線③ M–Q、線⑤ I–K 觸點與處置 |
| `inputs/review-sheet-property_manager-delta-20260908.md` | 業主（已 ✅） | pm 正本四句事實修正 |
| `docs/deployment-runbook.md` §19／§20 | **業主部署** | env、key 發行、demo 版逐條指令 |
| `.claude/DECISIONS.md` DSP-037～041 | 我們 | 決策正本 |
| `rag-orchestrator/canon/property_manager.md`（2026-09-08.1） | 系統 | pm 正本 36 細目、四句已修 |

## 5. 數字（帳本 §1c／§1d；最終回測跑完補）

W6 口語 36 回合：r1 52%→r2 48%（有閘）→r3 7%（觀察，正式組態）；r3 21 筆「若擋會擋」全為正確輸出、0 捏造、0 個資洩。R-寫 首輪：延 3 天整條通（確認卡→0.0 s 兌現→8/18）。**真資料替身最終回測（帳本 §1e）：R-讀 20 回合 1 轉人（應轉）0 錯；W6 可答題轉人 15%／反問 6%／收尾 4/4／p50 8.7 s（四門檻全達）；R-寫 延 3 天／取消／重送／失敗注入 0.0 s 兌現路徑全對、開單卡第 2 回合出。**

## 6. 未完與後續

| 項 | 誰 | 狀態 |
|---|---|---|
| line-bot MCP client（B1–B6） | line-bot | 契約＋清單＋資料表已交 |
| demo key（§19-2） | 業主 | 未發（本機 key 98 已停用） |
| 部署（§20） | 業主 | 指令已備；push 後執行 |
| W6-b3 Verifier 正式參數（取代實驗旗） | 我們 | 排主功能後 |
| W7 語音 | 我們 | Plan 完稿、審查暫停（三輪） |
| JGB：`agent/v1` 存取、`PATCH bills`、`update` 權限、冪等、C1′ 標籤 | JGB／業主 | 需求文 §B′／§E |
| L-list（L2 LIFF 線、L3 快取、L4 逾時降級、L5 reranker 健檢…） | 我們 | 帳本 §3 |

## 7. 查證
```bash
git log --oneline 4687701f..HEAD          # 本輪 commit 鏈
rg -n "AGENT_WRITE_TOOLS_ENABLED|AGENT_VERIFIER_OBSERVE_ONLY|UVICORN_WORKERS" docker-compose.prod.yml
rg -n "confirm_submit|CONFIRM_QUICK_REPLY" rag-orchestrator/services/agent/tools/confirm.py
python3 -c "import json;d=json.load(open('rag-orchestrator/services/jgb/fixture_data/demo_vendor4.json'));print(sorted(b['id'] for b in d['bills'] if 12291 in d['bill_visibility'].get(str(b['id']),[])))"
```
