# 交接（2026-08-29 收工）

分支 `fix/retrieval-routing-stability`｜⛔ **從未 push**｜工作區乾淨

## ⏳ 唯一進行中的事

```text
A01 authorization 步驟 6：frozen implementation 首次跑那 456 句（**BURN 點已越過**）
  執行位置   容器內 `docker exec aichatbot-rag-orchestrator python3 /tmp/execute.py`
  結果檔     容器內 `/tmp/a01_result.json`（尚未產出）
  預估       456 次真實檢索 × 約 1.5–2 秒 ≈ 12–15 分鐘
```

⚠️ **重開視窗後第一件事**：確認結果檔並**先驗完整性**

```text
docker exec aichatbot-rag-orchestrator sh -c 'ls -la /tmp/a01_result.json'
docker exec aichatbot-rag-orchestrator python3 -c "import json;d=json.load(open('/tmp/a01_result.json'));print(len(d))"
```

⚠️ **已知風險**：`/proc` 掃描時看到**兩個** `execute.py` 進程（171／178）——
178 應是先前逾時的前景執行未隨 timeout 中止，兩者會寫**同一個**結果檔。
⛔ 不得直接拿來判 coverage；必須先驗 `len == 456`、序號 1..456 齊全、無重複。
異常 → 以一次乾淨重跑為準（corpus 已 BURNED，重跑不影響 holdout 效力，
只是重新量測同一份已凍結語料）。

## 下一步（frozen protocol 第 7–8 步，⛔ 順序不可調換）

```text
7. coverage precondition（判準已凍結，⛔ 不得調整）
     C1 judgeable 且 top1 ∈ Level-A 10 rows  >= 30
     C2 truth = instance  >= 10
     C3 truth = general   >= 10
   任一不足 → **INCONCLUSIVE**；⛔ 不得計 PASS/FAIL、不得追加 corpus、
   不得改 sampling、不得放寬 top1→topK、不得回頭補題
8. coverage 全過**才**計 authorization PASS／FAIL
   ⚠️ 一致率門檻**尚未補登**——需業主在 coverage 判定通過後、**看結果之前**裁定
```

⚠️ 最可能卡住的一格：judgeable truth 的 **instance 只有 38 句**（general 306），
C2 要求其中至少 10 句的 top1 落在 Level-A 那 10 筆內。

## 已凍結物（重開視窗後可直接引用）

```text
implementation SHA        f89861250d9cd01f4585fc9624a629b75bbcc7d5
Level-A population digest 57413ee8a4068f73bc4e0c2c0512967e（10 rows：instance 7／general 3／UNKNOWN 0）
face requirement digest   bde00b206c9c8b7d4b1328e7440d754f（required 16／not_required 6／unknown 1）
A01 corpus digest         628e3ca51679ba43（n=456，全量 census）
A01 labels digest         41743b7219f0f224（L1=95.4%、κ=0.902，judgeable 344）
Level-A 10 row ids        3402 3406 3495 3496 3498 3499 3519 4640 4656 4657
gate                      **OFF**（INSTANCE_REFERENCE_GATE 未設）
```

## 方向確認：主線**沒有偏離**

今天所有工作都在同一條因果鏈上，且每個岔口都由業主裁定：

```text
裁定 001 authority governance
  → 不變量 9（發現 Face preemption 吃掉 execution capability）
  → 21 筆 diagnostic census（發現漏的是**能力**不是 mapping）
  → 三處能力回補 ＋ subscription 面向（真 API 唯讀 smoke 收案）
  → 11.5 五槽 wiring（順手抓到候選填錯 estate_id）
  → U1（結論：category 在 identity 上夠用，缺的是**正交的 applicability truth**）
  → P1a–P1f（把 applicability 落成三態資料契約，並完成 authority 換手）
  → A01 authorization（第一次真正驗新的 truth-contract gate）
```

貫穿的同一個病灶形狀：

```text
nomination ≠ authority｜select=api ≠ capability equivalence｜有資料流 ≠ 流到正確語義槽位
⇒ **形式上有接線，不等於語義上接對。**
```

終點未變、且仍受閘：`gate authorization → 3.4 → release`，
而 release 仍是 **BLOCKED_BY_VALIDATION_COMPLETION**，⛔ 沒有任何東西被偷偷放行。

⚠️ 唯一誠實要說的是**深度**擴張了（從「修 routing 穩定性」到「建立 applicability truth contract」），
但那是 U1 的結論逼出來的，且每一步都有業主裁定，⛔ 不是自行擴散。

## 仍 PAUSED／已登記未動（⛔ 不要順手做）

```text
⏸ 3.4｜⏸ instance gate 實際啟用｜⏸ 擴 PREENTRY_ROUTABILITY_FACETS
📋 76 筆 proposal 的 B 桶 18／D 桶 55（本輪只處理 A 桶 3 筆）
📋 742 筆 general review queue（需正面 reviewed declaration，⛔ 不得盲標）
📋 MIXED categories 粒度問題（U1 的延伸，未立案）
📋 debt：ONLINE_FAILURE_STATE_COVERAGE／SUBSCRIPTION_PRESENTATION_FIDELITY／
        INV9_WARN_3366／INV9_WARN_3548／INV4_WARN_修繕報修
```

## 環境狀態

```text
容器已重建並與本地一致（不變量 3 綠）｜稽核**十條全綠**｜unit 1586 passed / 0 failed
本機資料庫已寫入：34 deterministic ＋ 6 reviewed declarations ＋ 23 Face requirement
migration 皆冪等（重跑 0）
```
