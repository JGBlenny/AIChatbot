# 交接：conversational-routing-execution（2026-08-29）

分支 `fix/retrieval-routing-stability`，⛔ **從未 push**。工作區乾淨。
⛔ **production 完全未動**——以下所有 DB 寫入與容器操作皆在**本機驗證環境**。

---

## 一、當前狀態（一眼版）

```text
稽核            20 條 OVERALL PASS（CODE_CONTRACT_REGRESSIONS 0／PRODUCT BLOCKERS 0）
unit            1636 passed / 0 failed
LEVEL_A         V1 immutable（10 rows）／**V2 active（9 rows）**
A04             INCONCLUSIVE / CORPUS_INSUFFICIENT（harness NOT_EXERCISED）
A05             解鎖條件已達成：P5 census 顯示 **LEVEL_A_V2 9 列 → 9 個相異 responsibility**
                （無共用、無多重 membership）⇒ denominator 可維持 9，理由升級為機器可追溯普查
                ⚠️ 是否開案仍待業主裁示
R10             target architecture 已裁：**C（responsibility_id）＋ C2 collapse**
R10-P           **P4 sealed ＋ P5 census 完成**：active 30／historical 1；
                row disposition 54＝A36／B1／C1／**D16 未解**
```

---

## 二、這一輪完成了什麼（依序）

### D1/D2/D3 —— retrieval representation 契約落地
```text
契約模組 services/retrieval_representation.py；scoring_surface() 為 D2 的**唯一實作點**
producer：retriever 兩個 SELECT ＋ _format_result 投影 representation ＋ provenance（成對）
consumer：semantic_reranker payload ＋ semantic_model/api_server 優先採用
Level-A 10 筆 reviewed declaration 已寫入本機 DB，embedding 已重生
⑨ transport 四段閉合（DB → retriever → embedding → reranker），含負對照：
   sim(stored, embed(representation))=1.0000 ×10；對 question_summary 僅 0.58–0.82
   reranker 三向對照：A(representation) .9191 ／ B(summary) .6398 ／ C(舊 client) .6398
   真實檢索路徑上 4656 的 semantic_score 與 A **逐位相同** ⇒ 產線確實用 reviewed text 評分
```

### A04 holdout —— 收於 INCONCLUSIVE
```text
P=200 ＋ N=100 同一輪隔離生成並封存（兩 generator tool_uses=0）
兩位隔離 labeler（34-way 匿名開放式）：L1 0.9700／κ 0.9682
GATE=FAIL：P stratum **3498 僅 13/20** confirmed single-owner truth
⇒ A04 = INCONCLUSIVE / CORPUS_INSUFFICIENT；harness **NOT_EXERCISED**
⇒ corpus SEALED_UNBURNED，✅ 可作 diagnostic，⛔ 不得用於 authorization、
  ⛔ 不得抽子集續用於 A05（labels 已被看過）
⚠️ 業主定案的紀律：看過 ground-truth 分布後排除失敗 stratum 再續跑
  ＝ 依 validation outcome 改 scope，即使沒看檢索輸出也破壞 provenance
```

### 3498 → 退役；Level-A 版本化
```text
T2 逐 claim 比對：K1 FULLY_SUBSUMED（C5=NONE）＋ UNDER_QUALIFIED_DUPLICATE
  （無條件公式，對階梯式／固定金額版會給**錯誤金額**）
停用採「**失效，不失憶**」：is_active=false ＋ retirement 區塊；
  ⛔ 不刪 applicability／representation／provenance
LEVEL_A_V1 immutable（10 rows，digest 98a1b8af…）｜LEVEL_A_V2 active（9 rows，fcf67d29…）
不變量 13 版本化並新增 **V1 immutability 檢查**（重算 V1 digest 必須不變）
mutation 實證：把 3498 改回 active，三個 late-fee 查詢中**兩個**它回到 rank 1
  與真 owner 3939/3940 搶排序 ⇒「重複 knowledge 會重製 ranking competition」
```

### late-fee ownership 收斂（T1 → Step 1 → Step 2）
```text
T1 雙引擎比對：`_diagnose_late_fee`(A) = **E3 ONE_SIDE_PARTIAL**
  A-only 5 項中**只有 1 項是真能力**（pay_at/complete_at）；其餘為缺陷或量尺誤判
  A 會把**合約列叫成帳單**、**認不出滯納金帳單**
Step 1：把 pay_at/complete_at 搬進 B ⇒ **B_CAPABILITY_SUPERSET = CONFIRMED**
Step 2：移除 dispatch **＋** 移除 commit authority（決定性排除 late_fee_exclusion_facts）
  ⚠️ 只刪 dispatch 不夠——會落 generic path，那是「錯誤綠燈」
  `_DIAG_KEYWORDS` ⛔ 未動（generic discriminator，動它會誤傷發送／取消／手動到帳）
不變量 14 守住；precedence = **REJECTED**
```

### 3939/3940 —— ENTRY_ALIAS
```text
T3 provenance（正向證據）：billing-knowledge-review.md 明文
  「錨點（12 筆，answer 空、**一種講法一筆**）」；batch schema 每筆只有
  facet/question/keywords ⇒ 唯一語義負載是 facet ⇒ **H2 VARIANTS_BY_DESIGN**
裁定：KEEP_BOTH_AS_ENTRY_VARIANTS；⛔ CONSOLIDATE_ONE REJECTED；
  ⛔ 各自擁有 semantic contract REJECTED；representation = NOT_POPULATED_PENDING_ALIAS_GOVERNANCE
不變量 16（**TRANSITIONAL_GUARD**）：同 facet alias ⛔ 不得有**不同**的 representation
T4：late_fee **不是**唯一 question-insensitive builder（帳單異常也是）⇒ S3 MIXED
applicability 補齊：3531/3532 general、3939/3940 instance（皆 positive declaration）
```

### R10 / R10-P
```text
R10 裁定：A、B 皆 REJECTED_AS_TARGET；**C（responsibility_id）＋ C2 collapse** SELECTED
  ⚠️ B 淘汰理由是 **current fact**：facet=late_fee 內已同時有 general 與 instance 兩個責任
  ALIAS_ROLE = NOMINATION_ONLY（⛔ 無 semantic authority、⛔ 無 voting weight）
  APPLICABILITY_AUTHORITY = RESPONSIBILITY_LEVEL（alias 繼承；⛔ 現在不刪 row-level）
  RESPONSIBILITY_REVIEW_REQUIRED；automatic clustering = **proposal only, never authority**
R10-P0：governance population **54 筆**（LEVEL_A_V1 10 ∪ ENTRY_ALIAS 12 ∪ DECLARED 44）
R10-P1：schema 凍結 R10P-SCHEMA-2（applicability **兩軸**：declaration_status + value，
  合法 state space 四格；unknown ≠ INSUFFICIENT_EVIDENCE）
不變量 17：expected==emitted==distinct==54 ＋ **ID-set digest**（⛔ 只比 COUNT 會假綠）
```

---

## 三、凍結 digests（執行前必須逐項複驗）

```text
A04    PROTOCOL_SHA256    01b4d49e…（FROZEN）｜N_SELECTION 206f3923…
       CORPUS_SHA256      83a79070…（SEALED_UNBURNED）
       MANIFEST_SHA256    62d064e3…
       A04_SCOPE_VERSION  LEVEL_A_V1（10 rows）⛔ 不重算
Level-A V1 population 98a1b8af…｜V2 population fcf67d29…
R10-P  POPULATION 51c04b88…｜IDSET 00581c42…｜SCHEMA b72e749e…（R10P-SCHEMA-2）
       PROPOSAL fe6e5f3f…｜REGISTRY 3e5bcdc6…｜PROJECTION 0635b469…｜CENSUS ee8e398d…
執行期 IMPL_SHA 見 git HEAD；semantic-model image 8ea2a0b2…／api_server de4788b9…
       rag-orchestrator image 5f7559d2…／0.65・0.3・20
```

---

## 四、⚠️ 已知債與被擋的事

```text
divergent_pending  knowledge-admin create_knowledge 的 legacy embedding surface
                   （只建新列 ⇒ 跑不到已宣告的列；⚠️ 是**宣告的假設**非證明）
未解              B1 ×5（模型泛化）／B3 ×2（reranker discrimination）
未寫              3939/3940 的 retrieval_representation（待 alias governance）
未擴              LEVEL_A_V2 仍 9 rows；要擴需另立 V3 決策
⚠️ 不變量 17 會擋人：`APPLICABILITY_DECLARED` 是活查詢，任何新宣告都讓母體變 55 ⇒ FAIL
   （刻意做成 FAIL：review 中途母體靜默長大會讓已完成 review 失真）
⚠️ PROCESS_DEVIATION：`--build rag-orchestrator` 連帶重建 semantic-model image
   （業主已裁：記錄即可，⛔ 不判污染 ⑨）
```

---

## 五、下一步

```text
R10-P2  ✅ 已完成（見 r10p-p2-proposal-record.md）——47 群；允許規則②③**實際掛零**，
        唯一產生合併的是 entry-alias registry 的明文裁定；新增不變量 18 守分割身分
R10-P3  ✅ 完成 47／47
        C-1：6 具名分支 CONFIRMED＋3531/3532 CONFIRMED＋4420 MERGE→3365（joint_seal_pending）
        判準先例 P-C1/C2/C3/C4 已凍（p3-verdicts.json._precedents）⛔ 後續不得個案推翻
        C-4：3508／3514／3365 CONFIRMED（3365 members={3365,4420}，4420 pending 已封）；
             3361／3362／3366 INSUFFICIENT。先例增 P-C1a／P-C5／P-C6
        ⚠️ Batch A 補充 epoch：01/02/03 OWNERSHIP → CONFIRMED（BILL_FACE_BUILDERS 具名 builder）
           05 的「無 builder」升為正面 architecture fact；⛔ 舊 epoch 一律保留不覆寫
        C-5：3499 epoch2→CONFIRMED（_diagnose_manual_complete）；3501/3502 CONFIRMED；
             contract 十列收成 6 責任（3490/3491/3492/3493/3494/3510）＋4 筆 MERGE
        先例 P-C7（capability binding 由 utterance 決定）／P-C8（同 branch 變體合併）
        ⚠️ audit lesson L1：generation_metadata.evidence_detail ⛔ 不是 capability census
        最後六群：3507 CONFIRMED（wiring 錯但責任成立）／3511 SPLIT_REQUIRED／
                 3512・3513・4255 INSUFFICIENT／3498 HISTORICAL_ONLY
        當前統計：CONFIRMED 30／MERGE 5／INSUFFICIENT 10／SPLIT 1／HISTORICAL 1
        CAPABILITY_ALIGNMENT ＝ 正交 diagnostic 軸（⛔ 不進 verdict、⛔ 不改 SCHEMA-2）
        先例共 11 條（P-C1…C11＋P-C1a）；audit lesson L1 已記
        ⚠️ ⛔ 尚不得宣稱相異責任數——MERGE/SPLIT/HISTORICAL 未投影；⛔ 不得先開 A05
        裁定正本 r10p/p3-verdicts.json；不變量 19（12 條規則／22 項正對照）
R10-P4  ✅ registry 3e5bcdc6…／projection 0635b469…（五類投影規則已凍）
R10-P5  ✅ census ee8e398d…；⛔ 30 不是最終責任數（16 列未解）
下一步  ① canonical_responsibility 23/31 筆 PENDING_OWNER_STATEMENT ＋ responsibility_id
           目前是機械序號 R-01…R-31 —— 正式命名待業主
        ② 16 個未解列要不要另立調查
        ③ 才決定 A05 是否開案
```

> **Prime directive（寫在 R10-P schema 第一行）：
> Proposal is allowed to reduce search cost, never to establish responsibility truth.**
