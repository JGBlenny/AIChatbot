# T2：3498 vs 3531／3532 逐 claim 涵蓋比對（2026-08-29）

```text
判定單位  3498 的每個**可判語義 claim**（⛔ 不看字數、⛔ 不看主題相似度）
⛔ 不把 3498 的舊 `instance` metadata 當內容證據——本輪只裁 Knowledge content identity
⛔ 不因 3532 較長就預設它更完整——逐 claim 看**明示**內容，沒有就是沒有
⛔ 未改任何內容／applicability／categories
```

## 三列原文（逐字）

```text
3498（48 字）
逾期費計算公式：租金 × 遲繳天數 × 費率%。遲繳天數 = 付款日 - 到期日 - 緩衝天數。

3531（231 字）
系統的滯納金有兩種機制，依團隊採用而定：（一）延遲金（付款後結算）：租客實際付款當下
若逾期，以「租金 ×（實際付款日 − 繳費期限 − 緩衝天數）× 費率」結算，開立一張獨立的
延遲金帳單（到期日為產生當日），帳單附完整計算過程備註；（二）排程滯納金（對逾期未付款
的帳單開單）：每日排程檢查逾期帳單並依團隊設定開立滯納金帳單。共同規則：每張逾期帳單
各自結算、不累加；滯納金帳單本身逾期不會再產生滯納金；前提是合約的滯納金設定已啟用。
自動產生目前僅特定客製團隊開通。

3532（233 字）
滯納金的客製版本行為差異：（1）延遲金版（付款後結算）：百分比計算、帳單附三行式計算
過程備註、到期日為產生當日；（2）階梯式版（排程對未付款開單）：以租金乘固定費率
（不乘逾期天數），逾期超過門檻（預設 30 天）改用加重費率，每張帳單最多開兩筆，
到期日為合約迄日；（3）固定金額版（排程對未付款開單）：逾期滿一定天數（如 5 天）
收取一次固定金額，到期日為當月最後一天，且該滯納金帳單不標記逾期。各版本由團隊白名單
開通，同團隊採用一種；不確定適用哪種請聯繫客服確認。
```

## 逐 claim 對照

| claim | 3531 是否明示 | 3532 是否明示 | evidence 原句 | 判定 |
|---|---|---|---|---|
| **C1** 計算基礎是租金 | ✅ | ✅ | 3531「**租金** ×（…）× 費率」／3532「以**租金**乘固定費率」 | `COMPLETE` |
| **C2** 乘上遲繳天數 | ✅ | ⚠️ 明示**相反**（僅限階梯式版） | 3531「（實際付款日 − 繳費期限 − 緩衝天數）」／3532「以租金乘固定費率（**不乘逾期天數**）」 | `COMPLETE`（由 3531 承接） |
| **C3** 再乘費率 % | ✅ | ✅ | 3531「× **費率**」／3532「**百分比**計算」 | `COMPLETE` |
| **C3b** 遲繳天數＝付款日−到期日−緩衝天數 | ✅ 逐項對應 | — | 3531「實際**付款日** − 繳費**期限** − **緩衝天數**」 | `COMPLETE` |
| **C4** 這是一條 general calculation rule | ✅ | ✅ | 兩列皆為機制／版本的規則性說明 | `COMPLETE` |
| **C5** 3498 是否含 3531／3532 沒有的限制、例外或適用條件 | — | — | 逐句掃描：3498 **48 字全部**已對應到上列 C1–C4；⛔ 無任何額外限制、例外或條件 | `NONE` |

## ⚠️ 反向發現：3498 **少**了條件，而不是多了內容

```text
3498 把公式寫成**無條件**的「逾期費計算公式」。
3531 明示該公式**只適用**「延遲金（付款後結算）」這一種機制；
3532 明示另有兩種版本，其中階梯式版**不乘逾期天數**、固定金額版是**一次性固定金額**。
⇒ 單獨採用 3498，會對採用階梯式／固定金額版的團隊給出**錯誤金額**。
```

⚠️ 這**不是** `K3 SEMANTIC_CONFLICT`：公式本身與 3531 的延遲金公式**逐項相同**，
⛔ 沒有「相同命題有不同公式」的真實牴觸；差別是 3498 **缺少適用範圍限定**。
⇒ 屬於 **under-qualified duplicate**，停用它**不會損失任何 truth**，反而移除一個
會誤導的無條件斷言。

## Verdict

```text
K1 FULLY_SUBSUMED
  3498 的全部有效 general knowledge（C1–C4）已被 union(3531, 3532) 完整承接
  C5＝NONE：⛔ 無任何獨立責任殘留
```

## ⇒ 對 3498 的處置建議（產品裁定仍在業主）

```text
✅ 停用 3498 作為 Knowledge row
⛔ **不**建議改成 general
   理由：instance ownership 已被 B 接走（Step 2）＋ general answer 已被 3531/3532
   完整吸收 ⇒ 3498 沒有剩餘獨立責任；硬改 general 只會保留一筆**重複 knowledge**，
   重新製造 ranking competition，並且保留那個無條件公式的誤導風險。
```

## ⚠️ 停用前必須一併處理的連帶事項（⛔ 本輪未做）

```text
① 3498 是 **Level-A frozen 10 rows** 之一，且已寫入 reviewed
   `retrieval_representation`（本機）。停用它會讓
   **不變量 13（Level-A population 10/10）** 的母體定義失效。
   ⇒ 需先裁：Level-A scope 是否隨之縮為 9 rows，並重算 population digest。
② 3498 亦持有 `instance_applicability = instance` 宣告（P1e 逐筆裁定）。
   停用時該宣告一併失效，⛔ 不得留成孤兒宣告。
③ A04 corpus 中 intended=3498 的 20 句，其 truth 分布已記錄在 label manifest，
   ✅ 可留作本案的 diagnostic 證據。
```
