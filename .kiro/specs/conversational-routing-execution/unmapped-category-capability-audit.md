# 未 mapping category 的 capability disposition 盤查

- 日期：2026-08-29｜方法：純 SQL。代表例以**固定規則**選（同 category 內 `id` 升冪取首／中／末；
  ≤3 筆則全列），⛔ 未人工挑「最能證明問題」的標題。
- 目的：**先判斷 category 有沒有能力承擔 nomination evidence 這個角色**，
  ⛔ 不是問「要不要補 mapping」——後者會重新掉進「category = responsibility」的舊坑。

⚠️ 數字更正：先前寫 53 個未 mapping category，那次查詢未排除 `對話規則`／`系統脈絡`。
以情境知識為母體的正確數字是 **49 個**。

## 最重要的發現：**六個 `條件診斷：*` 分類全部沒有 Face**

```text
條件診斷：合約  10 筆  「合約為什麼不能發送簽約邀請」「合約為什麼不能續約」
條件診斷：付款   4 筆  「已付款但帳單狀態沒有更新」「信用卡付款失敗」「虛擬帳號過期或轉帳失敗」
條件診斷：物件   3 筆  「為什麼不能新增物件」「物件為什麼突然全部下架」「物件為什麼不能建立合約」
條件診斷：發票   2 筆  「發票為什麼沒有開出來」「發票為什麼作廢不了」
條件診斷：IoT    1 筆  「IoT 廠商帳號綁定失敗」
條件診斷：訂閱   1 筆  「訂閱扣款失敗導致功能異常」
                ─────
                21 筆，**零 Face mapping**
```

對照：`條件診斷：帳單` **有** mapping（→ `bill_diagnosis`）。

⇒ 這不是我看 category 名字判的——**KB 自己的命名慣例**就把這些標成診斷情境，
而它們的標題全是「為什麼我這個 X 不能／沒有／失敗」，非查個別狀態無法回答。
**七個診斷類只有一個被接上。**

## 逐 category disposition（**提案，待裁定**）

### INSTANCE_CAPABLE — 確實存在「需讀個別系統狀態」的情境，目前零 nomination path

| Category | rows | 代表例（機械選出） |
|---|---:|---|
| 條件診斷：合約 | 10 | 合約為什麼不能發送簽約邀請／不能續約／取消點交邀請 |
| 條件診斷：付款 | 4 | 已付款但帳單狀態沒有更新／信用卡付款失敗 |
| 條件診斷：物件 | 3 | 為什麼不能新增物件／物件為什麼突然全部下架 |
| 條件診斷：發票 | 2 | 發票為什麼沒有開出來／作廢不了 |
| 條件診斷：IoT | 1 | IoT 廠商帳號綁定失敗 |
| 條件診斷：訂閱 | 1 | 訂閱扣款失敗導致功能異常 |
| 租客管理 | 6 | 帳單無法寄送 租客聯絡方式未驗證／停用租客帳號 |
| 通知系統 | 6 | 租客收不到通知 租客資訊／各類通知觸發時機 |
| 大房東管理 | 6 | 大房東月報表／報表排程 自動寄送設定 |

### MIXED — 同一 category 同時承載 general ＋ instance

| Category | rows | 為什麼混 |
|---|---:|---|
| 合約管理 | 43 | 「合約狀態 12 階段總覽」（制度）vs「合約修改後更新 改租金要重填嗎」（要看那份約） |
| 帳單管理 | 26 | 「帳單狀態流程」（制度）vs「帳單統計報表 收支分析」（要看自己的帳） |
| 付款金流 | 24 | 「繳租金方式」（制度）vs「逾期帳單虛擬帳號 可否繳款」（要看那筆） |
| 業者操作指引 | 23 | 「系統功能總覽」（制度）vs「物件總表篩選 快到期空房 本月到期」（要看自己的物件） |
| 物件管理 | 9 | 「物件狀態 定義條件」vs「物件帳務合約紀錄 Dashboard」 |
| 修繕管理 | 5 | 「修繕流程 狀態說明」vs「報修多個品項 都壞了分開報嗎」 |
| 租客操作指引 | 7 | 「租客可以使用哪些功能」vs「查看合約帳單 資訊查詢」 |
| 發票開立 | 11 | 「發票開立時機 觸發條件」vs「發票補開 特殊處理」 |
| 帳號管理 | 10 | 「註冊帳號 建立流程」vs「一個帳號加入多個團隊 身分切換」 |
| IoT 智慧設備 | 5 | 「智慧門鎖 使用說明」vs「電表低電量警報」 |
| 委託合約 | 4 | 「與出租合約差異」vs「PDF 文件下載」 |
| 綜合指引 / 訂閱方案 / 社區管理 | 5/5/3 | 制度說明與操作／狀態查詢並存 |

### GENERAL_ONLY — 本質只需通用說明；無 mapping **不構成缺口**

```text
社會住宅制度 24｜管理費收費規則 24｜分租與轉租 24｜設備管理責任 24
公共設施與社區規範 23｜租客權益與爭議處理 23｜包租業 vs 代管業差異 23｜租賃契約與法規 23
修繕與維護責任歸屬 21｜安全與保險 21｜居住使用規範 21｜戶籍與居住證明 21｜業者服務範圍 21
入住與退租常識 18｜押金（保證金）規定 18｜點交規範與責任 18｜委託管理合約 17
租金與費用常識 14｜稅務相關 12｜提前解約與違約 11｜租金代收制度 9
售前模組 7｜售前方案 3｜售前競品 3｜售前顧問 3｜售前價格 2
```

⇒ 這些是法規、行情、制度、產品介紹——**同一段說明對所有使用者成立**。
它們占了 78.4% 裡的絕大部分，所以那個大數字**本身不是架構缺口**。

## 三個結論

```text
① GENERAL_ONLY 占大宗 ⇒ 「78.4% 無 mapping」看起來嚇人，但多數不是問題
② 真正的結構缺口很具體：**六個 條件診斷：* 分類（21 筆）零 mapping**，
   而第七個（帳單）有 —— 同一套命名慣例只接了七分之一
③ MIXED 才是最深的一層：合約管理 43 筆、帳單管理 26 筆同時承載制度與實值查詢
   ⇒ 問題可能不是「漏補 mapping」，而是
     **category 這個粒度不足以表達 responsibility applicability**——
     這直接支持裁定 001（category = nomination evidence ≠ responsibility authority），
     並進一步指向：**連作為 nomination evidence，它可能都太粗。**
```

## 本盤查**不能**回答

```text
⛔ production 有多少 request 被漏接
⛔ 某 category 應該以多少比例進 Face
⇒ 兩者都需要真實 Layer-0 traffic
```

---

# ⚠️ 重大更正（2026-08-29，逐筆 census 之後）

## 上面「六個條件診斷分類 → 21 筆缺口」的推論**在 row level 不成立**

逐筆查完 21 筆才發現：**它們大多帶兩個 category**，第二個是有 mapping 的。
而 `_knowledge_category` 回的是**全部** categories，nomination 迴圈會逐個試——
所以「某個 category 沒 mapping」**不等於**「這筆知識無法被提名」。

```text
3490 合約為什麼不能發送簽約邀請  條件診斷：合約 ／ **狀態判斷** → contract_diag  ✅ 可提名
3497 已付款但帳單狀態沒有更新    條件診斷：付款 ／ **繳費金流排障** → billing_flow ✅
3503 發票為什麼沒有開出來        條件診斷：發票 ／ **發票** → billing_invoice     ✅
3505 為什麼不能新增物件          條件診斷：物件 ／ **物件操作引導** → estate_guide ✅
3507 物件為什麼不能建立合約      → estate_diag, contract_diag                     ✅
```

### 真正的 row-level 孤兒只有 **2 筆**

```text
3508  IoT 廠商帳號綁定失敗       僅 條件診斷：IoT     form_fill／jgb_iot_diagnosis
      ⇒ **已是不變量 1 的明文豁免**（「表單為廠商選擇分流 by design」）
3509  訂閱扣款失敗導致功能異常   僅 條件診斷：訂閱    direct_answer、無 form_id
      ⇒ **唯一未被涵蓋、也未被豁免的一筆**
```

⇒ 實際缺口是 **1 筆知識**，不是 21 筆。

## 連帶：`make audit` 不變量 1 通過是**正確的**

它以 **row level** 判「動作知識必有面向接管或明確豁免」——這正是對的層級。
我先前以 category level 推論才得到相反的印象。

## 我在這條線上重複犯的錯（記下來，避免第三次）

```text
① 21.5% 被寫成「production 覆蓋上限」——實為 KB configuration coverage
② 「六個條件診斷分類零 mapping」被推成「21 筆知識無法提名」——實為 1 筆
共同形狀：**用 category 層的事實去推 row 層的結論**，
而 nomination 實際上是 row 層、逐 category 迴圈的行為。
⇒ 往後任何 category 層統計，都必須先在 row 層驗證再下結論。
```

## 修正後的 disposition

```text
3509「訂閱扣款失敗導致功能異常」→ **AMBIGUOUS，需產品裁定**
  ・它是 direct_answer 且答案自足（已寫明處理方式：到訂閱方案頁確認狀態與付款）
  ・「訂閱」目前**沒有**任何 Face，也沒有 responsibility contract
  ⇒ 是要新增 Face、掛到既有 Face、還是維持單發即可？這是產品能力決定，不是設定缺陷。
3508 → **OWNER_EXISTS（by design 豁免）**，不動。
其餘 19 筆 → **OWNER_EXISTS**，已可經第二個 category 提名，⛔ 不需補 mapping。
```

## 另一個獨立事實：responsibility contract 幾乎不存在

```text
22 個面向中，只有 **bill_diagnosis／billing_anomaly 兩個**宣告了 responsibility。
其餘 20 個沒有任何 machine-readable 的 accepts／excludes／delegates。
⇒ 「逐筆對 existing responsibility contract 做 ownership audit」目前**做不到**——
  可比對的契約只有兩份。ownership 只能從 persona 自然語言推，那不是機器證據。
```
