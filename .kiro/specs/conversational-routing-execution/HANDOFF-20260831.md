# 交接：conversational-routing-execution（2026-08-31）

> ⚠️ 本檔為**可無上下文接手**的狀態快照。舊檔 `HANDOFF-20260830.md` **保留為歷史 checkpoint，⛔ 未覆寫**。
> ⚠️ **本檔是摘要，⛔ 不是 authority。** 逐筆事實一律以
> `r10p/fulfillment/f2-dossier.json` 為準；本檔與 dossier 衝突時**以 dossier 為準**。

分支 `fix/retrieval-routing-stability`｜HEAD `3eb27bd`｜工作區乾淨｜⛔ **從未 push**
⛔ production JGB 全程未動；所有 DB 寫入與容器操作皆在本機（`aichatbot_test` / `aichatbot_admin`）。

---

## 0-A. 🔄 2026-08-31 修訂（**後於本檔封存**，以本節為準）

```text
兩條原始 ⛔ 已被業主裁決取代，⛔ 不要再依原文拒絕：

① F-OPEN-01 = **CLOSED_BY_PRODUCT_TRUST_BOUNDARY**（凍 F-C25）
   role_id 的權限由**上游 API** 保證；AIChatbot 視為 verified_role_id，⛔ 不重複驗權。
   ⛔ 不得在 AIChatbot 補 `request.user == role` 或 authorization middleware。
   ⚠️ 用詞：`verified_role_id = role_id authorized by trusted upstream API`，
      ⛔ 不得寫成「AIChatbot verified role_id」（它實際沒有驗）。
   ⇒ R-20／R-21／R-22 的 IDENTITY_RESOLUTION、R-23 另加 ROLE_SCOPE_REACHABILITY **已解除**。
   ⚠️ 只涵蓋 role identity；R-24 的 user identity 仍受 F-C13 拘束，**維持 BLOCKED**。
   ⚠️ 前提：所有到達端點的 role_id 皆經上游授權。出現繞過上游的呼叫者即前提破，需重開。

② D1-R = **UNBLOCKED**（外部測試輸入條件已具備；⚠️ D1-R1／D1-R2 **尚未執行**）
   業主裁定 **role_id=20151 是測試團隊**，www 可用於 D1-R
   ⇒ 原「⛔ 不得用 production JGB 代替 D1-R」的**理由**（真實客戶資料）不成立。
   known test entity：contract 89071『測試物件』／**bill 759304**
   ⚠️ ground truth 是快照，重跑必須重抓，⛔ 不得沿用舊數值當期望值。
   ⛔ 仍然有效：不得用 mock 代替、不得因 unit 綠就當已驗、不得因環境已備就升級。

⚠️ 逐筆事實一律以 dossier 為準：`_d1r_status`／`_f_open_01_closure_2026_08_31`。
```

---

## 0. ⛔ DO NOT（下一個 session 最容易犯的七件事）

```text
⛔ push / merge main / deploy            —— SEC-01 OPEN
🔄 用 production JGB 代替 D1-R           —— **已修訂**：www+role 20151（測試團隊）獲授權，見 §0-A②
🔄 把 request.role_id 當 verified_role_id —— **已修訂**：F-OPEN-01 已閉合，見 §0-A①
⛔ 因 F2 30/30 就註冊 24 個 proposed bindings —— closure ≠ capability approval（見 §5）
⛔ 關掉 delegates 來「修」PROD-DEFECT-02  —— 那只是把缺陷換一面（見 §7）
⛔ 把 flaky baseline 只寫成 failure count —— E-C1：判準是集合不是數字（見 §7）
⛔ 重開已裁完的 responsibility identity review —— Registry V2 已 sealed（見 §3）
```

---

## 1. Governing principles

```text
責任 = semantic authority boundary          responsibility ⛔ 不由 row／utterance／face 決定
row 只是 reviewed content storage source    ⛔ 永遠不是 semantic authority（F-C4／L2）
no-reroute 由**結構**保證                    簽名裡沒有 user_question／face／category，⛔ 不靠自律
否定結論需正對照                             回報「沒有／不存在」前必須有已知必存在的對照項
空跑不是通過                                 passed=0 / no tests ran ⇒ 沒跑，⛔ 不是綠
mutation 用真實原始碼                        in-test 模擬 ⛔ 不足以證明 guard 不是裝飾
```

## 2. 已推翻的舊前提（NEVER REINTRODUCE）

```text
❌ 「contract 註冊了就等於 execution 可閉合」        → F-C14
❌ 「resolve_collection 就是 fetch resolver」        → F-C15（它只收已取回的 members）
❌ 「lane 就是 blocker 全集」                        → F-C16（blocker 是合取）
❌ 「endpoint 收 bill_id / 信封回吐 bill_id ＝ 已圈定」→ F-C18／F-C20
❌ 「空集合沒有反例 ＝ scope 已驗」                   → F-C19（vacuous truth）
❌ 「adapter 沒註冊 ＝ adapter semantics 未建立」     → F-C24
❌ 「baseline 只要看 failure count」                 → E-C1
❌ 「E-DEBT-02 是 DB path 問題」                     → 實為 rollback **SQL 檔**路徑；且無第二顆乾淨 DB 可換
❌ 「PROD-DEFECT-01 的 stale state 會改變輸出」      → 584 組合實測：0 組輸出不同（見 §8）
```

## 3. R10 / Registry V2 / canonical / C2-SCORE（最終狀態，⛔ 不重開）

```text
R10-P          P0…P5 全收束：active 30 責任／historical 1／row disposition 54＝A36/B1/C1/D16
R10-CANONICAL  30/30 完成；registry epoch 2 已封存
R10-C2         I1/I2/I3 ＋ RERANK-REQUIRED ＋ C2-SCORE 全部 module 層完成
稽核            23 條 OVERALL PASS（CODE 0 ／ PRODUCT 0）

REGISTRY_V1     3e5bcdc6…（immutable）   REGISTRY_V2 2071b7f5…（sealed）
V2_EQUIVALENCE  ac9108d0…（STRUCTURALLY_EQUIVALENT：只有 canonical 面改變，topology 零漂移）
POPULATION 51c04b88…｜IDSET 00581c42…｜SCHEMA b72e749e…（R10P-SCHEMA-2）
PROJECTION 0635b469…｜CENSUS ee8e398d…（⛔ 不改寫）
FULFILL_SCHEMA e3f774dd…｜FULFILL_PROPOSAL ffc564fe…（⛔ 未 seal）
CANONICAL_EMBEDDINGS 30 筆／dim 1536／text-embedding-3-small
⚠️ 兩個 denominator ⛔ 不得互相污染：row coverage=54；responsibility census=30 active
⚠️ 16 unresolved rows 暫不追（P5 已證與 LEVEL_A_V2 無責任共用）
```

## 4. T4 fulfillment architecture

```text
T4-A 責任 session carrier ✅   T4-B entity resolution ✅   T4-B4/B5 fetch resolvers ✅
T4-C 直接執行 registry ✅      T4-D1 completion handler ✅  T4-D2 GROUNDING_FACTS 出口 ✅
D1-E envelope converter ✅（**三個 result family**，依型別分流）
```

**三個 result family**（⛔ 不靠 key 猜）
```text
FulfillmentExecutionResult          FINAL_TEXT
ResponsibilityGroundingResult       GROUNDING_FACTS → grounding_presentation.present()
ResponsibilityInputResolutionResult AMBIGUOUS／NO_MATCH／INVALID_INPUT
```

**三種 fulfillment precedent**（遇到新函式形狀 ⛔ 不需重新發明規則）
```text
R-29 direct FINAL_TEXT   ／   R-28 direct GROUNDING_FACTS   ／   R-31 composite
```

**凍結判準 43 條**，本輪新增 13 條（完整清單在 dossier `_frozen_contracts_added`）：
```text
F-C14 contract 註冊 ≠ execution closure
F-C15 shape resolver ≠ fetch resolver
F-C16 blockers are conjunctive（lane 只是「最具決定性的一條」）
F-C17 fetch resolver is transport, not semantic routing
F-C18 scope proof 可來自 transport，但**必須被證明**（簽名／參數／echo 都不算）
F-C19 empty collection cannot prove member scope
F-C20 request scope ≠ data scope；升 CONFIRMED 必須附可稽核證據引用
F-C21 envelope reference integrity ≠ scope authority
F-C22 empty adapter extends reviewed branch domain
F-C23 knowledge content must be scope-contained
F-C24 implemented semantics ≠ runtime authorization
W-G1  input_contract_id → exact registered resolver（⛔ 無 fallback／動態組名）
W-G2  grounding exit 保持 responsibility authority（authority 取自 plan，⛔ 非 adapter 輸出）
E-C1  baseline identity is set-based, not count-based
```

---

## 5. ⚠️⚠️ F2 最終狀態 —— 最容易被誤讀的一節

```text
F2_STRATEGY_REVIEW_COVERAGE = 30/30      每筆責任的 fulfillment strategy 都審完了
F2_APPROVAL_CLOSURE         = 30/30      每筆都有**最終裁定**了

                    ≠

APPROVED_CAPABILITY         = 0          ⚠️ 仍然是零
```

**machine count（⛔ 不手寫，取自 dossier）**
```text
APPROVED_KNOWLEDGE_CONTENT     5    R-06 R-07 R-25 R-26 R-27
INSUFFICIENT_EVIDENCE         24    F2-A 4 ＋ F2-B 20
FULFILLMENT_NOT_ESTABLISHED    1    R-01（有輸入卻沒有合法 responsibility-specific capability）
APPROVED_CAPABILITY            0
```

> ⚠️ **「30/30 closure」⛔ 不是「30 個 runtime fulfillment 都完成」。**
> 它只表示「每筆都審完並有裁定」；其中 24 筆 instance responsibility 仍因
> execution／external blocker 維持 `INSUFFICIENT_EVIDENCE`。
> ⛔ **executable registry 仍只有 3 個 binding**（R-28／R-29／R-31），本輪 0 個新註冊（F-G1）。

## 6. Input-contract / resolver 現況

```text
SELECT_ONE   bill.by_ref.v1 ／ contract.current_entity.v1 ／ estate.by_ref.v1
             point_refund.bill_by_contract.v1 ／ late_fee.bill_or_contract.v1
SINGLETON    subscription.current.v1 ／ tenant.summary.v1（singleton_runtime_contract=NOT_ESTABLISHED）
COLLECTION   payment_logs.by_bill.v1 ／ invoice_logs.by_bill.v1 ／ iot.manufacturers.v1
             （三者 empty_collection_policy 皆已裁 RESOLVED_EMPTY）
```

**接線（W-G1，`responsibility_completion._RESOLVER_SPECS`）**
```text
ROLE_AND_REF   bill.by_ref.v1 ／ estate.by_ref.v1
ROLE_ONLY      subscription.current.v1 ／ iot.manufacturers.v1
UPSTREAM_REF   payment_logs.by_bill.v1 ／ invoice_logs.by_bill.v1
               ⚠️ 先經 bill.by_ref 解析出 resolved_bill_id 才跑 fetch；
                  上游未 RESOLVED 一律原樣交還，⛔ 不呼叫下游
               ⚠️ 拿 form 的 raw bill_ref 冒充 resolved id ＝ 與 R-24 的 tenant_keyword 同一種病
tenant.summary.v1  ⚠️ **刻意不接**（R-24 IDENTITY_BLOCKED）——明示缺席才看得見
```

**scope provenance（三個 COLLECTION contract）**
```text
payment_logs   BILL_SCOPE_PROVENANCE        = CONFIRMED（ENVELOPE_VERIFIABLE）
               member_scope_proof           = UNAVAILABLE_BY_RESPONSE_SCHEMA
               ENVELOPE_REFERENCE_INTEGRITY = VERIFIED / N/A_EMPTY（F-C21）
invoice_logs   MEMBER_VERIFIABLE（member 帶 bill_id）
iot            MEMBER_VERIFIABLE（member 帶 role_id；唯一與 collection_scope_key 同名）
```
payment_logs 的 server-side 證據（jgb2 `External\PaymentLogApiController@index`，唯讀實查）：
```text
① role_id／bill_id 皆必填（缺即 400）
② bills WHERE id=$billId AND owner_role_id=$roleId AND active=1 → 查無 404
③ payments WHERE paymentable_type='App\Bill' AND paymentable_id=$billId   ← 直接述詞
④ payment_logs WHERE payment_id IN (③ 的 ids)                              ← 傳遞性述詞
⑤ if (!empty($paymentIds)) 守衛 ⇒ 無未過濾路徑
旁路：只有一條 /payment-logs 路由；External 15 支 controller 中只有這支碰 payment_logs
⚠️ **傳遞性、非直接述詞** ⇒ 若 payments 的 polymorphic 關聯改動，本 provenance **需重驗**
```

## 7. Baseline Epoch 1（regression oracle）

artifact `evidence/baseline-epoch-1.json`｜digest `2b5d353dec38fc5b…`

```text
DB_STATE   seed_responsibility_delegates_v1 = APPLIED ＋ ledger PRESENT ＋ consistency CONFIRMED
scope      test_facet_entry_routing_req.py ＋ test_instance_gate_seam_req.py（collected 103）

KNOWN_STABLE_FAILURES (7)
  facet:op_billing[收據 PDF 在哪裡下載]
  facet:op_general[點退做完後，帳單會自動出來嗎？]
  gate:exception_fails_open_and_is_not_disguised_as_abstain
  gate:instance_questions_survive_activation ×4

FLAKY_RED_ALLOWLIST (2)   ⚠️ 具名白名單，⛔ 不得寫成 FLAKY_SLOT=0..N
  facet:op_billing[系統怎麼算點退帳單的金額]
  facet:op_billing[點退帳單的金額是怎麼算的]

ORACLE（E-C1）
  actual_red − KNOWN_STABLE_FAILURES − FLAKY_RED_ALLOWLIST ≠ ∅  ⇒  NEW_REGRESSION
  ⚠️ 只看 7..9 這個數字會假綠：**穩定紅少 1 筆 ＋ 冒出全新一筆 ＝ 總數不變**
UNEXPECTED_BASELINE_IMPROVEMENT
  stable 之一突然變綠 ⇒ ⛔ 不叫 regression；查清產品修好／模型 drift／harness drift 前 ⛔ 不得刪除
```

**PROD-DEFECT-02 DELEGATION_SCOPE_TOO_BROAD = CONFIRMED**（另案，⛔ 未修）
```text
chain  bill_diagnosis → billing_anomaly → contract_closeout
A/B    delegates ON  → instance 進得去，但 operation／制度型問句被過度吸入
       delegates OFF → operation 不再被吸，但 instance 進不去
       兩態各非全綠、失敗族**互換**
證據   同一 semantic family：stable boundary violations = 2 ／ boundary-flaky members = 2
結論   缺 **applicability boundary**；⛔ 關掉 delegates 不是修復；⛔ 不得改 expectation 讓它們變綠
⚠️ 它以前看不見，是因為測試 DB 長期髒著（E-DEBT-02）
```

**E-DEBT-01（未修）**：`scripts/run-tests.sh` 會把 `-k "a or b"` 逐字拆開 ⇒ `or` 被當檔名，
`no tests ran` 卻印 `failed=0`。⚠️ 本輪實際踩到；跑子集請直接指定整個檔案。

## 8. PROD-DEFECT-01（BUGFIX-PAYMENT-NOT-REFLECTED-01）

```text
status  **FIXED_LOCAL**        introduced_before_T4 = CONFIRMED（⛔ 非 responsibility 架構造成）
target  services/jgb/payments.py::_diagnose_payment_not_reflected
change  迴圈內新增 per-iteration `code = ""` —— 6 行純新增，零刪除零修改

A CRASH  第一筆缺 response → UnboundLocalError（容器內實測重現）
B STALE  **CONFIRMED_STATE_LEAKAGE ／ NOT_OUTPUT_OBSERVABLE**
         窮舉 8 形狀 × 長度 1–3 ＝ 584 組合：修前 crash 219 組；修前/修後輸出不同 **0 組**
         ⚠️ ⛔ 不升 CONFIRMED_BEHAVIOR
         ⚠️ 這同時證明本修對所有非 crash 輸入是**行為保持**的
guards  tests/unit/api/test_bugfix_payment_not_reflected_01_req.py（19 條，G1…G5 ＋ M1）
        M1 實際改原始碼移除該行 → 19 中 8 紅；還原後位元一致
history ⚠️ `test_r12_records_known_production_defect` 由「記錄缺陷仍在」**轉為**「守住不回歸」，
        ⛔ 未刪除——docstring 保留完整歷史，否則日後看不出 R-12 為何曾被擋
```

## 9. Blocker matrix（10 筆尚未 executable-approved 的 instance responsibility）

> ⚠️ 完整逐筆 required_blockers 在 dossier；本表是摘要，**⛔ 不得取代 dossier**。

```text
R-12 R-15 R-16 R-18 R-19          BINDING_REGISTRATION ＋ EXTERNAL_VALIDATION        ← 最接近
R-20 R-21 R-22                  ＋ IDENTITY_RESOLUTION（F-OPEN-01）
R-23                            ＋ IDENTITY_RESOLUTION ＋ ROLE_SCOPE_REACHABILITY（F-OPEN-01）
R-24                              IDENTITY_BLOCKED ＋ INPUT_FETCH_RESOLVER（刻意未接線）

其餘 14 筆 instance（類 A 等）維持 INSUFFICIENT_EVIDENCE —— 逐筆見 dossier
lane：B1 與 B3 皆已清空；⚠️ lane 空 ⛔ 不代表完成（F-C16）
```

## 10. External blockers（本地無法推進）

```text
D1-R       🔄 **UNBLOCKED（2026-08-31）** —— ⚠️ D1-R1／D1-R2 尚未執行
           業主裁定 role_id=20151 為測試團隊 ⇒ www 可用；實查 516 份合約佐證
           known test entity：contract 89071 ／ bill 759304（ground truth 已抓，⚠️ 是快照）
           安全邊界已證：_request=GET；_post_request 唯一呼叫者 /repairs；無 put/delete/patch
           ⛔ 仍不得用 mock 取代、⛔ 不得因環境已備就升級（需實跑）

F-OPEN-01  DATA_ROLE_SCOPE        = CONFIRMED
           CALLER_ROLE_AUTHORITY  = REQUEST_CLAIM_UNRESTRICTED（AIChatbot 邊界，repo 內已證）
                                  ＋ INCONCLUSIVE（jgb2 實例設定在 production DB，未查）
           layer1：role_id 是純 request 欄位；/api/v1/message 無 Depends 認證；
                   verify_api_key 只回 {id,name} ⇒ 完全不綁 role；RAG_API_AUTH_ENFORCE 預設關
           layer2：jgb2 role 白名單 **opt-in**，未設即不限團隊；is_root 直接跳過
           ⚠️ 即使 JGB_API_KEY 設了白名單也閉合不了：它限制「服務能觸及哪組 role」，
              ⛔ 不是「哪個呼叫者有權使用哪個 role」。**信任止於服務，⛔ 不傳遞到端點呼叫者。**
           ⚠️ ⛔ 這**不是 implementation debt**，是 **PRODUCT_AUTHORITY_DECISION_REQUIRED**：
              尚未知道真正的 authentication principal 是誰、role 是一對一還是一對多、
              server-to-server caller 是否本來就被視為 trusted。
              ⛔ 工程師 ⛔ 不得自行加 `request.user == role`。
           ⛔ 未定性為 security defect、⛔ 未查 production 設定

F-OPEN-02  invoice-logs endpoint 在 production **不做 role 圈定**（NOT_YET_RULED）
           正確問題：bill scope 是否足以繼承已驗 bill 的 authority，還是存在跨 role 以 bill_id 讀取？
           ⛔ 不得因 controller 沒 role filter 就直接判錯，⛔ 也不得因此關閉

SEC-01     **OPEN —— 擋 push，不擋本地開發**
           OPENAI_API_KEY 曾明文進 transcript。push 前硬閘：撤銷 key／裝替代 key／掃 committed history
           ⛔ 線上操作不代執行
```

## 11. Tests / audit / git

```text
unit          1956 passed / 0 failed
稽核           23 條 OVERALL PASS（CODE_CONTRACT_REGRESSIONS 0 ／ PRODUCT BLOCKERS 0）
integration   逐檔跑；判準用 Baseline Epoch 1 的**集合式 oracle**（§7），⛔ 不看總數
分支           fix/retrieval-routing-stability｜HEAD 3eb27bd｜工作區乾淨｜⛔ 從未 push

本輪 11 個 commit（6681973..3eb27bd）
  6c0f5a0 10 筆重分類 → 四條 primary_closure_lane ＋ F-C14/15/16
  7d8d111 R-16 empty review → ADAPTER_REQUIRED
  45d31aa B3 三個 fetch resolver ＋ F-C17
  2ea5957 _RESOLVERS 接線 ＋ T4-D2 出口 ＋ F-C18/19/W-G1/W-G2
  1638fd6 E-DEBT-02 修復 ＋ 封存 BASELINE EPOCH 1（E-C1）
  bfdbd70 payment_logs scope audit → ENVELOPE_VERIFIABLE ＋ F-C20
  420fbf4 F-C21 ＋ F-OPEN-01 全鏈裁定
  221e9c2 R-16 auto_pay empty adapter ＋ F-C22
  8da06a6 F2-C 五筆收案 ＋ F-C23
  25665c4 BUGFIX-PAYMENT-NOT-REFLECTED-01
  3eb27bd F-C24 ＋ R-26 窄內容收斂 ⇒ F2 closure 30/30
```

⚠️ **改過產線檔後（不變量 3）**：
```bash
docker cp <file> aichatbot-rag-orchestrator:/app/<path> && docker restart aichatbot-rag-orchestrator
```
⚠️ 測試一律走 `scripts/run-tests.sh`（容器內 py3.11）。

---

## 12. NEXT START

```text
No locally unblocked implementation work remains.

Resume only when one of these changes:

A. 🔄 **已具備**（2026-08-31）：www + role 20151（測試團隊）+ bill 759304
   → **下一步就是執行 D1-R1／D1-R2**，不需再等環境
   → 通過後解 10 筆的 EXTERNAL_VALIDATION

B. ✅ **已完成**（2026-08-31）：F-OPEN-01 CLOSED_BY_PRODUCT_TRUST_BOUNDARY（F-C25）
   → R-20 R-21 R-22 R-23 的 IDENTITY_RESOLUTION 已解除
   → ⚠️ 四筆仍餘 BINDING_REGISTRATION ＋ EXTERNAL_VALIDATION（F-C16 合取）

C. SEC-01 is closed
   → run secret/history scan and reassess push readiness
```

⚠️ 🔄 **2026-08-31 後已不適用**：D1-R 可執行，路徑 A 是明確的下一步。
   （以下原文保留為歷史）若下一個 session 找不到事情做——**那是正確狀態**，⛔ 不要為了推進數字去碰 production、
⛔ 不要擅自定義 authentication semantics、⛔ 不要註冊尚未通過 external validation 的 binding。
