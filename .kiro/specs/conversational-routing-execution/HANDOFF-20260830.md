# 交接：conversational-routing-execution（2026-08-30）

分支 `fix/retrieval-routing-stability`，⛔ **從未 push**。工作區乾淨。HEAD `7e12cc8`。
⛔ **production JGB 完全未動**；所有 DB 寫入與容器操作皆在**本機驗證環境**。
本輪共 **46 個 commit**（`d308780..7e12cc8`）。

---

## 一、當前狀態（一眼版）

```text
稽核            23 條 OVERALL PASS（CODE 0／PRODUCT 0）
unit            1869 passed / 0 failed
R10-P           P0…P5 **全部收束**：active 30 責任／historical 1／row disposition 54＝A36/B1/C1/D16
R10-CANONICAL   **完成 30/30**；registry **epoch 2 已封存**（V2 2071b7f5…）
R10-C2          I1/I2/I3 ＋ RERANK-REQUIRED ＋ C2-SCORE 全部 module 層完成
T4 transport    A ✅／B1 B2 B3 B4 ✅／C1 C2 ✅／D1 D1-E ✅／D2 ✅
R10-FULFILLMENT F0 F1 ✅；F2-A 5 筆 ✅／F2-B 20 筆 ✅／**F2-C 5 筆未開**
A05             judgment_unit RESOLVED／denominator 9；blocker＝TARGET_CANDIDATE_IMPLEMENTATION_INCOMPLETE
SEC-01          **OPEN —— 擋 push，不擋本地開發**
D1-R            **BLOCKED_EXTERNAL_TEST_INPUT**
```

---

## 二、⚠️ 開工前必讀的三件事

```text
① SEC-01（OPEN）：OPENAI_API_KEY 曾明文進 transcript（我自己下 `env | grep -i openai` 造成）。
   push 前硬閘：撤銷 key／裝替代 key（建議 restricted permissions）／掃 committed history。
   ⛔ 線上操作不代執行。已加 4 條 unit guard 防「第二次由 exception/debug log 洩漏」。
② 每次改 routers/chat.py 等產線檔後，**不變量 3 會紅**（容器與本地不一致）：
   `docker cp <file> aichatbot-rag-orchestrator:/app/<path> && docker restart aichatbot-rag-orchestrator`
③ 測試一律走 `scripts/run-tests.sh`（容器內 py3.11）。integration 打的是 **aichatbot_test** DB，
   不是 admin DB——T4-B2 的 migration 兩個 DB 都要套過才綠。
```

---

## 三、凍結 digests（執行前逐項複驗）

```text
POPULATION 51c04b88…｜IDSET 00581c42…｜SCHEMA b72e749e…（R10P-SCHEMA-2）
PROPOSAL   fe6e5f3f…
REGISTRY_V1 3e5bcdc6…（**immutable**）｜PROJECTION 0635b469…｜CENSUS ee8e398d…（⛔ 不改寫）
REGISTRY_V2 2071b7f5…（sealed）｜V2_EQUIVALENCE ac9108d0…（STRUCTURALLY_EQUIVALENT）
CANONICAL_EMBEDDINGS 檔 75dab05d…／30 筆／dim 1536／provider text-embedding-3-small
FULFILL_SCHEMA e3f774dd…｜FULFILL_PROPOSAL ffc564fe…（⛔ 未 seal）
R10P_STAGE = FULFILLMENT_F0_F1
```

---

## 四、本輪產出的**新增不變量**（18→23）

```text
18 proposal 分割身分（防「漏一列＋重複一列、總數仍 54」假綠）
19 P3 裁定紀錄合法性（12 條規則／22 正對照；含 epoch 模型、MERGE/SPLIT、capability_alignment 正交）
20 P4 registry 投影完整性（防為湊滿 54/54 而**製造 authority**）
21 active canonical 閉合（**產品閘**；30/30 後轉綠）
22 registry V1→V2 scope lock（只准改 canonical 面；sealed 須附 structural-equivalence）
23 canonical embedding index（G10；provider_model_id／service_version／contract_version **三欄分開**）
```

---

## 五、⚠️ 未決事項（依阻塞層級排）

```text
D1-R          BLOCKED_EXTERNAL_TEST_INPUT
              本機 USE_MOCK_JGB_API=false 且 base_url 是 **production-like**
              需要：test/staging 設定（⛔ 不要貼進對話）＋ 一筆已知測試帳單
              ⛔ 不得用 mock 取代、⛔ 不得把 ENTITY_INPUT_REACHABILITY 升 CONFIRMED

PROD-DEFECT-01 **CONFIRMED PREEXISTING**（⛔ 未修）
              payments.py::_diagnose_payment_not_reflected 的 `code` 只在 `if response:` 內指派
              ⇒ 第一筆 log 缺 response 即 UnboundLocalError（實測）
              另記 STALE_STATE_PROPAGATION_RISK = CONFIRMED_BY_CODE_SHAPE

E2E-DEBT-01   PREEXISTING（**非 D1 regression**，已用 parent file 回放證明）
              test_digression_cancel_answers_pending_question_e2e：sources=[]／vendor_sop 缺
              ⛔ 不得 xfail 只為讓 suite 變綠

tenant.summary IDENTITY_REACHABILITY_BLOCKED
              form api_config 是 `user_id: "{form.tenant_keyword}"` ⇒ keyword 逐字當 id
              分類為 **identity assumed, not resolved**（⛔ 不是 F-C3 的 first-row 問題）

R-16          empty_input_handling = NOT_YET_REVIEWED（⛔ 不得因同 family 繼承 R-15）
F2-C          5 筆 KNOWLEDGE_CONTENT 未開
16 unresolved rows  暫不追（P5 已證與 LEVEL_A_V2 無責任共用）
```

---

## 六、下一刀（業主已定序）

```text
1. 重新分類 F2-B 那 10 筆 NOT_YET_JUDGEABLE ——resolver 落地後狀態已不一致，
   需拆出 EXECUTION_BLOCKED_BY_PROD_DEFECT（R-12）與 IDENTITY_BLOCKED（R-24）兩個新類
2. 之後：R-16 empty review／PROD-DEFECT-01 修不修／F2-C ／ D1-R 解封
⛔ 不要立刻把 20 個 proposed binding 註冊進 executable registry
   （F-G1 終態是「exactly one **reviewed** fulfillment strategy」，⛔ 不是找到函式就啟用）
```

---

## 七、凍結的判準先例（散落各檔，此處為索引）

```text
P3      P-C1…C11 ＋ P-C1a          p3-verdicts.json._precedents
canonical  α-C1/C2、β-C1/C2、γ-C1…C6   registry-v2.json._*_rulers_frozen
C2      C2-NOMINATION-ADMISSIBILITY-1／P-F2-1a   r10-c2-v1-contract.md／f2-dossier
F 系列  F-C1…F-C13、F-G1…F-G5、T4-B-C1/C2/C3     f2-dossier.json._frozen_contracts_added
audit lessons  L1（evidence_detail 不是 capability census）／L2（同名欄位 ≠ 同一 authority）
```

> **三個 fulfillment 形狀已有 precedent**：R-29 direct FINAL_TEXT／R-28 direct GROUNDING_FACTS／
> R-31 composite。⇒ 後續遇到新函式形狀 ⛔ 不需要重新發明規則。
