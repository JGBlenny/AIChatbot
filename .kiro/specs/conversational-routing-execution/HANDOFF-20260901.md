# 交接：2026-09-01（可無上下文接手）

> ⚠️ 本檔是**現行**交接。舊檔 `HANDOFF-20260829/30/31.md` 保留為歷史，⛔ 已不反映主線。
> ⚠️ 逐筆事實以 `PLAN-retrieval-coverage.md`（v2）與 `r10p/fulfillment/f2-dossier.json` 為準。

分支 `fix/retrieval-routing-stability`｜**464 筆未 push**｜⛔ 從未 push｜repo 為 **PUBLIC**

---

## 0. 主線是什麼（⛔ 最容易搞錯的一件事）

```text
主線 spec   conversational-routing-execution（唯一）
現行計畫    .kiro/specs/conversational-routing-execution/PLAN-retrieval-coverage.md（**v2**）
業主目標    驗證架構 → 治「單一知識點覆蓋率不足」→ 擴測試情境
            → 大量回測驗證 → 補知識庫 → 上版

⛔ retrieval-decision-layer 已封存至 .kiro/specs/archive/，⛔ **不是主線**
   （但其 DecisionConfig 仍在生產運行，⛔ 勿刪；見該目錄 HALTED.md）
⛔ PLAN-mainline-r10.md 已刪除（以 R10 對話品質為主軸，非業主目標）
⛔ PLAN-r29-vertical-slice.md 已完成封存，責任架構停止擴張
```

## 1. 現在該做什麼（P0 起手）

```text
P0-0  裁決缺口呈核（**擋住其他任務**）              業主裁
      ① expected_answer 全 0 ⇒ 答案正確性只有 heuristic 尺，接受或排補
      ② 療效訊號門檻 —— 業主已裁「屆時再評估，取決於測試資料品質」
         參考下限：同 HEAD 重跑變異 13%（14/107）
P0-3  知識庫覆蓋率盤查（唯讀，可並行）              產出**適格母體**真分母
P0-1  量測 harness 擴充（**真前置，開發任務**）
P0-2  100 筆命中 ground truth 標註
P0-4  51 筆無 embedding（絕對檢索不到，優先消滅）
```

## 2. ⚠️ 環境殘留（接手第一件事先確認）

```text
容器 aichatbot-rag-orchestrator 目前：
  RESPONSIBILITY_TELEMETRY = true            每輪 inline 加 3–6 秒
  RESPONSIBILITY_MIGRATION_ALLOWLIST = R-29  **R-29 handoff 是開著的**
⇒ 若不打算續用，改 .env 兩個值為 false／空，再 `docker compose -f docker-compose.prod.yml
  up -d --force-recreate rag-orchestrator`
⚠️ 服務 recreate 後需等就緒（會先回 Connection reset），⛔ 別把它當成產品故障
```

## 3. 本 session 的關鍵事實（⛔ 別再重查）

```text
知識庫  active 922｜有 retrieval_representation **9（1%）**｜有 embedding 871
        51 筆無 embedding 且 keywords 亦全空 ⇒ **兩條檢索路都進不去**
        98 筆空 answer 錨點
測試    test_scenarios 4,875｜expected_category 3,533｜related_knowledge_ids **24**
        expected_answer **0**｜collection_id 全 NULL（⚠️ **與能否批次無關**）
        凍結語料 107 輪／26 輪有期望答案｜同 HEAD 重跑變異 **13%**
基準    60.2%(污染) → **69.7%**(regrade291-triage.md:3) → 75.2%(僅記憶檔)
        ⛔ **62.8% 不存在**，查無來源（記憶檔已修正）
        母體是 test_scenarios 548 題，**非 production holdout**
        最後回測 2026-07-06
管線    base_retriever.py:585 `0.1×vector + 0.9×rerank`
        ⚠️ **僅套用於實際進 rerank 的候選**（top-20／vector≥0.3）
        ⚠️ ENABLE_RERANKER **程式碼預設 false**（:47），compose 才是 true
```

## 4. ⛔ 已知錯誤文件（照它改碼會出事）

```text
docs/architecture/COMPLETE_CONVERSATION_ARCHITECTURE.md §3（行 432-434）
  ⚠️ **業態過濾漏掉 b2b 嚴格分支** —— b2b 是 `&& ['system_provider']` 且**無 IS NULL 放行**
     ⛔ 照文件補 IS NULL 會打穿刻意設計的隔離
  業者隔離漏「vendor_ids 空＝全業者共用」；運算子是 && 非 @>
  角色隔離漏 NULL 放行與 all_users
  :383 把仍在仲裁使用的 0.6 標成「已過時」（0.65 是檢索過濾門檻，不同命題）
  :440 行號漂移；主排序鍵是向量相似度，priority 只是次鍵
chat.py:2624  註解引用「既有 append_turn」——**全 repo 無此函式**（幽靈引用）
```

## 5. ⛔ 工具地雷

```text
⛔ scripts/regenerate_all_embeddings.py —— 全庫 legacy surface 重生，事後無法回推
⛔ backtest_framework_async.py:1304 —— 呼叫不存在的 load_test_scenarios，__main__ 一跑就炸
⛔ scripts/backtest/freeze_measurement.py —— 綁 corpus-20260810 母體，**凍不了 test_scenarios**
⚠️ scripts/run-tests.sh 會把 `-k "a or b"` 逐字拆開 ⇒ 跑子集請直接指定整個檔案
⚠️ migrate.sh --apply 會連帶跑 9 支無關 migration ＋ regenerate_all_embeddings（always 支）
   ⇒ 單支 migration 用單檔套用＋手動記帳
```

## 6. 硬 blocker（上版前必解）

```text
SEC-01  現行 OpenAI key 曾明文進 transcript，**尚未撤銷**
        ⚠️ 換 key 時點須與回測協調（中途換會整輪作廢）
SEC-02  現行 AWS Access Key ID 在 **public repo** 歷史（origin/main 祖先）
PUB-01  repo 為 PUBLIC；464 筆未 push 含全部 .kiro/specs 與稽核紀錄
        SECRET-SAFE ≠ PUBLICATION-SAFE
證據    evidence/sec-c1-audit.md（immutable）／evidence/sec-c2-ledger.md（活帳）
```

## 7. 本 session 的教訓（⛔ 別重蹈）

```text
主線認錯三次   曾誤把 retrieval-decision-layer 當主線；分支名 fix/retrieval-routing-stability
               但 49% 的 commit 進了責任架構
量測缺席       17 個 commit 對 R10 四指標貢獻 0/4；沒有基準就講不出改善什麼
數字失真       62.8% 這個「基準」是記憶索引行的錯字，被引用了整個 session
計畫錯前提     v1 把 collection 連結當批次前置（實際 runner 根本不讀），
               而真正的阻塞「量測 harness 不存在」完全沒寫
⇒ 每一刀開工前先問：**這會讓哪個數字動多少？動不了就不做。**
```
