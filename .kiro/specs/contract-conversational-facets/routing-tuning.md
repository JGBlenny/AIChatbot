# 進場路由調校紀錄（2026-07-03，Run 288 誤進場收斂）

> 驗證 harness：`tests/integration/conversational/test_facet_entry_routing_req.py`（24 案全綠）
> 重演鏈路＝真檢索（含 reranker）→ 0.75 門檻 → categories → config_for_category，零 LLM。

## 調校內容（全資料側，零程式）

| # | 動作 | 原因 |
|---|---|---|
| A | **3388 到期提醒 untag `續約`**（backfill migration 已同步移除） | 制度 yes/no 題被掛面向後，「系統會自動提醒嗎」(0.965) 進對話反問；模糊句「快到期怎麼辦」由錨點 (0.819) 覆蓋 |
| B | **新知識**「租客同意點交後 下一步 水電基準」（單發） | 缺口：點交完成後下一步教學不存在，錨點「接下來怎麼做」(0.916) 成最近鄰吸走問句 |
| C | **3402 措辭重嵌**：「點退帳單 費用結算」→「點退帳單 自動產生 費用結算」 | 「會自動出帳單嗎」被退租收尾封存知識 (0.919) 吸走，正解缺「自動產生」語彙輸排序 |
| D | **3530 措辭重嵌**：「批次續約多筆合約 快速日期設定」→「批次續約 多筆合約一起續 快速日期設定」 | 「批次續約要怎麼做」與續約錨點 0.925 vs 0.899 近身戰，補使用者語彙反超 |

## 保護清單（調校不得傷及，已入測試 DIALOG_CASES）

12 錨點口語＋e2e 實跑進場句「合約快到期了要怎麼續約」＋既有狀態判斷保證句——調校後全數仍進對話。

## 部署重放

本機已套用。**prod 部署時**（6.3 知識匯入步驟之後）執行：
`python3 tune_routing.py`（本目錄，需 EMBEDDING_API_URL 與 DB env；冪等）
——backfill migration 已含 A；B/C/D 由此腳本重放。
