# 效能落檔：documind-ocr-mapping（需求 9.4）

> 量測日 2026-09-03｜方式：**進程內** TestClient（最小 FastAPI app 只掛本 router，無 DB、無 middleware）
> ⚠️ 不含網路與 `usage_events` 寫入；正式容器尚未 rebuild 進本 router，HTTP 層數字待 10.1 真實 e2e 時補。

## 純規則路徑（R8 未啟用）

```
payload   合約 fixture 擴成 20 頁（重複第 2、3 頁），16 KB
暖機 1 次後連打 20 次
p50   3.8 ms
p95   6.4 ms      ← 預算 2 s（需求 9.4），餘裕約 300 倍
max  11.0 ms
```

可重跑：
```bash
docker compose -f docker-compose.dev.yml run --rm -T rag-orchestrator python - <<'PY'
# 見 tests/unit/ocr_mapping/test_nfr_req.py::_twenty_page_payload；同一 payload 打 20 次取 p95
PY
```

## HTTP 層（正式容器 :8100，2026-09-03 12:43 rebuild 後）

```
同一 20 頁 payload，經 docker network＋api_key middleware＋端點內計量，暖機 1 次後連打 20 次
p50   5.6 ms
p95  23.5 ms      ← 預算 2 s，餘裕約 85 倍
max  25.8 ms
```
真實請求同時驗：200 draft（17 欄、rent 13800、date_start 20250121、deposit_amount 27600）、400 DOCUMENT_TYPE_MISMATCH、400 UNSUPPORTED_DOCUMENT_TYPE。

## R8 路徑（⛔ 作廢）

D1 於 2026-09-04 定案 A，`second_pass_extractor` 不實作；本端點無 LLM 路徑，只剩純規則路徑一條。

## 守門

`tests/unit/ocr_mapping/test_nfr_req.py` 以 p95 < 500 ms 守「不得退化到預算 1/4」，⛔ 不當基準用。
