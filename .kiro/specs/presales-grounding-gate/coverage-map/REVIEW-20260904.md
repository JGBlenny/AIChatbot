# 缺口地圖 v1 獨立驗證與處置（2026-09-04）

業主「幫我派代理驗證缺口地圖」⇒ 兩個角度並派：verifier（結論對不對）與 plan-verifier（判準憑什麼）。兩份發現不重疊、互相印證；v1 格數**作廢不得引用**，v2 依全部處置重做。

## plan-verifier：REVISE（1 P0／6 P1／5 P2／1 P3）→ 處置

| # | 發現 | 處置 | v2 落點 |
|---|---|---|---|
| P0 | 「已覆蓋」用被驗系統自己的檢索分數判＋作者本人人工覆核，違反 PLAN P2.5 ⛔ 不做；C01 與 C13 同為反問未答卻一判覆蓋一判缺口 | **FIX** | 每格 `rubric.must`（必含群）指向 kb 列或幫助中心逐字；coverage 只在 cause=S_OK ∧ entry 有答 ∧ 回答命中全部必含群才成立；無 rubric ⇒「未判定(待尺)」。intent=recommend 的反問算答（C01），fact 題反問不算（C13） |
| P1 | 缺 V（可見性）：關鍵詞只掃 prospect 池，池外正解被誤判缺口去補知識（C07） | **FIX** | `KW_SQL` 掃全庫並回 `visible` 欄；對題列只在池外 ⇒ cause=V（改資料歸屬，⛔ 非補知識）。v2 實測 7 格 V |
| P1 | 「表示法缺」任一關鍵詞命中任一列即算，N 被誤判 S（通知→租客不繳、專人→尾句） | **FIX** | 命中列須命中 rubric 必含群（多群 ≥2）；v2.1 再收緊 7 格泛詞 |
| P1 | 缺口是 catch-all 恆真出口，G0 未做就報總數 | **FIX** | 末端改 `UNCLASSIFIED(待G0)`；有權威來源者 `N_CAND`；摘要表不再有「缺口」總數，PLAN 引用同步改口 |
| P1 | 使用者層只比固定句字串、不看 handoff、常數硬抄 | **FIX** | `entry_state` 以 `handoff.reason∈{no_grounding,sensitive_no_grounding}` 為主判；固定句 import `PRESALES_HANDOFF_MESSAGE` 並落 sha；開跑以刻意不補格做固定句正對照 |
| P1 | 一格一句單輪，五格離門檻 <0.08 | **FIX（部分）／DEFER** | 每格量全部問句，cause 不一致 ⇒ `UNSTABLE[...]`（v2 實測 4 格）。每句 ≥3 輪 **DEFER 至 v3**：成本（55 格×多句×3 輪 ≈ 1 小時單機），且療效不在地圖量、在劇本 e2e 量 |
| P1 | 無 G2 管線自證、無環境指紋 | **FIX** | 跑前 `scripts/g2_selftest.py` 六項（本輪全過）寫入 `_meta.g2`；`_meta.fingerprint`＝HEAD／image／池筆數／池 max(updated_at)／demand sha256／固定句 sha；render 表頭顯示，缺 G2 即警語 |
| P2 | 元件層與使用者層 AND 成單一字串 | **FIX** | 拆 `cause_state`／`entry_state`／`coverage` 三欄，摘要表分層統計 |
| P2 | 「有知識被擋」無否決軌跡就斷言 G | **FIX** | 該狀態移除；分數過門檻但入口固定句的情形落在 cause=S_OK ∧ entry=fixed，coverage=未覆蓋，⛔ 不命名為閘門問題 |
| P2 | 需求側無 selection rule、無排除清單、無 sha256；分群後驗 | **FIX（文件）** | `_meta.selection_rule`（SQL＋22 條排除 id 與理由＋其他來源）；demand sha256 入指紋；明寫「後驗人工建構，只作盤點，⛔ 不作比率分母」 |
| P2 | 刻意不補由實作方自填、查無裁決 | **FIX** | DECISIONS **DSP-009**（引 requirements 已拍板決策 #2／#3）；五格 `policy_ref`；無引用者 ⇒ `UNCLASSIFIED(刻意不補無裁決引用)` |
| P2 | 重跑漂移無防護；v1 表手改不可重現 | **FIX** | 指紋如上；狀態集合封閉、render 與 JSON 同源；`--only/--merge-into` 局部重跑仍保留其餘格 |
| P3 | 供給側 31 筆無查證指令、`ts:` 語義、錯字 | **FIX** | 池 SQL 寫進工具 `POOL_PRED`（實查 32 筆）；`ts:`＝test_scenarios.id；錯字修正 |

## verifier：REFUTED → 處置

| 發現 | 處置 | v2 落點 |
|---|---|---|
| A 需求側漏 4 主題（系統管理模組 ×3、單／雙合約、發票載具、錯誤訊息解讀）；8182／8184 未列 C01 sources | **FIX** | 新增 C52–C55；C01 sources 加 ts 8182,8184 與問句「系統有哪些功能」 |
| B 檢索層 21/21 重現；10 格人工改判全部成立 | — | 證明 v1 的資料層可信，壞的是判準 |
| C 假命中漏抓 C31（水電分算→訂閱費 5376）、C24（「不需一張一張」越權） | **FIX** | C31 intent=advice ⇒ cause=ADVICE、coverage=n/a；C24 `rubric.forbid` 含「一張一張」⇒ v2 判「已覆蓋⚠️」 |
| D 刻意不補邊界乾淨；「有知識被擋 0」是依門檻定義非供給事實（C41） | **FIX** | 見 P2 第二列；C41 在 v2 為 cause=S（5378 有 Email＋LINE 對題）entry=fixed |
| E 關鍵詞高估：C08／C15／C44 假命中、C33／C47 borderline | **FIX** | 對題性 ≥2 群＋v2.1 收緊；C44 在 v2 為 V（3990 等池外註冊列）；C47 intent=cta ⇒ cause=CTA |
| P3 池 32 筆非 31；改判格 10 非 7 | **FIX** | 工具實查池筆數入指紋；本檔如實記 10 格 |

## v1 → v2 的定性變化
- v1 對外報「已覆蓋 21／缺口 13」⇒ **作廢**。v2 分層：元件層 S_OK／S／V／FALSE_HIT／N_CAND／UNCLASSIFIED／UNSTABLE，使用者層 fixed／answered／answered_handoff／ask_back，合成 coverage 只在兩層都成立才給。
- 新出現的類別 V（7 格）是 v1 完全看不見的：知識存在於 property_manager 池或系統脈絡列，prospect 檢索不到——修法是資料歸屬或另立 prospect 列，⛔ 不是重寫知識。
