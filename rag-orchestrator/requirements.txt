fastapi==0.115.14
starlette>=0.46,<1.0
uvicorn>=0.31.1
pydantic>=2.13,<3
pydantic-settings>=2.6,<3
# 任務 1.7b 實跑逼出（主 session 的 dry-run 未含此件）：mcp==2.1.1 要求
# python-multipart>=0.0.9，==0.0.6 會讓 pip 解析失敗（ResolutionImpossible）。
python-multipart>=0.0.9
anyio>=4.9

# Database
asyncpg==0.29.0
psycopg2-binary==2.9.9

# Cache
redis==5.0.1

# OpenAI
openai==1.54.0
httpx==0.27.0

# HTTP & Async
aiohttp==3.9.1
requests==2.31.0

# Progress & Retry
tqdm==4.66.1
tenacity==8.2.3

# YAML
pyyaml==6.0.1

# Utilities
python-dotenv==1.0.0

# Data Processing
numpy==1.24.3
pandas==2.0.3
openpyxl==3.1.2
xlsxwriter==3.1.9
python-docx==1.1.0

# HTML Parsing
beautifulsoup4==4.12.3
lxml==5.1.0

# Chinese text processing
pypinyin==0.51.0
jieba==0.42.1

# AWS S3 (影片儲存 + 圖片儲存)
boto3==1.34.0

# Image Processing (圖片壓縮)
Pillow>=10.0.0

# Reranker：透過 semantic-model 容器（HTTP API）呼叫，本服務不需要 sentence-transformers/torch

# ── MCP SDK（spec agentic-mcp-orchestration 任務 1.7 → 1.7b）─────────────────
# 業主 2026-09-04 裁 DSP-014 選項 A：升級 web stack 承載 mcp 2.1.1（見
# .claude/DECISIONS.md DSP-014，以及上方 fastapi/starlette/pydantic/anyio 已升版）。
# 主 session 已用 `pip install --dry-run` 驗證此組合可解（可重跑，於裝有本檔
# 依賴的環境）：
#   pip install --dry-run 'fastapi==0.115.14' 'starlette>=0.46,<1.0' \
#     'pydantic>=2.13,<3' 'anyio>=4.9' 'uvicorn>=0.31.1' 'mcp==2.1.1'
#   → 解得 fastapi-0.115.14 starlette-0.46.2 pydantic-2.13.5 pydantic_core-2.46.5
#     anyio-4.15.0 uvicorn-0.52.4 mcp-2.1.1 httpx-0.27.0 openai-1.54.0
mcp==2.1.1
