fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0
python-multipart==0.0.6

# Database
asyncpg==0.29.0
psycopg2-binary==2.9.9

# Cache
redis==5.0.1

# OpenAI
openai==1.54.0
httpx==0.27.0

# HTTP & Async
aiohttp==3.9.1
requests==2.31.0

# Progress & Retry
tqdm==4.66.1
tenacity==8.2.3

# YAML
pyyaml==6.0.1

# Utilities
python-dotenv==1.0.0

# Data Processing
numpy==1.24.3
pandas==2.0.3
openpyxl==3.1.2
xlsxwriter==3.1.9
python-docx==1.1.0

# HTML Parsing
beautifulsoup4==4.12.3
lxml==5.1.0

# Chinese text processing
pypinyin==0.51.0
jieba==0.42.1

# AWS S3 (影片儲存 + 圖片儲存)
boto3==1.34.0

# Image Processing (圖片壓縮)
Pillow>=10.0.0

# Reranker：透過 semantic-model 容器（HTTP API）呼叫，本服務不需要 sentence-transformers/torch

# ── MCP SDK（spec agentic-mcp-orchestration 任務 1.7）────────────────────────
# 🔴 **待業主裁決，⛔ 現在不得取消註解**。2026-09-04 實查（可重跑）：
#   docker run --rm aichatbot-rag-orchestrator-test:latest \
#     sh -c "pip install --dry-run 'fastapi==0.104.1' 'mcp==2.1.1'"
#   → ERROR: ResolutionImpossible
#       fastapi 0.104.1 depends on anyio<4.0.0 and >=3.7.1
#       mcp 2.1.1     depends on anyio>=4.9; python_version < "3.14"
#   正對照組：同一支指令**不加 mcp** 時解析成功（fastapi 已滿足）。
#
# 硬裝（獨立 image 實測）會把 starlette 0.27.0→1.6.0、pydantic 2.5.0→2.13.5、
# anyio 3.7.1→4.15.0 一併升上去，`import app` 當場炸：
#   TypeError: Router.__init__() got an unexpected keyword argument 'on_startup'
#   （routers/chat.py 的 APIRouter()；starlette 1.x 已移除 on_startup）
#
# ⇒ 掛上 /mcp 工具面必須先升 fastapi（連帶 starlette／anyio／pydantic），
#   那是跨全 app 的相依升級，**不在任務 1.7 的核可範圍**，交業主裁決。
#   裁決前：services/agent/mcp_facade.py 的三道請求層閘（401／403／400）與
#   額度落點照常運作且已測；MCP 工具面只在 SDK 可匯入時掛載
#   （mcp_facade.mcp_sdk_available()），未裝時 /mcp 過閘後為 404。
# mcp==2.1.1
