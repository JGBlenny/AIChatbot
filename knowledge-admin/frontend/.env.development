# 開發環境配置
# RAG Orchestrator API Base URL
# 開發環境直接訪問宿主機上的 Docker 端口映射
VITE_RAG_API_BASE_URL=http://localhost:8100

# RAG API v1 URL (用於 VendorChatDemo 等頁面)
VITE_RAG_API_URL=http://localhost:8100/api/v1
