# 部署紀錄 — 2026-07-29

- **推到 commit**：`5c59a27`（`fix(db): embed_missing 改用「包含」判定…`）
- **起點 commit**：`6607031`（prod 部署前）
- **範圍**：6607031 → 5c59a27（18 commit backlog ＋ 本次工作：runner／embed_missing／文件收斂）
- **正統文件**：`docs/deployment-runbook.md`（§0 前置／§17 runner／§4 重建／§5 煙囪／§10 audit／§16 登入修復）

## 這次上了什麼

| 修正 | 內容 | 狀態 |
|---|---|---|
| 空白 500（NameError/串流查無） | `_finalize_response` req 作用域（a4421d54） | 碼已上；✅ **正面驗證 HTTP=200（2026-07-29）** |
| 後台「登入即被踢」（§16） | 發 `RAG_ADMIN_API_KEY`＋admin-web nginx 注入 | ✅ **登入已驗成功** |
| 進場路由調校 | keyword 衛生＋4 口語錨點 | migration＋錨點已嵌 |
| 對話式修繕面向 | 引擎交易語義＋修繕面向 config＋知識 seed | 已上 |
| brain-kb-grounding | 岔題掛 search_kb＋usage_events.search_kb_status | 已上 |
| 觸發語彙還債 P0／emergency_status 語義／計量分數欄 | — | 已上 |
| migration 帳本收斂 | 帳本感知 runner `migrate.sh`＋`seeds.manifest` | ✅ 已用＋記帳 |

## 執行摘要（prod）

- **碼**：`git pull` 到 5c59a27 → `docker compose up -d --build --no-deps rag-orchestrator semantic-model embedding-api`
- **帳本 bootstrap**：`20260722_schema_migrations_ledger.sql`（回填 facet 大批 37 支，帳本共 76→77）
- **migration（runner）**：6 支非破壞性套用並記帳（disable_repair_sop_vendor24／usage_events_facet_columns／search_kb_status／routing_keyword_hygiene／add_usage_events_scores／seed_repair_facet_config）
- **legacy 例外**：`create_digression_config` 未跑、僅記帳（prod 已有 10 筆、非冪等勿重跑）
- **seed**：`seed_repair_facet_knowledge`（4 錨點＋1 查進度，容器內跑、記帳）
- **補嵌**：`embed_missing.py` 補 4 筆 routing 錨點 embedding（已排除系統脈絡/對話規則）
- **§16 登入**：發金鑰入 api_keys＋寫 .env（`RAG_ADMIN_API_KEY`）→ 重建後台前端＋重開 admin-web → 登入正常

## 尚未收（待辦）

- [x] **破壞性 M2**：`drop_knowledge_trigger_dead_columns` ✅ 已刪＋記帳（2026-07-29）
- [x] **`make audit`**：六不變量無 FAIL ✅（M2 前後皆綠，2026-07-29）
- [x] **空白 500 正面驗證**：帶金鑰打查無題 `HTTP=200` ✅（2026-07-29）
- [ ] §5 煙囪 20 面向功能驗（修繕/合約/帳單面向）
- [ ] 文件：runbook 4 個個別驗證 curl 逐條補金鑰（已有 §0 總說明兜著）

## 踩過的坑（供下次參考）

- **seed 批不能在主機跑**：主機無 asyncpg，須在 orchestrator 容器內（`docker exec`）。runner seed 批待改 in-container。
- **manifest 誤用工具**：`regenerate_all_embeddings`（全庫重嵌＋互動確認）非缺嵌補算，已改用 `embed_missing`。
- **驗證 curl 撞認證**：`RAG_API_AUTH_ENFORCE` 開啟後，直接打 rag 的驗證要帶 `X-API-Key`（runbook §0 已補總說明）。

## 收尾（2026-07-29）

**三核心目標全驗證通過（空白 HTTP=200／登入成功／audit 無 FAIL）＋ 破壞性 M2 完成。部署正式結束。**
剩餘非阻塞：§5 煙囪 20 面向功能驗（可另跑）、不變量4 修繕報修無系統脈絡（WARN，待確認是否輕引導型免脈絡）、runbook 4 個個別 curl 補金鑰、doc commits push。
