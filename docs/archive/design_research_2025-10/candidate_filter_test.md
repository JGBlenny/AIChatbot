# 審核中心候選知識篩選功能測試

## 功能說明
審核中心的「知識庫審核」tab 現已支援根據 URL 參數過濾顯示特定的候選知識。

## 支援的 URL 格式

### 1. 單個候選知識
```
http://localhost:8087/review-center?candidate_id=84
```
- ✅ 只顯示候選 #84
- ✅ 自動滾動到該候選並高亮
- ✅ 顯示篩選橫幅：「正在顯示 1 個指定的候選知識」

### 2. 多個候選知識（逗號分隔）
```
http://localhost:8087/review-center?candidate_id=84,85,83
```
- ✅ 只顯示候選 #84, #85, #83
- ✅ 自動滾動到第一個候選（#84）並高亮
- ✅ 顯示篩選橫幅：「正在顯示 3 個指定的候選知識」

### 3. 無參數（查看全部）
```
http://localhost:8087/review-center
```
- ✅ 顯示所有待審核的候選知識
- ✅ 不顯示篩選橫幅

## UI 元素

### 篩選模式橫幅
當使用 `?candidate_id` 參數時，頂部會顯示紫色漸變橫幅：

```
┌─────────────────────────────────────────────────────────────┐
│ 🔍 正在顯示 2 個指定的候選知識    [查看全部 (18)]  │
└─────────────────────────────────────────────────────────────┘
```

**特性**:
- 紫色漸變背景（#667eea → #764ba2）
- 顯示當前過濾的候選數量
- 「查看全部」按鈕顯示總候選數
- 點擊按鈕返回無篩選狀態

### 候選列表
- **篩選模式**: 只顯示指定 ID 的候選
- **正常模式**: 顯示所有待審核候選
- **空狀態處理**:
  - 找不到指定候選時顯示「找不到指定的候選知識」
  - 提供「查看全部候選」按鈕

## 測試用例

### 測試 1: 單個候選篩選
**步驟**:
1. 訪問測試情境頁面: http://localhost:8087/test-scenarios
2. 找到場景 158「借址營登費用及服務內容」
3. 展開測試詳情
4. 點擊「前往審核頁面」

**預期結果**:
- URL: `http://localhost:8087/review-center?candidate_id=84`
- 自動切換到「知識庫審核」tab
- 顯示篩選橫幅：「正在顯示 1 個指定的候選知識」
- 只顯示候選 #84
- 候選 #84 被高亮顯示（紫色邊框 + 脈衝動畫）
- 3 秒後高亮消失

### 測試 2: 多個候選篩選
**步驟**:
1. 直接訪問: `http://localhost:8087/review-center?candidate_id=84,85,83`

**預期結果**:
- 自動切換到「知識庫審核」tab
- 顯示篩選橫幅：「正在顯示 3 個指定的候選知識」
- 只顯示 3 個候選：#84, #85, #83
- 候選 #84 被高亮（第一個）
- 「查看全部」按鈕顯示總數（例如：查看全部 (18)）

### 測試 3: 查看全部功能
**步驟**:
1. 訪問篩選 URL: `http://localhost:8087/review-center?candidate_id=84`
2. 點擊篩選橫幅中的「查看全部」按鈕

**預期結果**:
- URL 變為: `http://localhost:8087/review-center`
- 篩選橫幅消失
- 顯示所有待審核的候選知識（18 個）

### 測試 4: 不存在的候選 ID
**步驟**:
1. 訪問: `http://localhost:8087/review-center?candidate_id=999`

**預期結果**:
- 自動切換到「知識庫審核」tab
- 顯示空狀態：「🔍 找不到指定的候選知識」
- 提示：「候選可能已被審核或不存在」
- 顯示「查看全部候選」按鈕

### 測試 5: 混合存在與不存在的 ID
**步驟**:
1. 訪問: `http://localhost:8087/review-center?candidate_id=84,999,85`

**預期結果**:
- 自動切換到「知識庫審核」tab
- 顯示篩選橫幅：「正在顯示 2 個指定的候選知識」
- 只顯示存在的候選：#84, #85
- 不存在的 ID (#999) 被忽略

## 技術實現

### 前端邏輯
**文件**: `KnowledgeReviewTab.vue`

1. **Computed 屏性**:
```javascript
filteredCandidates() {
  if (!this.candidateId) {
    return this.aiCandidates;  // 返回全部
  }
  // 解析逗號分隔的 ID
  const candidateIds = String(this.candidateId)
    .split(',')
    .map(id => parseInt(id.trim()));

  // 過濾候選
  return this.aiCandidates.filter(
    candidate => candidateIds.includes(candidate.id)
  );
}
```

2. **篩選模式檢測**:
```javascript
isFilterMode() {
  return this.candidateId !== null && this.candidateId !== undefined;
}
```

3. **清除篩選**:
```javascript
clearFilter() {
  this.$router.push('/review-center');
}
```

### 樣式特性
- 紫色漸變橫幅
- 滑動動畫進場
- 半透明按鈕效果（backdrop-filter）
- Hover 狀態變化

## 與知識庫頁面的對比

| 特性 | 知識庫頁面 | 審核中心 |
|------|----------|---------|
| URL 參數 | `?ids=498,499` | `?candidate_id=84,85` |
| 參數名稱 | `ids` | `candidate_id` |
| 過濾邏輯 | 客戶端過濾 | 客戶端過濾 |
| 篩選提示 | ✅ | ✅ |
| 查看全部按鈕 | ✅ | ✅ |
| 自動滾動 | ✅ | ✅ |
| 高亮效果 | ✅ | ✅ |

## 數據流

```
URL 參數
  ↓
ReviewCenterView (父組件)
  ↓ props: candidateId
KnowledgeReviewTab (子組件)
  ↓ computed: filteredCandidates
模板渲染 (v-for="candidate in filteredCandidates")
```

## 已知限制

1. **客戶端過濾**: 仍然會載入所有待審核候選，然後在前端過濾
   - 優點：不需要修改後端 API
   - 缺點：如果候選數量很大（1000+），性能可能受影響

2. **只支援待審核狀態**: 目前只能過濾 `pending_review` 狀態的候選
   - 如需過濾已批准/已拒絕的候選，需要切換 API 端點

3. **URL 狀態不同步**: 點擊「查看全部」後，如果手動刷新頁面，會回到篩選模式
   - 原因：父組件的 candidateId props 仍然存在

## 未來改進建議

1. **後端支援**: 添加 API 參數 `?ids=84,85` 在伺服器端過濾
2. **狀態過濾**: 支援 `?candidate_id=84&status=approved`
3. **URL 同步**: 使用 `$router.replace()` 而不是 props 傳遞

## 完成狀態

- ✅ 單個 ID 過濾
- ✅ 多個 ID 過濾
- ✅ 篩選橫幅顯示
- ✅ 查看全部按鈕
- ✅ 空狀態處理
- ✅ 自動滾動和高亮
- ✅ 樣式和動畫
