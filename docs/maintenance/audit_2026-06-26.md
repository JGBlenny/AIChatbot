# 文件健全度稽核 — 2026-06-26

> 範圍：全 repo 文件(docs/ 約 129 份 + 根目錄 + .kiro/steering + database/)與現行程式碼的一致性盤查。
> 方法：4 個面向平行盤查(近期改動過時 / intent 殘留 / 多餘重複 / 索引與壞連結),逐項對照程式碼。

## 摘要

- **近期改動文件未跟上**:categories 多值化、兩層分類(parent_value)、API Key 認證 → 已更新 5 份核心文件。
- **大量歷史/規劃文件**:功能已實作的草案、帶日期報告 → 封存 29 份至 `docs/archive/2026-06/`。
- **索引與根目錄嚴重壞連結**:`docs/archive/`(舊)先前被刪但索引沒同步 → 修復 4 份索引/根文件。
- **校正前次假設**:2026-04-22 稽核稱「intent 機制已移除」,但本分支程式碼中 `intent_boost`/`knowledge_intent_mapping`/`intent_classifier` **仍在用**(僅 /intents、suggested_intents API 端點未掛載=死端點)。故提及 intent 的文件**多為有效引用,未刪**。

## 已處理

### A. 更新(因近期改動而過時)
| 文件 | 更新內容 |
|------|---------|
| `architecture/DATABASE_SCHEMA.md` | 補 knowledge_base `categories TEXT[]` + GIN 索引 + chk_categories_no_reserved;category 標退役;category_config 補 parent_value;新增 api_keys 表定義 |
| `api/API_REFERENCE_KNOWLEDGE_ADMIN.md` | category→categories;category-config 回應補 parent_value;新增 /api/api-keys 全套 + category-config 寫入端點 + remove-from-knowledge |
| `architecture/SYSTEM_ARCHITECTURE.md` | knowledge_base 欄位更新;新增服務對服務 API Key 認證層 + api_keys |
| `API_ENDPOINTS_COMPLETE_INVENTORY.md` | 補 /api/api-keys、/api/category-config 全套 |
| `guides/deployment/ENVIRONMENT_VARIABLES.md` | 新增 `RAG_API_AUTH_ENFORCE` |

### B. 封存(多餘/歷史)→ `docs/archive/2026-06/`
- form-filling(5):FORM_FILLING 規劃/草案(保留 DIALOG_DESIGN)
- knowledge-action(3):KNOWLEDGE_ACTION 實作筆記(保留 SYSTEM_DESIGN、QUICK_REFERENCE)
- sop(9):2026-01-22 設計快照 + 歷史實作/測試報告(保留 README、INDEX、KEYWORDS_COMPARISON、FLOW_STRICT_VALIDATION、VENDOR_SOP_FLOW_CONFIGURATION)
- frontend(4):過期開發筆記 requirements/todo/implementation-summary/insertion-guide
- maintenance-2026-04-22(7):前次一次性稽核報告
- UPDATE_NOTES.md(統計與現況脫節)
→ 共 29 份。docs 活躍 129→100。

### C. 修索引/根目錄壞連結
| 文件 | 處理 |
|------|------|
| `README.md`(根) | 重寫(原停 2025-10-28、~29 壞連結、主打已下架意圖系統)→ 符合現況、連結全驗證存在 |
| `CHANGELOG.md` | 36 個指向已刪檔的連結中性化(保留條目文字)|
| `docs/README.md` | 重寫為簡潔導覽(原 11 個純文字引用全為已刪檔)|
| `docs/INDEX.md` | 移除過時 archive 結構描述,指向現存 archive/ |

## 未處理(待決策)

- **intent 機制**:文件提及 `intent_boost`/`knowledge_intent_mapping`/`intent_classifier` 多為有效引用(程式仍在用)。若要清,**先決定程式是否移除這些機制**,再更新文件。死端點 `/api/v1/intents`、`suggested_intents`(router 存在但未掛載)可於 SYSTEM_ARCHITECTURE、README 標注停用。
- **待人工確認狀態**:`docs/frontend/` 3 份 2026-03-27 需求文件、`docs/design/IMPROVED_API_ARCHITECTURE.md`(若架構已落地可封存)。
- **API Key / 兩層分類 feature 文件**:目前只在 schema/api 參考有記;如需獨立 feature 說明可另建。
