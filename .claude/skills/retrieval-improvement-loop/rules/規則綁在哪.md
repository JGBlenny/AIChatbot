# 規則綁在哪：每條規則管到哪些具體產出

> 流程全圖、鐵律與閘門見 [`../SKILL.md`](../SKILL.md)。**⛔ 本檔只放這一塊。**
>
> ⚠️ 源於業主 2026-09-02「這是不是代表 skill 不夠清楚」。
> ⚠️ **初版被獨立審查回 REVISE ×7**，其中一條是：本檔自證用的兩條測試路徑**當天就是錯的**
> （少了 `rag-orchestrator/` 前綴）——⛔ **綁定表在寫出當天就腐爛了一處**。本版已修並補防腐節。

## 鐵律

> **規則寫對了，但沒說它管到哪個檔案 ⇒ 那條規則等於不存在。**
> ⚠️ **但這條只治一種病**（見下方真因分類）——⛔ 不是所有失敗都能靠綁定表解決。

---

## 三次失敗的**真因分類**（⚠️ 初版把三種病誤治成一種）

| 失敗 | 真因類別 | 本檔治得了嗎 |
|---|---|---|
| `status.py` 對 api 型面向印「⚠️ 知識 2」，而 [`型別分批.md`](型別分批.md) 早就寫明 T2 正解＝API 回傳 | **規則對，但無綁定點** | ✅ 這一類 |
| 一天四個母體（772／381／320／293） | **規則當天才誕生，且初版自己就把 293／264 誤標一次** | ⛔ 治不了——規則當時不存在 |
| [`正解判定.md`](正解判定.md) 沒說只適用 T1 | **規則寫漏了射程**（業主當場指出才補） | ⛔ 治不了——那是規則本身缺一段 |

⇒ **只有第一類是綁定表能治的。** ⛔ 不得拿本檔當「所有規則失效」的解方。

⚠️ 共同點仍成立且值得記：**規則與違規的程式住兩個地方、分開讀都看起來對**
——三條全是業主發現的，⛔ 不是實作方。

---

## 綁定表（⚠️ 實查得出；新增實作點必須同步本表）

### ① 母體定義（正本：`scripts/status.py` 的 `_B2B`／`ELIGIBLE`）

```text
正本      scripts/status.py                    符號 _B2B（答題母體）／ELIGIBLE（全庫適格）
✅ 已同源  scripts/audit/kb_vs_helpcenter.py    符號 B2B_VISIBLE
          scripts/audit/kb_config_census.py    符號 ELIGIBLE
⚠️ 刻意不同 rag-orchestrator/scripts/backtest/contract_enrich.py  符號 visibility
          模型兩條檢索路（向量／詞面），⛔ 不得改成用 _B2B
❓ 待裁    scripts/analysis/p1b_applicability_census.py
          scripts/audit/checks/instance_applicability_contract.py
          ⚠️ **初版把它們標成「⛔ 未同源缺陷」——那是未經證成的**：
            它們稽核的是「誰需要適用性契約」，而空 answer 錨點正是**面向進場錨點**；
            `instance_applicability_contract` 檔頭自陳「本不變量不要求 coverage，
            population 是 P1b」⇒ 母體語義**可能是刻意分工**。
          ⛔ **實害警告**：`scripts/analysis/p1e_corpus.py` 的母體**繼承自 P1b census**
            ⇒ 若照初版待辦把 p1b「改成同源」，會把面向錨點靜默移出稽核母體，
            並讓一份**已凍結的標註 corpus** 變動。
          ⇒ 待裁一句話：**適用性稽核是否應涵蓋空 answer 面向錨點？**
⚠️ 未列    scripts/audit/check_invariants.sh 內含 9 處直寫 `is_active AND …` 的 where
          scripts/audit/checks/level_a_representation_completeness.py  符號 LEVEL_A_ROWS（凍結 10 筆）
          scripts/audit/checks/entry_alias_contract.py／retired_row_isolation.py
          scripts/check_embedding_coverage.py
          ⇒ 各不變量本來就有自己的母體，⛔ 不必與 ELIGIBLE 同源；此處僅登錄存在
其他      rag-orchestrator/scripts/routing/run_p1_corpus_sibling.py  符號 exposure_surface
          （母體 146，且**已寫進凍結協定當判準**）
          scripts/g2_selftest.py  符號 ORACLE_Q（註解自陳刻意不複製生產過濾）
          scripts/knowledge-batches/tune_routing.py（匯入腳本，寫死 business_types，非查詢母體）
```

### ② 四型分批（正本：[`型別分批.md`](型別分批.md) 的「型別判定（機械）」節）

```text
scripts/status.py                    符號 print_b2b_surface
  ⛔ api 型面向印「入口」⛔ 不印「覆蓋」——答案來自 API
     （`conversational_engine._ground_by_api` 以 API 回傳為合成底稿；
       spec conversational-diagnosis 核心元件 1）
  ⛔ 空 answer 錨點不計覆蓋（業主 2026-09-02 定案）
scripts/audit/kb_vs_helpcenter.py    符號 _TYPE_SQL
  ⛔ 型別是**標記**不是閘門——事實對帳跑全型
  ⚠️ 已知歧異：多面向取法與 status.py 不同（api 優先 vs max(f.sel)），尚未收斂
scripts/audit/checks/face_preemption_capability.py
  用 grounding_scope.select=='api' 判能力，屬不變量 9，⛔ 非本迴圈射程
```

### ③ 正解判定（正本：[`正解判定.md`](正解判定.md)）

```text
⛔ **只適用 T1**（正解＝某一筆 knowledge_base row）
   T2 正解＝API 回傳｜T3 正解＝逐輪動作｜表單型＝欄位契約
⚠️ 但「內容有沒有講錯事實」**型別無關** ⇒ kb_vs_helpcenter 跑全型
rag-orchestrator/scripts/backtest/nominate_ground_truth.py  提名候選（獨立 oracle）
```

### ④ 量測層級（正本：[`量測層級.md`](量測層級.md)）

⚠️ **初版只列一支 `run_batch.py` —— 實際有六個入口，且多數現行違規。**

```text
✅ 合規  rag-orchestrator/scripts/backtest/run_batch.py
         逐題序列＋符號 _REQUIRED_PREFIX = "backtest_session_"

⛔ 違規（現行）
   scripts/backtest/backtest_framework_async.py            符號 run_backtest_concurrent
   rag-orchestrator/scripts/backtest/backtest_framework_async.py（同名副本）
     ⇒ asyncio.Semaphore + gather **併發**；session 前綴 `backtest_` 而非 `backtest_session_`
   scripts/backtest/run_backtest_with_db_progress.py       符號 BACKTEST_CONCURRENCY（預設 5）
   rag-orchestrator/scripts/backtest/run_backtest_with_db_progress.py（同）
   knowledge-admin/backend/app.py
     ⇒ **後台「執行回測」按鈕**設 BACKTEST_CONCURRENCY=5 並啟動上面那支 ⇒ 一般使用者可觸發
   rag-orchestrator/services/knowledge_completion_loop/backtest_client.py／run_first_loop.py
     ⇒ **生產服務碼**呼叫 run_backtest_concurrent

⚠️ 兩個實害
   併發    semantic-model 是 CPU 推論 ⇒ reranker 逾時而靜默落到詞面分支，整輪數字作廢
   前綴    `chat._record_no_knowledge_scenario` 的豁免比對 `backtest_session_`
           ⇒ 這些路徑**吃不到題庫豁免**，仍在寫 test_scenarios／suggested_intents
```

⚠️ **規則本身互相矛盾，⛔ 未裁前不得單方選邊**（已記 **DSP-003**）：

```text
量測層級.md〈執行規約〉  「session ⛔ 前綴**避開**內部保留字（backtest_／loop_／smoke_）」
run_batch.py            「⛔ 前綴**必須** backtest_session_」（而它 startswith 'backtest_'）
⇒ 依正本做會違反綁定行，依綁定行做會違反正本，**兩者都自稱 ⛔**
```

---

## ⛔ 防腐：本檔沒有任何強制，人不讀就不會發生

⚠️ **初版寫「下一個碰這些檔的人優先把它們變成測試」——那不是機制，是期待。**
而本檔自證用的兩條測試路徑**當天就是錯的**（少 `rag-orchestrator/` 前綴），
⇒ 本檔的自我維護在第 0 天就失效。

已落成、會紅的測試（路徑已修正）：

```text
rag-orchestrator/tests/unit/retrieval/test_kb_candidate_telemetry_contract_req.py
rag-orchestrator/tests/unit/retrieval/test_visibility_ruler_contract_req.py
```

**⛔ 以下目前無任何強制**（⛔ 不得寫成看起來已被指派）：

```text
母體定義同源     B2B_VISIBLE／ELIGIBLE 的複製品是否仍與 status.py 逐條相同
型別判定同源     status.py 與 kb_vs_helpcenter 的多面向取法（已知不同）
api 型不印覆蓋   print_b2b_surface 是否仍依 select 分流
**本檔自身**     表中每個路徑能否 Read、每個符號能否 grep 到
```

⇒ 專案已有成熟強制面：`scripts/audit/check_invariants.sh` 逐條呼叫
`scripts/audit/checks/*.py`，每支先跑 `--self-test`（突變控制）再跑本體，紅則 FAIL。
**做成一支 check 的成本遠低於「請下一個人優先」。** 是否現在做 ⇒ BACKLOG 待辦。
