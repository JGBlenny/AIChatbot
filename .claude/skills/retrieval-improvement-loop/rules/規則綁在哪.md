# 規則綁在哪：每條規則管到哪些具體產出

> 流程全圖、鐵律與閘門見 [`../SKILL.md`](../SKILL.md)。**⛔ 本檔只放這一塊。**
>
> ⚠️ 本檔源於業主 2026-09-02 的一句話：「**這是不是代表 skill 不夠清楚**」。
> 答案是**缺的不是知識，是「這條規則綁在哪」**。

## 鐵律

> **規則寫對了，但沒說它管到哪個檔案 ⇒ 那條規則等於不存在。**

---

## 為什麼需要這一檔（同一天三次同型失敗）

| 規則早就寫對 | 但沒說綁在哪 | 實際發生 |
|---|---|---|
| [`型別分批.md`](型別分批.md)：T2 的正解來源是 **API 回傳本身**、⛔ 拿知識庫當 T2 正解是禁止的 | 沒說「也管面向覆蓋這個指標」 | `status.py` 對 api 型面向印 `⚠️ 知識 2`，把「API 供料正常、只是入口少」讀成缺知識 |
| 母體定義用 `status.py` 的 `_B2B`／`ELIGIBLE` | 沒列出哪些檔要遵守 | **一天四個母體**（772／381／320／293），之後每一句「業者池 N 筆中…」分母都偏大 |
| [`正解判定.md`](正解判定.md) 的協議 | 沒說它**只適用 T1** | 拿 T1 的問法（「庫裡有沒有答案」）去評 T2／T3，而它們的正解根本不在知識庫 |

⚠️ **這種缺陷不會自己浮出來**：規則與違規的程式住在兩個地方，**兩邊分開讀都看起來對**。
⇒ 只有同時讀兩邊的人才會發現。上面三條全部是業主發現的，⛔ 不是實作方。

---

## 綁定表（⚠️ 實查得出，⛔ 不憑印象；新增實作點必須同步本表）

### ① 母體定義（正本：`scripts/status.py` 的 `_B2B`／`ELIGIBLE`）

```text
正本      scripts/status.py            符號 _B2B（答題母體）／ELIGIBLE（全庫適格）
逐條複製  scripts/audit/kb_vs_helpcenter.py   符號 B2B_VISIBLE   ✅ 已同源
          scripts/audit/kb_config_census.py   符號 ELIGIBLE      ✅ 已同源
可見性    rag-orchestrator/scripts/backtest/contract_enrich.py  符號 visibility
          ⚠️ **刻意不同**——它模型兩條檢索路（向量／詞面），⛔ 不得改成用 _B2B
⛔ 未同源  scripts/analysis/p1b_applicability_census.py
          scripts/audit/checks/instance_applicability_contract.py
          （兩支母體 871，缺 id<>4253 與 answer 非空；正本 772，差 99）
⚠️ 另有   rag-orchestrator/scripts/routing/run_p1_corpus_sibling.py  符號 exposure_surface
          （母體 146，且**已寫進凍結協定當判準**）
          scripts/g2_selftest.py  符號 ORACLE_Q（870，註解自陳刻意不複製生產過濾）
          scripts/knowledge-batches/tune_routing.py（匯入腳本，寫死 business_types，非查詢母體）
```

### ② 四型分批（正本：[`型別分批.md`](型別分批.md) 的「型別判定（機械）」節）

```text
scripts/status.py                    符號 print_b2b_surface
  ⛔ **api 型面向印「入口」⛔ 不印「覆蓋」**——答案來自 API，
     `conversational_engine._ground_by_api` 以 API 回傳為合成底稿
     （spec conversational-diagnosis 核心元件 1）
  ⛔ 空 answer 錨點不計覆蓋（業主 2026-09-02 定案）
scripts/audit/kb_vs_helpcenter.py    符號 _TYPE_SQL
  ⛔ 型別是**標記**不是閘門——事實對帳跑全型；
     只有「這筆是不是那題的正解」才分型（只 T1）
  ⚠️ 已知歧異：對「一列命中多面向」本檔 api 優先、status.py 用 max(f.sel) 取非 api，差 2 筆
scripts/audit/checks/face_preemption_capability.py
  用 grounding_scope.select=='api' 判能力，屬不變量 9，⛔ 非本迴圈射程
```

### ③ 正解判定（正本：[`正解判定.md`](正解判定.md)）

```text
⛔ **只適用 T1**（正解＝某一筆 knowledge_base row）
   T2 正解＝API 回傳｜T3 正解＝逐輪動作｜表單型＝欄位契約
⚠️ 但「內容有沒有講錯事實」**型別無關** ⇒ scripts/audit/kb_vs_helpcenter.py 跑全型
rag-orchestrator/scripts/backtest/nominate_ground_truth.py  提名候選（獨立 oracle）
```

### ④ 量測層級（正本：[`量測層級.md`](量測層級.md)）

```text
rag-orchestrator/scripts/backtest/run_batch.py   走正式入口 POST /api/v1/message
  ⛔ 不並行（semantic-model CPU 推論；併發會讓 reranker 逾時而靜默落到詞面分支）
  ⛔ 前綴必須 backtest_session_（兩個豁免用的前綴不一樣，見該檔 _REQUIRED_PREFIX）
```

---

## ⛔ 能做成靜態不變量的就做，⛔ 不要只寫文件

⚠️ 本檔本身也是文件——**文件擋不住任何人**。已落成會紅的測試的：

```text
tests/unit/retrieval/test_kb_candidate_telemetry_contract_req.py   剝欄清單兩份必須逐項相同
tests/unit/retrieval/test_visibility_ruler_contract_req.py         可見性尺與兩條 WHERE 同源
```

**尚未落成測試（⇒ 目前只靠人讀本檔）**：

```text
母體定義同源     B2B_VISIBLE／ELIGIBLE 的複製品是否仍與 status.py 逐條相同
型別判定同源     status.py 與 kb_vs_helpcenter 的多面向取法（已知差 2 筆）
api 型不印覆蓋   print_b2b_surface 是否仍依 select 分流
```

⇒ **下一個碰這些檔的人，優先把上面三條變成測試**，⛔ 不要再加一條文件。
