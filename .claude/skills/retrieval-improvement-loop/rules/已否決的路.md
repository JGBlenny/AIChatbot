# 已經走過並否決的路

> 流程全圖、鐵律與閘門見 [`../SKILL.md`](../SKILL.md)。**⛔ 本檔只放這一塊。**
>
> ⛔ 別重提。沒有**新事實**就別重新包裝端上來。

## 走過並否決的

| 走過的 | 死因 | 證據 |
|---|---|---|
| 補 `expected_answer` 逐字標準答案當答案尺 | 換個講法就誤殺，率被壓成雜訊；且及格線仍在被驗方 repo | 業主 2026-09-01 定案改用接受範圍 rubric |
| 用生產檢索管線提名「該命中哪筆」 | 業主順著點頭，尺就與系統同構，及格線又回到被驗方手上 | `t1-slot-selection-rule.frozen.md`〈命中候選提名〉 |
| 為 `embedding IS NULL` 的列補向量 | 那 51 筆是系統脈絡／對話規則設定列，retriever 兩條路都寫死排除；補了等於把系統提示詞丟進候選池 | `p0-3-knowledge-coverage-census.md` §2；業主 2026-09-01 裁「不做」 |
| 以 913 當覆蓋率分母 | 毛數字，把設定列 51 與空 answer 錨點 98 都算進分母；適格母體是 773 | 同上 §1 |
| 把 collection 連結當批次前置 | 現行 runner 完全不讀它，49 筆歷史 run 皆如此 | `PLAN-retrieval-coverage.md` §2 |
| 傳 `similarity_threshold=0.0` 想關掉門檻 | `retrieve()` 用 `threshold or default`，0.0 是 falsy ⇒ **靜默換成 0.6** | `base_retriever.py` 符號 `retrieve` |
| 拿 b2c／租客情境做檢索改善 | 真實非內部流量 b2c/tenant 僅 21 筆，b2b/property_manager 有 1,321 筆 | `b2b-b2c-pool-audit.md` §3；業主 2026-09-01 定案只做 b2b |
| 改 b2b 的 `business_types && {system_provider}` 過濾 | 刻意的隔離機制 | `docs/retrieval-recall-audit-20260822.md:82`，⛔ 勿改 |
| 補 `business_types` 空的那 75 筆的 `categories` | b2b 看不到它們，補了也不會被檢索到 | 同上 :114 |
| 補 `keywords` 空的 107 筆 | `keyword_boost` 只在無 rerank_score 時生效，KB 路徑幾乎不走；且關鍵字加成飽和塌陷是已知排序漂移根因 | 業主定案不補 |
| 以文件（架構母圖）為準去改過濾邏輯 | §3 五處全錯，照它給 b2b 補 `IS NULL` 會打穿池隔離 | `b2b-doc-status-ledger.md` 一 |

⛔ **共同模子**：凡是「看起來能讓數字變好」的一刀，先問**它動的是哪一類成因**。
本專案已四次把 `I 管線` 或 `V 可見性` 誤判成 `S 表示法`／`N 知識缺口`，
每一次的修法都是白工——因為那兩類根本不是知識寫法的問題。

## ⚠️ 尚未否決、但前提已被推翻，需重新評估的

```text
「補 retrieval_representation 以提升召回」
  ⏸ **狀態已更新（2026-09-01 同日兩次翻轉，⛔ 以本段為準）**

  第一次判定  IVFFlat lists=100／992 筆 ⇒ 宣稱「生產召回率 31%」
  ⛔ 已撤回    那是**自寫診斷 SQL** 的行為，⛔ 不是生產路徑
              生產 `ORDER BY (1 - dist) DESC` 用不上向量索引、一律精確掃描
              35 題經正式入口重跑：**濾後結果修前修後逐筆相同**
  ✅ 索引現況  已全部改為 HNSW，`make audit` 不變量 26 PASS
              ⇒ ⛔ **索引不再是待修的活坑**，⛔ 不得再以它為由擋下表示法評估

  真正推翻前提的是另一件事：**reranker 曾靜默停用**（佔最終分數 90%）
  ⇒ 在 reranker 停用期間量到的召回，⛔ 一律不可引用
  ⇒ 要評估表示法，**先過 G2⑥**（reranker 實際在跑），再談
  證據 `ivfflat-index-defect.md`（含其自我更正段）＋ `scripts/BACKLOG.md`
```
