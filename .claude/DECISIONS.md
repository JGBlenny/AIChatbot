# 決策紀錄

**起始日:2026-08-22**

⛔ **本表只保證此日之後的決策。** 在此之前的決策沒有回填,不在此表**不代表**沒有決定過——
歷史散落在 git log、`.kiro/specs/*/design.md` 的技術決策段落,以及
`.kiro/specs/retrieval-decision-layer/decisions/DECISIONS.md`(該 spec 自帶的 D-01 起決策表)。
⚠️ 回填歷史會製造「這就是全部」的假象,那比空表更危險。

## 怎麼寫一列

| ID | 決定 | 來源 | 出處 | 落點 | supersedes |
|---|---|---|---|---|---|

- **來源** = 誰做的決定。`業主` / `代理`(代理發現的爭議,未經裁決)
- **出處** = 證據在哪(對話日期、commit、spec 段落)
- **落點** = 這個決定落在程式的哪個符號或路徑,⛔ 會被 refcheck 驗
- **supersedes** = 這一條取代了哪一條(填 ID)

⚠️ `來源=代理` 的條目會被算成 **unresolved**,⛔ 永遠不會變成 current,
直到人裁決為止。這是刻意的:系統不准偷偷選邊。

---

| ID | 決定 | 來源 | 出處 | 落點 | supersedes |
|---|---|---|---|---|---|
| D-001 | 架構母圖 §3 的過濾公式**以 runtime 為準逐行修正**，⛔ 不是加警語。判準:`descriptive` 類文件描述「現在怎麼跑」而描述錯了，處置是修文件，不需裁決 | 業主 | 2026-09-01 對話「錯誤順便修正處理？」 | `docs/architecture/COMPLETE_CONVERSATION_ARCHITECTURE.md` | |
| D-002 | b2b 業態過濾 `business_types && {system_provider}` **無 `IS NULL` 放行**，⛔ 補上會打穿跨業者隔離。b2c 才是 `IS NULL OR &&` | 業主 | `docs/retrieval-recall-audit-20260822.md`「勿改」條 ＋ 2026-09-01 逐行對碼 | `rag-orchestrator/services/vendor_knowledge_retriever_v2.py` | |
| D-003 | 已封存 spec ⛔ **不得列為前置必讀**。`decision_layer.py` 的 3 條死引用**移除**而非改指 `archive/`——後者會強迫閱讀一份已停案的 spec | 業主 | 2026-09-01 對話「避免被讀到」；`HANDOFF-20260901.md` §0「⛔ 不是主線」 | `.claude/prerequisites.json` | |
| D-004 | `KNOWLEDGE_SCOPE_MIGRATION_GUIDE.md` 退休歸檔並自標——它引用的兩個檔案都不存在、過濾模型已被 v2 取代 | 業主 | 2026-09-01 對話；`b2b-doc-status-ledger.md` 第一類 | `docs/archive/2026-09/KNOWLEDGE_SCOPE_MIGRATION_GUIDE.md` | |
| D-005 | 文件的**刪除判準是「錯的是什麼」不是「有沒有錯」**:前提全滅且無唯一資訊→刪；前提滅但記錄決策→歸檔；局部錯而其餘是唯一正本→修正，⛔ 不准刪 | 業主 | 2026-09-01 對話「沒有留著的必要就刪了吧」（據此刪 3 份零引用檔） | `scripts/BACKLOG.md` | |
| D-006 | `.claude/MAP.md` 與 `DECISIONS.md` **移出 `.git/info/exclude`、納入版控**——`canon.json` 自己寫明它們是專案資產;先前被排除⇒修改無版本歷史 | 業主 | 2026-09-01 對話「好 進 git」 | `.claude/MAP.md` | |
| D-007 | IVFFlat 缺陷**降級為潛在地雷**:原判「靜默丟答案影響生產」**是錯的**——生產 `ORDER BY (1-dist) DESC` 用不上向量索引，35 題修前後逐筆相同。危險在於有人把它改成正規寫法就引爆 | 代理 | `ivfflat-index-defect.md`「2026-09-01 更正」節 | `.kiro/specs/conversational-routing-execution/ivfflat-index-defect.md` | |

⚠️ 本表的 `D-0xx` 與已封存 spec `archive/retrieval-decision-layer/decisions/DECISIONS.md`
的 `D-01` 系列是**兩套獨立編號**，⛔ 不得互相引用。
⚠️ `D-007` 來源為**代理**⇒ 依本檔規約算 unresolved，⛔ 在業主裁決前不會變成 current。
| DSP-001 | ⚠️爭議:面向 grounding_scope.requires_instance_reference=true 是否構成該面向所屬知識的 knowledge 層 instance machine evidence | 代理 | rag-orchestrator/services/instance_applicability.py 模組 docstring（裁定②證據門檻不對稱 vs 兩層契約語義不同） ／ 實作:instance_applicability_decision 允許 general×REQUIRED→INELIGIBLE；contract_closeout/contract_change/contract_sign/contract_renew 四面向 requires_instance_reference=true |  |  |
| DSP-002 | ✅**已裁（業主 2026-09-02）**:①空 answer 錨點 ⛔ **不計為面向覆蓋**（它不能成為答案）；②**api 型與對話型面向的答案不來自 KB answer 欄**（`_ground_by_api` 以 API 回傳為合成底稿） ⇒ 對它們「知識數」量到的是**提名入口數**、⛔ 不是答案覆蓋。三個數字各自的用途（非爭議，事實層）：293 答題母體約定（`_B2B`，含刻意窄化：僅 property_manager、扣 4253，⛔ 不是生產答題路徑的模型）／354 兩路聯集上界／354·320 分路上界（`visibility().paths.*`）。⚠️ `visibility()` 只給**結構上界**：`visible=False` 是硬結論，`visible=True` ⛔ 不保證任何一題撈得到。 | 業主 | spec:conversational-diagnosis/design.md 核心元件 1（API grounding：收斂以 API 回傳為合成底稿）、domain-conversational-facets/design.md 元件 2 ／ 實作:`conversational_engine` 符號 `_ground_by_api`；`status.py` 符號 `print_b2b_surface`（已依 select 分開標示） ／ ⚠️ 原版問法「_B2B vs visibility() 二選一」不成立且引用錯數字（293/264，實為 354/320），已作廢 | scripts/status.py:print_b2b_surface |  |
| DSP-003 | ⚠️爭議:回測 session_id 前綴：量測層級.md 說避開 backtest_，run_batch.py 說必須 backtest_session_ —— 兩者都自稱 ⛔ | 代理 | .claude/skills/retrieval-improvement-loop/rules/量測層級.md〈執行規約〉「⛔ 前綴避開內部保留字（backtest_／loop_／smoke_）否則會被標成內部流量」 ／ 實作:run_batch.py 符號 _REQUIRED_PREFIX='backtest_session_'（為吃到 chat._record_no_knowledge_scenario 的題庫豁免）；usage_metering 符號 INTERNAL_RULES 比對 startswith('backtest_') ⇒ 用了必被標成內部流量 |  |  |
| DSP-004 | ✅ 租約特有欄位來源＝**A**：chatai 消費 DocuMind 既有 `rental_terms`（租約值優先、通用進 conflicts；`deposit` 混鍵不消費），第二段 LLM 抽取（B）作廢；缺欄由 DocuMind 補樣式 | 業主 2026-09-04「依序處理」採預設 A | .kiro/specs/documind-ocr-mapping/requirements.md〈待業主裁決〉D1 ／ 實作:services/ocr_mapping/contract_mapper.py 符號 CONTRACT_SOURCE_MAP（DocuMind contract 型無 deposit_type／cycle_date／cycle／early_termination_*；second_pass_extractor 未實作、ENABLE_OCR_SECOND_PASS 預設 false）；⚠️ 2026-09-03 晚新事實：DocuMind `contract_field_extractor.py` 符號 `rental_fields` 已輸出 `rental_terms` 群組（14 欄，僅制式標籤命中），chatai 未消費 ⇒ A 可先零改 DocuMind 起步 |  |  |
| DSP-005 | ⚠️爭議:端點形狀：新資源 POST /api/v1/ocr-mapping/{document_type}（A，已實作）還是掛在 /api/v1/message 加 action（B） | 代理 | .kiro/specs/documind-ocr-mapping/requirements.md〈待業主裁決〉D2 ／ 實作:routers/ocr_mapping.py 符號 router = APIRouter(prefix="/api/v1/ocr-mapping")（已依建議 A 實作；若改掛 /api/v1/message 需重做 router） |  |  |
| DSP-006 | ⚠️爭議:承租方姓名要不要由 chatai 切姓／名（split，預設）還是整串交 LIFF 拆（keep_full）；複姓表以 JGB 慣例為準（Q5） | 代理 | .kiro/specs/documind-ocr-mapping/requirements.md〈待業主裁決〉D3 ／ 實作:services/ocr_mapping/name_splitter.py 符號 NamePolicy／COMPOUND_SURNAMES（兩策略皆已實作；env OCR_NAME_POLICY 預設 split，兩欄恆進 needs_confirmation） |  |  |
| DSP-007 | ✅ 不變量 10 與 17 互斥的解法＝**D 明示排除**：R10-P V1 母體維持 08-29 凍結的 54 列；凍結後才宣告的 id 登記於 `r10p/v1-scope-exclusions.txt`（＝明示版本決策），不變量 17 只對**未登記**的新宣告判漂移、且排除清單不得含凍結成員。⛔ 否決 B（凍結 54 內本有 5 筆 general，改定義成「只納 instance」會把母體縮到 49）；⛔ 未採 A（升版 88 需重跑 F1 proposal／P4 projection，三支檢查器寫死 54）。代價：3327／3329／3331／3333 標 general 的責任變化 V1 看不到，V2 必納 | 業主 2026-09-04「好」（採建議 D） | scripts/audit/checks/instance_applicability_contract.py 不變量 10（2026-08-30 後 touched 的情境知識必須明示 instance_applicability）vs scripts/audit/checks/r10p_population_integrity.py 不變量 17（R10-P 母體＝la ∪ alias ∪ 所有已宣告列，凍結 54，任何新宣告＝漂移 FAIL） ／ 實作:2026-09-04 00:4x 對 3327–3360＋3930 共 34 筆寫入 generation_metadata.instance_applicability=general（業主授權）⇒ 不變量 10 轉綠、不變量 17 現況乾淨轉紅：聯集多出 34 id；還原則 10 重紅。symbol: r10p_population_integrity.py declared AS (SELECT id FROM knowledge_base WHERE generation_metadata ? 'instance_applicability') |  |  |
| DSP-008 | ✅ **業主 2026-09-04 裁：匯入事實以 jgb2 現況為準。** jgb2 原始碼（`~/jgb/project/jgb/jgb2`，分支 ticket_12899002511，scout 2026-09-04）：可批次匯入＝物件（`routes/web.php` `/general/batch/*`→`EstateController@generalBatch*`、社宅 `socialHousingBatch*`）、租客（`routes/api.php` `/batch/import`→`LesseeController@batchImport`）、帳單（`BillController@batchImportExcel*`，僅 Ango role＋法人房東；匯入＝建立待發送新帳單，⛔ 不是搬歷史帳單）、社區（`CommunityController::batchPreview`）、大房東記錄（`BiglandlordController::importBigLandlordRecordsFromExcel`）；**合約 ⛔ 不支援**（Web 只有單份 PDF 上傳 `/api2/contract/upload/{id}` 與 `batchDelete`；社宅合約是後台 CLI `social-housing-contract:import`）。⇒ 盤查檔 §P1-3「只有帳單支援批次匯入，物件／合約／租客都要手動建」**錯誤**（幫助中心 `onboarding4_zh-Hant.html` 亦明寫物件批次上傳）；知識 3357／3431／3476／3404／4638 **與 jgb2 一致、不改**；真缺口＝「合約不能批次匯入、帳單匯入不是搬歷史」邊界句 ⇒ 補一筆 prospect 知識。P1-2／P1-3 定性：LLM 把「物件／租客可匯」推廣成「合約／歷史帳單可匯」＝**答錯**（非答不完整）。⚠️ 證據取自 ticket 分支非 master；四類功能路由＋Controller＋Validator＋Job＋前端俱全，上線狀態以 preview 部署為準 | 業主 | docs/presales-assistant-quality-audit-20260902.md §P1-3（原爭議）；scout 報告見本 session；幫助中心 onboarding4／Landlordonboarding06Finance；⚠️ 落點另含 `docs/presales-assistant-rootcause-20260904.md` §DSP-008，該 json 內容＝匯入邊界句列 | scripts/knowledge-batches/presales-phrasing-batch-20260904.json | 原 DSP-008 爭議列 |
| DSP-009 | 售前缺口地圖「刻意不補」五格的裁決引用：客戶案例／規模、SLA／賠償、資安認證／資料存放、個資法、客製報價 ⇒ 一律固定句轉人、⛔ 不補知識。依據＝presales-grounding-gate requirements「已拍板決策」#2（敏感五類 customer_reference／pricing／contract_sla／compliance／security 由 fact_class 分流出口）與 #3（客戶規模／案例類刻意不補），業主 2026-09-04 裁 A 收案時確認。地圖 demand JSON 的 policy=deliberate_no 必引本條；無引用者不得計入刻意不補（plan-verifier P2） | 業主 | .kiro/specs/presales-grounding-gate/requirements.md 已拍板決策表；perf-20260904.md §9–10；⚠️ 落點細節＝cells C10/C48–C51 的 policy_ref | .kiro/specs/presales-grounding-gate/coverage-map/demand-v2.json |  |
| DSP-010 | ⚠️爭議（未裁）：合約範本數——幫助中心 `landlordonboarding04lease_zh-Hant.html` 寫「內政部所規範的五種合約範本」（原房東中文／包租業中文／代租代管中文／英文／英越文），`qa06_zh-Hant.html`（編輯日期 2026/06/29）寫「通用範本共 12 種」＋三種特殊用途範本。兩份官方文件不一致；3600 主題頁採較新的 qa06。待業主向內容端確認，⛔ 未裁前不得改回五種 | 代理（verifier 2026-09-04） | 幫助中心兩檔逐字；scripts/knowledge-batches/presales-topic-contract-3600-20260904.json `_meta.disputes`；⚠️ 落點段落＝3600 主題頁【範本】段 | scripts/knowledge-batches/presales-topic-contract-3600-20260904.json |  |
| DSP-011 | ✅ **業主 2026-09-04 裁：權限由 jgb2 API 全權處理，本系統只管額度。** 適用 agentic-mcp 全路徑（in-process 與 MCP 門面）：`role_id`／`user_id` 為上游信任輸入（沿用 F-C25），個資可見範圍由 jgb2 `external/v1` 兩層權限（API Key 權限表＋`viewer_user_id`→`VisibleScope`）裁；本系統對呼叫者只有 `api_key_guard`（服務層）與 `quota_check`（額度）。⇒ MCP 門面定為**非公開認證面**（只給上游／內部呼叫者），⛔ 不實作 bearer 簽發／claims 驗證；security-reviewer 的 P0「bearer 信任鏈未定義」「MCP 暴露 jgb2.query 打穿上游授權前提」依本條 REJECT。**例外**：知識池可見性（vendor_ids／business_types／target_user／保留分類）住本系統 DB，仍由本系統謂詞守（`build_visibility_predicate`）。前提破了（出現未經上游的公網呼叫者）要重開 | 業主（「權限的部分 由 api 全權處理」「此系統只管額度」） | .kiro/specs/agentic-mcp-orchestration/design.md 1.1〈授權定案〉；jgb2-source-index.md §3.5；memory project_role_trust_boundary；⚠️ 落點段落＝design 1.1 元件 4、決策 8、附錄 C | .kiro/specs/agentic-mcp-orchestration/design.md |  |
| DSP-012 | ✅ **業主 2026-09-04 裁：採選項 A。** ①字面衝突不成立——R11.1 禁令主詞是「**回傳**文字」而非信任清單裡的「知識文字」，大綱屬 server 端程式組裝（R5.4 明訂可重跑＋版本戳＋sha256、⛔ 無 LLM），不在禁令內 ⇒ R11.1.1 措辭收窄為「工具回傳文字」。②新增 R11.5：進 system prompt 只允許兩種來源（本系統程式產生的指令文字／`OutlineAssembler` 由已審核列組裝的大綱目錄），一律套每回合 nonce 資料標記，⛔ 其餘走工具回傳位置。③⛔ **否決另一選項**（大綱改走 `kb.get("outline:*")` 按需讀取）——R5.2 已訂 prospect 不呼叫 `kb.search`，大綱再改按需即把「每一句對到同一份完整大綱」降級為模型自選，打穿 R5 立案理由與決策 3 的量化依據（pilot 11%→52%），且同一批文字換位置不降低風險。④🔴 **裁決同時補 design 原假設缺的資料面**：design 寫「由**已審核**知識列組裝」，但 `knowledge_base` 實查**無任何審核旗標** ⇒ 現況＝有 KB 寫入權即有 system prompt 寫入權。新增 R11.6：加 `outline_approved_by`／`outline_approved_at`，只有已審核列得進 prospect 大綱；未審核列**仍可**被 `kb.get` 當引用來源（工具回傳位置，風險等級不同）。M1 前置＝現有售前池 31 筆一次 UPDATE 標記，審核 UI 另案，⛔ 不得因 UI 未完成而放行 | 業主（「A」） | 原爭議由 security-reviewer 第二輪 2026-09-04 提出；requirements.md R5.1／R5.2／R5.4／R11.1；⚠️ 審核旗標不存在的實查證據：`grep -rniE "approved_by\／is_approved\／review_status" rag-orchestrator/models/ rag-orchestrator/database/` 零命中，正對照組同法搜 `target_user` 有命中；⚠️ 落點段落＝design 1.2 元件 5、決策 14、資料模型 | .kiro/specs/agentic-mcp-orchestration/design.md |  |
| DSP-013 | ✅ **裁（主 session 2026-09-04）：agent 邊界不變量以 27–31 落地，design 附錄 B 改標「實際編號 27–31（原 18–22）」，checker 每行印 design 對照號；`check_invariants.sh` 既有 18–22 不動。** 原爭議:design.md 附錄 B 的『不變量 18–22』與 scripts/audit/check_invariants.sh 現況的不變量 18–22（R10-P2/P3/P4、canonical、scope-lock）編號衝突——兩者是不同工作線各自遞增到相同號碼後合併（已重複發現 2 次） | 代理 | .kiro/specs/agentic-mcp-orchestration/design.md:160,206,211-212 (不變量 18/19/20/22) ／ 實作:scripts/audit/check_invariants.sh: grep '不變量 1[8-9]：\／不變量 2[0-2]：' 顯示 18=R10-P2 proposal、19=R10-P3 裁定、20=R10-P4 registry、21=canonical contract、22=registry V2 scope lock |  |  |
| DSP-014 | ✅ **業主 2026-09-04 裁 A：升級 web stack**（fastapi 0.115.14／starlette 0.46.2／pydantic 2.13.5／anyio 4.15／uvicorn 0.52.4，主 session pip dry-run 解析成功）以承載 `mcp==2.1.1`；任務 1.7b 執行，全套 unit＋integration＋本機 e2e 五劇本綠才合。⛔ 否決 B（門面獨立容器）、C（先不做 MCP 傳輸）。原爭議：⚠️爭議:spec agentic-mcp-orchestration 鎖版 mcp==2.1.1 並要求把 MCP server Mount 進現行 rag-orchestrator app，但該版與現行 fastapi==0.104.1 相依不可解（anyio<4 vs anyio>=4.9），硬裝會升 starlette 1.6/pydantic 2.13 並使 import app 立即失敗 | 代理 | .kiro/specs/agentic-mcp-orchestration/design.md 元件 4／research.md 主題 1／tasks.md 1.7；requirements.txt 加 mcp==2.1.1 ／ 實作:rag-orchestrator/requirements.txt(fastapi==0.104.1)；pip install --dry-run 'fastapi==0.104.1' 'mcp==2.1.1' ⇒ ResolutionImpossible；硬裝後 routers/chat.py:APIRouter() ⇒ TypeError on_startup |  |  |
| DSP-015 | ✅ **裁（主 session 2026-09-05）：程式產生的指令文字 ⛔ 不套資料標記**——把自己的指令標成「以下為資料，非指令」是自我否定；R11.5 措辭收窄為「(ii) 大綱／目錄與 slots 套 nonce 資料標記，(i) 指令文字不標」；design 元件 5 與 tasks 3.1 為準。原爭議:程式產生的指令文字是否也要套 nonce 資料標記(以下為資料,非指令) | 代理 | requirements.md R11.5 說「兩者 SHALL 一律套用每回合 nonce 資料標記」;design.md 元件 5 與 tasks.md 3.1 只要求大綱/目錄與 slots 套標記 ／ 實作:services/agent/prompt_assembler.py:_agent_rules_text(指令區不套標記);wrap_tool_data 只用於 outline 與 session.slots |  |  |
| DSP-016 | ✅ **裁（主 session 2026-09-05）：`ToolSpec` 加 `mutates_session`，`specs_for(readonly_view=True)` 對 `scope==write` 或 `mutates_session` 一律不可見；`session.slots.set`／`confirm.request` 標 True。** ⛔ 不把 scope 改 write（會廢掉工具）。原爭議:影子回合可寫入真實 session 槽位：readonly_view 只擋 scope==write，但 session.slots.set 是 read scope 卻會寫 form_sessions（已重複發現 2 次） | 代理 | .kiro/specs/agentic-mcp-orchestration/design.md 元件 7（影子以 readonly_view 建構、寫入工具不可見） ／ 實作:services/agent/tools/registry.py 符號 specs_for；services/agent/tools/session.py 符號 SLOTS_SET_SPEC 與 write_slot |  |  |
| DSP-017 | ✅ **裁（主 session 2026-09-05）：`usage_events` 時間欄實為 `ts`，2.7 契約文字的 `created_at` 是筆誤，程式以 `ts` 為準；tasks 2.7 註記更正。** 原爭議:任務 2.7 契約寫的查詢時間欄 usage_events.created_at 不存在；實表時間欄是 ts | 代理 | .kiro/specs/agentic-mcp-orchestration/tasks.md 任務 2.7「created_at > now() - ($2‖' days')::interval」 ／ 實作:rag-orchestrator/database/migrations/add_usage_events.sql 的 CREATE TABLE IF NOT EXISTS usage_events 只有 ts TIMESTAMPTZ；四支 ALTER TABLE usage_events migration 皆未加 created_at；實查測試庫 information_schema.columns 亦無此欄（正對照：同一查詢 ts/decision_snapshot/vendor_id 等 11 欄全部命中） |  |  |
| DSP-018 | ✅ **裁（主 session 2026-09-05）：session slots 的儲存形為 design 的巢狀 `SlotValue{value, source, confirmed}`（`form_sessions.collected_data.slots`），進 prompt 的形為純量；`runtime._slots_for_prompt` 在邊界攤平為契約，`prompt_assembler._slot_blocks` 維持只收純量。** 原爭議由 2.9 executor 提出，`dispute.py` 誤併入 DSP-016 | 主 session | design 元件 5 `PromptAssembler.build(slots: dict[SlotKey, SlotValue])` vs `services/agent/prompt_assembler.py:_ALLOWED_SLOT_VALUE_TYPES` | `services/agent/runtime.py:_slots_for_prompt` |  |
| DSP-019 | ✅ **業主 2026-09-05「授權」：售前池 17 列（3584、3585、3596–3599、3601–3611）補宣告 `instance_applicability=general` 並登記 `r10p/v1-scope-exclusions.txt`（DSP-007 同法）。** 起因：3.3 標記 UPDATE 觸發 `update_kb_updated_at`，31 列 `updated_at` 撞成當日，不變量 10 對未宣告的 17 列判紅。教訓：對 `knowledge_base` 任何欄位寫入都會撞 `updated_at`，標記類欄位要一併評估不變量 10／17 | 業主 | `make audit` 不變量 10 輸出；`SELECT ... WHERE outline_approved_by='owner-20260905' AND NOT coalesce(generation_metadata ? 'instance_applicability', false)` | UPDATE 17；exclusions 檔追加 17 行；`make audit` PASS |  |
| DSP-020 | **大綱章節在回合開始預載成一筆 provenance（保留 `tool_call_id="outline"`）、章節標題帶 section id。** 起因：影子 2026-09-05 第一筆真流量（prospect「可以線上簽約嗎」）——模型照鐵則引用大綱、Verifier 只認工具回傳的 provenance ⇒ 兩次 QUOTE_NOT_VERBATIM → budget_exhausted 轉人，且大綱文字只印標題、模型無從得知 `outline:*` id。否決「要模型先 `kb.get("outline:*")` 再引用」：多一輪模型呼叫（約 +3–5 秒、+0.0005 美元）換不到任何安全性——同一批文字本來就在 system prompt。`kb.get("outline:*")` 保留；pm／tenant 目錄同樣預載但 `citable=False`，Verifier 以 SOURCE_NOT_CITABLE 拒（決策 10 修訂不變）。模型偽造同名 tool_call id ⇒ 記 violation、不覆寫種子 | 主 session（自主，業主「真的需要我處理跟我說」） | `services/agent/runtime.py` `OUTLINE_TOOL_CALL_ID`／`_seed_outline_provenance`；`services/agent/outline.py` `_build_doc`；`services/agent/agent_rules.py` 輸出契約第 4 條；`tests/unit/agent/test_outline_citation_seed_req.py` | outline_sha／rules_sha 皆變（快取自然失效）；影子 smoke 重打 |  |
| DSP-021 | **Verifier／輸出契約四項校正（影子首日真流量，主 session 自主）**：① `kind=handoff` 是敏感五類應走的出口——`fact_class ∈ SENSITIVE` 在 handoff 上不拒、`handoff_reason` 必在 `HandoffReason` 值域、模型文字不出去（Runtime 換 `effective_handoff_message` 固定句），Verifier 對 handoff 不跑逐句檢查（原本模型正確轉人卻因 `sentence_map=[]` 判 SCHEMA、兩拒耗盡、reason 誤記 budget_exhausted）；② 給模型的 strict schema 把 `fact_class`／`handoff_reason` 收成封閉列舉（原本模型回 null 即被 fail-closed 當敏感拒）；③ 大綱引用 source 在 Runtime 做機械正規化（去誤抄標題、補 `outline:` 前綴）、找不到標的節時在**同一 tool_call_id** 的 provenance 全組掃逐字（citable 以實際命中為準；標籤錯≠引文假）；④ 極性改詞組層級（句「不支持」vs 引「不支援」同為否定），否定詞表補「不支持」「不行」（rules 1.1.0）。⑤ SCHEMA 拒因回饋附「系統對模型 answer 的切句結果」（句數＋每句前 12 字，不含來源原文）——gpt-4o-mini 1 句標 2 筆／2 句標 1 筆是最常見拒因，只回 SCHEMA 它無從修正。⛔ 尺沒有放寬：逐字、同一回傳、citable、敏感五類全保留 | 主 session | `services/agent/verifier.py`（handoff 分支、fallback、極性）；`services/agent/runtime.py`（`_canonicalize_outline_sources`、response_format enum、handoff 固定句）；`tests/fixtures/agent/known_good.json`＋`known_fabrications.json` 各補 3／3；`tests/unit/agent/test_outline_citation_seed_req.py` | 診斷腳本實跑（一次性、不進 commit）：修前 5 題 0 題正確回答，修後見 DSP-022 | 每項都有 fixture 雙向釘住（放行一例＋拒一例） |
| DSP-022 | **P0 修正：`AgentRuntime.run_turn` 從未把當前 `user_message` 放進送模型的 messages，也從未把 user／assistant 寫回 `state["agent"]["dialog"]`**——REST 入口、影子、MCP `agent.turn` 三路皆然；模型只看到 system prompt（大綱）就作答，今天所有「答非所問」（問簽約答匯入、問價格講功能）皆源於此。單元測試全部用假 provider 不看 messages，所以 M0／M1 收案沒抓到；真線路第一天才現形。修：messages 末尾追加當前 user 訊息；回合收尾（含固定句、含快取重播）寫回 dialog，上限 `DIALOG_MAX_MESSAGES=20`（10 輪）丟最舊。附帶：`schedule_shadow` 把空 state 交給影子、沒塞 `app.state.agent_outline`（影子 prompt_tokens 僅 2.4k、每題 no_grounding），同修。教訓：**假 provider 的測試至少要斷言「最後一則 user 訊息＝當前問題」**，已加 | 主 session | `services/agent/runtime.py` `_append_dialog`／`DIALOG_MAX_MESSAGES`；三條回歸測試 | 修前後同 5 題對照：gpt-4o-mini 0/5→2/5 正確、gpt-4o 0/5→4/5（n=5，僅供 D1 參考） | 之前所有真線路影子紀錄（含 `agent_shadow` 首筆）作廢，M2 評估從此版起算 |
| DSP-023 | ✅ **業主 2026-09-05「統一 gpt-4o-mini」（D1 裁定）**：agent 路徑（REST 入口、影子、MCP `agent.turn`）與舊鏈同一模型，⛔ 不為 agent 另開 gpt-4o；`AGENT_MODEL` 只留作本機對照實驗用的覆寫，正式環境不設。依據：修正 DSP-022 後 n=5 對照 mini 4/5、gpt-4o 4/5，差距不足以支付約 15 倍單價；4.3 凍結樣本以 mini 一個模型跑 | 業主 | `services/agent/runtime.py`（`self._model` 回退鏈 AGENT_MODEL→OPENAI_MODEL→gpt-4o-mini）；`docker-compose.prod.yml` `OPENAI_MODEL` 預設 | 4.3 只跑 mini | D1 關閉 |
| DSP-024 | ✅ **業主 2026-09-05：JGB API 不足先以 mock 替代，之後整理一次請 JGB 開。** 落成總帳 `.kiro/specs/agentic-mcp-orchestration/jgb2-api-requests.md`（15 列，合併 source-index §6、line-bot §2、chatai-* 未驗主張）與六條 mock 紀律：只在 `JGBSystemAPI`（既有 `USE_MOCK_JGB_API`）邊界 mock、資料錄不編、每個 mock 標回列號、mock 路徑不得宣稱 e2e 已驗、正式環境不開 mock（compose 預設 true，⚠️ 部署必查）、每列有解除條件 | 業主／主 session | `grep -n use_mock rag-orchestrator/services/jgb_system_api.py`；`docker logs aichatbot-rag-orchestrator \| grep use_mock` | 本機容器 `use_mock=False` | 匯出給 JGB 時只寄需要 JGB 動的列（#1、#3、#4、#5、#6、#9–#14） |
| DSP-025 | ✅ **業主 2026-09-05「同意」D2 收案線＝requirements 草稿數字**：敏感五類 0 漏；功能邊界漏 ≤ 1/15；邊界題不硬答 ≥ 90%；p95 ≤ 12 秒；答到率 ≥ 56%（41%＋15 點）。寫進 `eval/samples-manifest.json` `acceptance_d2`，⛔ 看過結果不得改；只有樣本 C 可作放行證據 | 業主 | `python3 -c "import json;print(json.load(open('.kiro/specs/agentic-mcp-orchestration/eval/samples-manifest.json'))['acceptance_d2'])"` | — | D2 關閉 |
| DSP-026 | ✅ **業主 2026-09-05「同意」取消大綱重複列標記——主 session 核對後把範圍從工具建議的 7 列收窄為 2 列（5376、5377）**：只有 3584＝5377、3610＝5376 兩對答案逐字相同（DB 實查 `pair1=t pair2=t`）；5378／5379／5380 各有獨有內容（催繳／匯入／帳單格式），5379 更是今天模型引用的匯入答案，⛔ 不得取消。保留主題頁 id（3584、3610），取消講法列（5376、5377）。知識本身不動，只影響大綱組裝；SQL 存 `sql/unmark-outline-alias-20260905.sql` | 業主授權／主 session 收窄 | `SELECT id, outline_approved_by FROM knowledge_base WHERE id IN (3584,5377,3610,5376)` | 大綱 31→29 列，容器重啟重組 | 教訓：`agent_outline_dump.py` 的 alias 判準（batch_file）對大綱用途過寬——講法列不等於重複內容，建議 SQL 只能當人審輸入 |
| DSP-027 | ✅ **業主 2026-09-05 裁部署方式＝「整庫覆蓋加回灌」（本機庫為真相，線上獨有運行資料先導出、覆蓋後灌回）；時機＝spec 走完後，⛔ 目前不部署。** 基礎流程沿用 runbook 附錄 A（A-1 盤點／A-4 簡化版覆蓋／A-5 回灌），屆時補 §20 差異：回灌清單擴為 `api_keys`（覆蓋後 TRUNCATE 再灌 prod 的，內部 MCP key 依 §19-2 在正式機重發）、`usage_events`、`vendor_quotas`、`openai_cost_tracking`、`form_sessions`（樣本 C 來源）、`unclear_questions`、`image_uploads`；盤點後決定：`vendors`／`vendor_configs`／`vendor_sop_*`／`lookup_tables`／後台 07-07 後改過的 `knowledge_base` 列；回灌後逐表 setval 序列；本機出貨前 A-2 另加 TRUNCATE `agent_shadow_texts`／`agent_confirmation_tokens`。⚠️ 樣本 C 只有部署（任一分支含 commit `0464ff02`）之後才會開始累積 ⇒ M2 放行證據依賴部署時點 | 業主 | `docs/deployment-runbook.md` 附錄 A；本機 `api_keys` 三列（id 4 JGB／76 admin-web-proxy／97 mcp-internal-local） | 尚未執行 | 部署前主 session 依本列寫成 §20 逐條指令＋預期輸出 |
| DSP-028 | **輸出契約改逐句陣列（提案 v3：r10 七條＋r11 安全審七條全數處置；待 closing plan-verifier）**：`AgentOutput{kind, sentences[{text, kind, cite[]}], citations[], fact_class, handoff_reason}`；⛔ 模型不再輸出 `answer`＋`sentence_map`；`answer`＝程式 `"".join(text)` 導出屬性。**Verifier 步②（逐筆）**：(a) `kind≠handoff` 且 `sentences==[]` ⇒ SCHEMA；(b) 任一 `text.strip()==""` ⇒ SCHEMA；(c) 任一 `cite` 索引越界 ⇒ SCHEMA；(d) 對每筆以 `_split_sentences(text)` 切片段，**每一片段各自**跑步②型別複核（`_effective_kind` 以片段本文判）與步③～④（cite 存在性、逐字、覆蓋、極性、citable，比對文字一律用片段本文，⛔ 不用整筆或拼接 answer）——繼承的是 `kind`／`cite` 標籤，不是驗證結果；(e) `VerifierVerdict.sent`＝**筆索引**（片段不另編號），`trace_view` 顯示改「筆次」；步①⑤⑥⑦對拼接後 answer 照跑（跨筆拆數字規避靠這裡擋）。**範圍**：`output_schema.py`（`Sentence`／`AgentOutput`，刪 `SentenceCite`）、`verifier.py`、`runtime.py`（導出 answer、SCHEMA 回饋指出筆索引與原因三種）、`agent_rules.py`＋`prompt_assembler.py` 引用鐵則段（兩處都改，`rg sentence_map services/` 須零命中）、`trace_view.py`、30 fixture 機械轉換（規則：`sentence_map==[]`⇒`sentences:[]`；`schema_mismatch` 改 cite 越界；新增「疑問句夾斷言同一筆」拒 UNCITED_ASSERTION、空陣列 SCHEMA、空 text SCHEMA 三案；正對照：同筆只留問句放行）、6 個測試檔＋`tools/agent_eval.py`（假輸出改形＋新增 `verifier_reasons`／`budget_exhausted_n` 欄與報表列）。handoff 時 `sentences` 可空（Runtime 換固定句）。**驗收（四尺同列，任一退步即停）**：同 `samples_sha` f78344…、`--repeat 3`、mini：① `budget_exhausted`（自 `topics.jsonl` `handoff_reason` 統計）77/162 → ≤20/162；② `forbid_hit` ≤ 3/162；③ sensitive 0 漏維持；④ `OutputVerifier.self_test` 對轉換後 fixture 全綠。**r11 安全審處置**：F-1 **FIX**——(d) 量詞寫死：每一個降級為 fact 的片段，必須在該筆 `cite` 中**至少一筆** citation 完整通過③④（逐字＋覆蓋＋極性＋citable）；全數不過回最後失敗 reason；⛔ 不得以同筆其他片段已通過代替；F-2 **部分 FIX／部分 OPEN**——`assertion_terms` 補封閉詞「支持、包含、內建、整合、自動」（產品能力動詞），「有／是／已／將」過泛先不補（會把「您有幾間？」降成 fact 推高誤殺），改在 eval 加監控欄「整筆免 cite 的 question／greeting 筆數比例」；r2 第 10 條記為「多句變體 FIX、單句變體 OPEN」；F-3 **FIX**——`_assert_all` 加 `expected_reason` 比對；驗收尺④改為 `pytest tests/unit/agent/test_verifier_req.py` 全綠（含逐筆 reason 與 11 拒因覆蓋）；F-4 **FIX**——`answer` 用純 `@property`（⛔ 不用 `computed_field`），加測試 `set(response_format properties)=={kind,sentences,citations,fact_class,handoff_reason}`；F-5 **FIX**——(c) 明寫 `idx<0 or idx>=len(citations) ⇒ SCHEMA`＋負索引 fixture；F-6 **FIX**——片段 `strip()==""` 跳過型別複核與③④（①⑤⑥⑦仍掃拼接全文）；F-7 **DEFER**（既有、與本案無因果）——步⑥與字面型敏感詞另對壓空白文本再掃，列 BACKLOG（tasks 5.6）。**新增 fixture**（安全審 F-a～F-f）：跨筆拆字規避 assertion（UNCITED_ASSERTION）、跨筆拆數字（SENSITIVE_TOPIC，拼接掃描反向護欄）、片段繼承 cite 但覆蓋不足（QUOTE_NOT_COVERING，sent=0；正對照兩種放行）、greeting／routing 尾巴夾斷言（UNCITED_ASSERTION ×2）、cite 負索引（SCHEMA）、handoff 帶捏造 sentences（known_good 放行＋行為測試 `TurnResult.answer==effective_handoff_message(None)`）。驗收 grep 擴為 `rg -n sentence_map rag-orchestrator/services rag-orchestrator/tools rag-orchestrator/tests` 零命中。加測試 `result.answer == "".join(s.text)`。 **否決的替代方案**：不動 schema、由 Verifier 容忍句數不等再對齊——句數不等時程式得猜哪筆標籤配哪句，猜錯方向是放行；本案文字與標籤同筆攜帶，拼接相等成為定義。**r2 安全審第 10 條（疑問句夾斷言）處置**：FIX＝(d) 逐片段複核＋新 fixture 釘住 | 主 session（依 4.3c 實測＋r10 REVISE） | `services/agent/output_schema.py`；`verifier.py` 步②；`agent_rules.py`／`prompt_assembler.py`；`reviews/r10-plan-verifier-dsp028.md` | 見驗收四尺 | design 1.4.10；requirements R1／引用契約行同步 |
| DSP-029 | **引用改「指向來源句編號」，引文由程式解析（提案 v2，依 r12 plan-verifier 九條阻斷修訂；待 security-reviewer＋closing）。** 起因：4.3c R4 162 回合 QUOTE_NOT_VERBATIM 51 次＝mini 抄引文不逐字是主要拒因（R1 被 SCHEMA 遮住）；三輪提示詞調校證明越長越先轉人，⛔ 依業主紀律不再特例修改。**契約**：`Citation{tool_call_id, source, unit: int}`（刪模型端 `quote`；`$defs.Citation.properties=={tool_call_id,source,unit}`）。**unit 的唯一來源（P0-1）**：只由 `ToolResult.provenance` 逐筆產生——每筆 `Provenance.text` 以 `verifier.split_sentences` 切、片段 `strip()` 為空者不編號、其餘依序 0..n-1；送模型的文字＝各筆 `"\n".join(f"[{source}#{i}] {piece}")` 串接。`text_for_model`／`outline.text` ⛔ 不再作為編號或送模型的輸入：`kb_search` 等多列工具的送模型文字改由 provenance 生成（原 `text_for_model` 保留給無 provenance 的工具）；`provenance==[]` 的工具（handoff／session.slots／confirm）不標記、其 source 視為不存在。大綱：`outline.py:_build_doc` 改為逐節生成已標記文字（標題行 `【id】標題` ⛔ 不進編號），`sections[i].text` 保持未標記原文；`runtime._seed_outline_provenance` 仍以 `section.text` 為 provenance ⇒ 兩邊用同一把切句、同一份原文，編號恆一致。**落點（P0-2）**：大綱標記在 `outline.py`；工具標記在 `runtime.py` 呼叫 `wrap_tool_data` **之前**由 provenance 生成；`prompt_assembler.py` 只改鐵則文字，`build()` 簽名與 R11.5 白名單 ⛔ 不動（`test_build_signature_has_no_channel_for_tool_results` 須原樣綠）。**解析與尺（P1-1）**：Runtime 先跑 `_canonicalize_outline_sources`（語義不變），再以 `(tool_call_id, source, unit)` 解析 quote＝該 provenance 第 unit 個片段；source 在該 tool_call_id 內不存在 ⇒ SCHEMA（子成因 `source_not_found`，驗收單獨計數、上限 5/162）；unit 越界 ⇒ SCHEMA（`unit_out_of_range`）。Verifier ③ 改吃解析後 quote，**覆蓋改片段側相對覆蓋率**：`len(overlap) >= max(min_coverage_chars, ceil(min_coverage_ratio*len(_meaningful_chars(fragment))))`，`min_coverage_ratio=0.5` 進 `VerifierRules`（rules 1.3.0）；極性、citable 不變；`QUOTE_TOO_SHORT` 保留（來源片段 < `min_quote_len` 仍拒）、`QUOTE_NOT_VERBATIM` 在模型端不可達（fixture 轉為程式解析路徑的單元測試）。**標記防偽與防抄（P2-3）**：`sanitize_for_data_block` 對來源文字把 `[`／`]` 轉全形 `［］`（偽造標記不可能同形）；Verifier 步⑥前對拼接後 `answer` 掃 `\[[A-Za-z0-9_:.\-]+#\d+\]`，命中 ⇒ SCHEMA（子成因 `marker_in_answer`）。**提示詞三處同改（P1-2）**：`agent_rules.py`、`prompt_assembler.py:_agent_rules_text`、`runtime.py:_REASON_HINTS` 把「抄逐字」改「填 unit 編號」；刪兩例句（feature／pricing）；契約說明放 `Field(description=…)`，`fact_class`／`handoff_reason` 的說明維持在 `_agent_output_response_format` 覆寫字面內（P2-4）；驗收 `rg -n "quote" rag-orchestrator/services/agent rag-orchestrator/tools` 僅剩 `quote_len`／註解（正對照 `quote_len` 必命中）。**fixture（P2-2）**：`quote_not_verbatim` 案改為程式層測試（解析永逐字）；`quote_too_short` 改為指向過短來源片段；新增：unit 越界 ⇒ SCHEMA、source 不存在 ⇒ SCHEMA、指向無關句 ⇒ QUOTE_NOT_COVERING、R4 三個真捏造句（上傳合約「作為參考」×2、「編輯合約需要管理者權限」）⇒ QUOTE_NOT_COVERING、標記抄進 text ⇒ SCHEMA、DSP-021 兩種 source 抄錯（帶標題／缺前綴）仍放行；正對照：指對句放行、含 `[` 的來源文字轉義後解析不受影響。11 拒因覆蓋維持。**驗收（P1-3，含反向尺，跑前定好）**：同 54 句、3 rep、兩鏈：① 新拒因分佈逐項落表（SCHEMA 子成因三種、QUOTE_NOT_COVERING、UNCITED、POLARITY）；② `budget_exhausted` ≤20/162；③ 答到率 ≥ R4 18.5%（⛔ 不用 R1 42% 假象）且 ④ **抽審反向尺**：從本輪「答到」回合以固定種子隨機抽 20（不足則全抽），獨立代理逐句對大綱原文判有據／無據，無據率 ≤ R4 同法抽審基線（R4 基線在跑 DSP-029 前先量）；⑤ 禁詞 ≤3、敏感 0 漏；⑥ `pytest tests/unit/agent/test_verifier_req.py` 全綠。**否決的替代方案（r12）**：(a) 保留 quote＋最長公共子串修復＝在牆前加幫忙過關的中介，猜錯方向是放行；(b) unit／quote 雙軌＝模型不填 unit 就退回舊路，等於放寬。 | 主 session（依 R1–R4 實測） | `output_schema.py:Citation`；`outline.py:_build_doc`；`runtime.py`（provenance 標記、解析、`_REASON_HINTS`）；`verifier.py`（③ 相對覆蓋、標記掃描）；`prompt_assembler.py`（鐵則文字＋`sanitize_for_data_block`）；`agent_rules.py`；fixtures；`reviews/r12-plan-verifier-dsp029.md` | R4 數字見 `eval/perf-agent-regression-20260905/README.md` | design 1.4.11；requirements 引用契約行 |
