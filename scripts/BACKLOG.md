# BACKLOG — 危害、待裁決、待辦

> 由 `scripts/status.py` 讀取並**置頂印出**。⛔ 別把這些寫成交接檔中段的散文——
> 檔案幾千行，下一個 session 不會讀到那裡。
>
> 格式：`- [ ] (級別) YYYY-MM-DD 一句話 — 說明`
> 結案改成 `- [x]`，⛔ 不要刪除（刪掉就沒人知道它曾經是問題）。
>
> 三級的差別在**球在誰手上**，那決定了「擱置很久」是不是正常：
>   · `P1–P3` 球在我們 → 開單久是正常的（資源分配問題）
>   · `⛔`     球在我們 → **現在不能做什麼**，置頂印，擱著就是危險還在
>   · `❓`     球在**業主** → 擱置**一定是壞的**：不是他不想決定，是它沒被端到面前
>
> ⛔ 刻意不另開 HAZARDS.md：同一個 bug 的「不要跑」與「要修」同生同滅，
> 分兩個檔就是同一件事寫兩遍，漏改的那一處會變成假警訊。

- [x] (P2) 2026-09-01 向量索引 IVFFlat lists=100 — repo DDL 六處已改 HNSW、本機已重建、不變量 26 已加。⚠️ **降級**：原判「靜默丟答案影響生產」**是錯的**（生產 ORDER BY 寫法用不上索引，35 題修前後逐筆相同）；實為**潛在地雷**——有人把 ORDER BY 改成正規寫法就會引爆。⛔ **線上尚未重建**，見 `ivfflat-index-defect.md` runbook
- [ ] (⛔) 2026-09-01 **架構母圖 §3 的修正只在工作樹、未 commit** — 三條過濾公式已逐行對碼改好（`git diff docs/architecture/COMPLETE_CONVERSATION_ARCHITECTURE.md` 可見 +25/-7），但**沒進版控**。⛔ **勿跑 `git checkout .` / `git stash` / `git reset --hard`**，那會讓一整天查證出來的修正無聲消失。⇒ 收成 commit 後才可把本條打勾。⚠️ 在那之前，若有人重新 clone 或還原工作樹，母圖 §3 仍是錯的（照它給 b2b 補 IS NULL 會打穿池隔離；以 `b2b-ground-truth.md` 為準）
- [ ] (⛔) 2026-09-01 Reranker 可用性判定會靜默永久停用 — `semantic_reranker._check_service` 建構期只探測一次（原 timeout=2）失敗即整個 process 停用；rerank 佔最終分數 **90%**，一停就落到詞面分支、純詞面命中得 1.0 壓過正解 ⇒ 使用者收到「請撥打客服專線」。⚠️ 容器 healthy、`/api/v1/system/pipeline-health` 都會回綠（該健檢自己重探測，⛔ 不反映正在服務的物件）。**B 已修**（每 60 秒自動重試、probe timeout 5s、base_retriever 改用 `available()`），⛔ **線上要重 build image 才生效**
- [ ] (⛔) 2026-09-01 semantic-model CPU 推論撐不住 — 實測延遲 >90 秒、CPU 600%+（老毛病，`.kiro/issues/reranker-returning-zero.md` 2026-04-17 已有 hotfix）。B 只讓它能復原，⛔ 沒治產能。加資源／換模型／改 GPU 待裁
- [ ] (P1) 2026-09-01 `include_debug_info` 對面向進場路徑無效 — `_conversational_to_response` 組回應時硬編碼不傳 `debug_info`，不論旗標為何恆 None；`processing_path='conversational'` 佔實測流量 **23%（59/261）** ⇒ 那些題的候選與分數**無法觀測**，回測會誤讀成「沒有候選」。⚠️ 這是**觀測性缺陷**，不影響使用者，但會讓量測失真
- [ ] (P2) 2026-09-01 兩個死開關 — `ENABLE_KNOWLEDGE_RERANKER`／`USE_SEMANTIC_RERANK` 在 compose 與容器都設 true，**serving 程式碼零命中**（只有已知會炸的回測腳本引用）；改它們不會改變任何行為
- [ ] (P3) 2026-09-01 `test_verdict_domain_matches_design_enum` 路徑失效 — 找 `.kiro/specs/retrieval-decision-layer/design.md`，該 spec 已封存進 `archive/`；且 `dr.REPO` 解析成 `/`。屬已封存 spec 的殘留
- [ ] (❓) 2026-09-01 b2b 35 題的「該命中哪筆」待確認 — 未確認前所有命中率只是診斷，不是收案證據
- [ ] (❓) 2026-09-01 80 筆宣告給租客卻在 b2b 池的知識是否誤置 — 取決於「平台操作知識該不該由業者 SOP 承接」的產品意圖
- [x] (✅) 2026-09-01 文件處置**已執行** — 架構母圖 §3 的三條過濾公式與兩處門檻敘述**逐行對碼修正**（非只加警語）；`KNOWLEDGE_SCOPE_MIGRATION_GUIDE.md` 已移入 `docs/archive/2026-09/` 並自標退休
- [ ] (❓) 2026-09-02 適用性把關 `max_checks=2`「未受審候選一律丟棄」算不算正確行為 — 2026-08-22 裁示射程只到「判準從『相關』改為『適用』」，⛔ 不涵蓋此項。判為正確會**結構性壓低失敗率**並把 gate 這個真正的槓桿排除在改善清單外。實證：35 題中 ts9945（top1 0.925／過門檻 5 筆）、ts7620（0.951／4 筆）皆因此轉客服
- [ ] (❓) 2026-09-02 b2b fail-closed 於判定 LLM 失敗時清空整批算不算正確行為 — 同上未裁；且必須能與 max_checks 那條**分開計**，否則兩種成因混為一談
- [ ] (❓) 2026-09-02 G 類（下游否決）的修法允不允許動 gate — 決定 P2 是往「補知識／補 representation」還是往「治適用性把關」走，兩條路完全不同
- [ ] (❓) 2026-09-02 要不要在 P1 基準之前把否決軌跡儀器化（動 chat.py 落盤第⑥格） — ⛔ 沒有它，契約 §C 的「轉客服」整欄算不出來（現況只能撈 log，而那是契約明文禁止的）；⚠️ 但 PLAN §5 明訂基準前不動門檻與規則
- [ ] (❓) 2026-09-02 `escalate`／`out_of_scope` 要不要逐題核可 — 這兩格直接決定分母（out_of_scope 明寫不計入分母）；P0-2 出口要求業主核可紀錄，P0-1 沒有 ⇒ 實作方自行標註即可同時降低失敗率又縮小分母
- [ ] (⛔) 2026-09-02 **kb3330 講錯：「系統保存歷史合約 5 年」，官方寫「沒有期限」** — 官方 `slug15` 專門寫了一段預防這個誤解（「系統保留期限」與「法規保存年限」是兩件事），知識恰好合成官方要防的那句。⇒ 客服照講會讓業者以為第 6 年資料消失，**錯誤方向不可回復**。改法零風險（照抄原文）。詳見 `kb-vs-helpcenter-20260902.md` D-1
- [ ] (❓) 2026-09-02 kb3336 帳單排定發送：三方說法不同，以哪個為準 — 知識「12:00～17:00、放進去立即寄」／官方「統一於當日 12:00」／程式 `Kernel.php` 排**四班**（12/13/14/15）。知識的 17:00 與「立即寄出」皆錯（實為整點批次掃 status=32）；⚠️ **官方頁也不完整**（漏 13-15 三班）⇒ 需一併回報文件方。⛔ 不自行選邊
- [ ] (❓) 2026-09-02 kb3345「永豐撥款日 = 繳款日 + 2 天」是哪來的 — 官方 `paymentapply` 逐字寫「永豐…提領方式與審核工作天，**待業務提供後補寫**」＝ JGB 內部尚未確定；程式唯一近似值 `AgreedDay VACC:3` 屬**藍新**、非永豐、且是 3。⇒ 斷言未知的金流時程風險高於講錯已知值，建議降級為「請洽永豐商戶後台」
- [ ] (❓) 2026-09-02 kb3344 分行代號 021 vs 程式 0210 — `app/Payment.php` 的單一真相來源 `SINOPAC_FVA_BRANCH_CODE='0210'`（4 碼），知識寫 3 碼。⚠️ 這是租客照著鍵入的臨櫃欄位。同筆「854+11 碼」「國泰分行可直存」兩來源皆查無
- [ ] (❓) 2026-09-02 永豐虛擬帳號是浮動還是固定 — 官方 `landlordonboarding05Paymentsettings` 稱「浮動／每期換號」，程式走 `SINOPAC_FVA_*`（Fixed Virtual Account）＋`bill:recharge-account`「固定虛擬帳號自動到帳排程」⇒ 兩個權威來源直接矛盾
- [ ] (P2) 2026-09-02 kb3332「自動儲存操作後 10 秒」與程式不符 — `useAutoSaveOld.js` debounce 5000ms、`useAutoSave.js` 預設 3000ms；⚠️ 同檔註解 `// 會delay x秒才顯示 已於 10 秒前儲存` ⇒ 知識很可能把**畫面提示字**當成觸發條件
- [ ] (P2) 2026-09-02 kb3327 點退草稿帳單的產生時機與官方不符 — 官方：房東**主動發起點退**時產生；知識：提前終止雙方簽名完成後自動產生。流程骨架其餘與官方一致
- [ ] (P3) 2026-09-02 官方 `slug22`／`onboarding8`「線上付款不會出現在待對帳」的通則會誤導 — 超商條碼是兩階段 T+N 結算，碼上確實會停在 status=8（kb3343 是對的）。⇒ 建議回報文件方加註例外
- [x] (❓→✅ 2026-09-04 業主「依序處理」採預設：34 筆全標 general，含 3331；`INSTANCE_REFERENCE_GATE` 未設故零運行時效果；⚠️ 假可逆，改回 NULL 會重新變紅) 2026-09-02 3327-3360 的 `instance_applicability` 怎麼標 —— `make audit` **不變量 10 現為 FAIL**（我 09:54 改 business_types 觸發 updated_at，34 筆未明示宣告）。⚠️ 獨立驗證**推翻**了我的「全部 general」：契約明文「knowledge text alone is insufficient evidence for authoritative general」（P1e-1 以 kb3509 為反證），而我正是憑文字判的。安全子集 **27 筆**可宣告 general；**4 筆待裁**（3327/3329/3331/3333 —— 其面向 `requires_instance_reference=true`）；3352 待內容審查。⚠️ 3331 是最強反例：答案自承「效期可申請調整為 7 天或 30 天」⇒ 正解依該團隊設定值
- [ ] (❓) 2026-09-02 **DSP-001**：面向的 `requires_instance_reference=true` 算不算該面向所屬知識的 instance 機器證據 —— 契約留白：裁定②說 instance 可由「診斷引擎契約／識別碼表單／動作端點」建立（四面向三者全中），但同 docstring 又說兩層契約「語義不同、⛔ 不得同名靠上下文猜」，且 `instance_applicability_decision` 允許 `general×REQUIRED→INELIGIBLE` 這個合法組合。⇒ 上面那 4 筆卡在這條
- [ ] (P3) 2026-09-02 不變量 10 的觸發訊號與 general 門檻相乘會產生壞誘因 — `updated_at` 分不出欄位別，任何 bulk 欄位維護都會一次把整批 legacy 拉進 ratchet；而被拉進 34 筆時最省事的出路正好是**整批宣告 general**，也就是契約 ⛔ 明文禁止的那條路。⚠️ 我這次的判斷就落在那個壓力方向上。⛔ 不建議改綠、也不建議改觸發訊號，僅記錄
- [ ] (P3) 2026-09-02 售前（prospect）池從未被任何回測涵蓋 — 3327-3360 那 34 筆 `target_user IS NULL`，補 business_types 時 prospect 池 **18→52（+189%）**、回滾又回 18。⚠️ 相對衝擊比 b2b（285→319，+12%）**大三倍**，而 35 題樣本全是 b2b ⇒ 售前側完全沒量。⇒ 任何可見度改動前先跑 `status.py` 記三池
- [ ] (P3) 2026-09-02 `make audit` 的紅燈計數不可靠，⛔ 別用 grep 數 — `CODE_CONTRACT_REGRESSIONS: 1` 是**陣列長度**不是紅燈條數；「不變量 1–12／14–20／22–23 有失敗」是 `check_invariants.sh` 裡**寫死的字串**、⛔ 不代表那些不變量都紅。⚠️ 不變量 4 只印 WARN、從頭到尾不印 PASS ⇒ 用「數 PASS 行」當尺會誤判。正解：`grep -n '❌' ` 逐條看
- [ ] (P3) 2026-09-02 `trigger_sync_api_endpoint_kb_ids` 會讓知識 UPDATE 靜默改到 `api_endpoints` — AFTER UPDATE 觸發器會重算 `api_endpoints.related_kb_ids`。本次 34 筆因 `form_id IS NULL AND api_config IS NULL` 而未觸發，⚠️ **但若改到有 form_id 的列，回滾會連帶改另一張表，而 `updated_at` 檢查完全看不到**
- [x] (✅) 2026-09-02 **DSP-002 已裁（業主）** — ①空 answer 錨點 ⛔ 不計為面向覆蓋；②**api／對話型面向的答案不來自 KB answer 欄**（`_ground_by_api` 以 API 回傳為底稿）⇒ 對它們「知識數」是**提名入口數**、⛔ 不是覆蓋。`status.py` 已依 `grounding_scope.select` 分開標示（api 印「入口」、其餘印「覆蓋」）。⚠️ 三個數字各有用途，非爭議：293 答題母體約定／354 兩路聯集／354·320 分路上界；⛔ `visible=True` 不保證撈得到（只給結構上界）
- [x] (✅) 2026-09-02 `status.py` 的「面向覆蓋」警訊算在錯的母體上 — **已修**：改印「答題／候選」雙欄。舊版只印答題數（`_B2B` 排除空 answer），而被排除的 61 筆**正是面向進場錨點**（狀態判斷 −9／繳費金流排障 −5／合約異動 −4／續約 −3／帳單異常 −3／條件診斷：帳單 −3／發票 −3／註冊驗證排障 −3／退租收尾 −3／電表排障 −3／其餘 12 面向各 −2）。⚠️ 修正後「帳單異常 答題 2／候選 5」標 ◐＝缺的是 answer ⛔ 不是缺知識。⚠️ 我原本把「⛔ 修繕報修 知識 0」也列為受影響例子——**那是舉錯的**，它在兩個母體都是 0，警訊本來就對
- [ ] (P3) 2026-09-02 還有第 7、第 8 個母體 — `rag-orchestrator/scripts/routing/run_p1_corpus_sibling.py`（符號 `exposure_surface`，母體 **146**，且**已寫進凍結協定當判準**）／`scripts/g2_selftest.py` 的 `ORACLE_Q`（**870**，註解自陳刻意不複製生產過濾，屬已宣告例外）。⚠️ 連「一共有幾個母體」都還沒數完
- [ ] (P3) 2026-09-02 還有第 6 個母體（871）— `scripts/analysis/p1b_applicability_census.py:82` 與 `scripts/audit/checks/instance_applicability_contract.py:169` 少了 `id<>4253` 與 `answer` 非空，母體 871（正本 772，差 99），且無同源註記
- [ ] (P3) 2026-09-02 `_B2B` 未宣告只涵蓋 `property_manager` — retriever 的 `is_b2b_mode` 還認 `system_admin`（該身分僅 8 筆可見）。窄化刻意，但讀者會以為 293 涵蓋整個 b2b。已於 status.py 與 SKILL.md 補宣告
- [ ] (P2) 2026-09-03 **回測 session 前綴：兩把豁免尺不一致** — 計量看 `startswith('backtest_')`（`usage_metering.INTERNAL_RULES`），題庫豁免看 `startswith('backtest_session_')`（`chat._record_no_knowledge_scenario`）⇒ 用 `backtest_` 開頭的路徑**被標成內部流量、卻仍寫 `test_scenarios`／`suggested_intents`**。⚠️ **目前唯一活著的肇事路徑是 `decision_replay.py`**（`--workers` 預設 4、`--tag` 預設 `backtest_rdl`）：DB 實證 2026-08-22 該輪把重播語料以 `source='user_question'`、掛真實 role user_id 寫進題庫（93920→14 筆、77001→6 筆；`suggested_intents` 93920→7 筆）。⚠️ 那些列的 created_by 有指紋、可指認可刪。⛔ 只有 `run_batch.py` 有前綴閘門
- [x] (✅) 2026-09-03 ~~五個回測入口併發跑~~ **原條文三處事實錯誤，已撤銷重開（見上）** — ①`BACKTEST_CONCURRENCY` 在 framework 的預設是 **1** 不是 5（5 只在 `run_backtest_with_db_progress` 與 admin 注入的 env）；②knowledge-admin 後台**使用者按不到**（前端只剩 `.bak`，且該端點跑的 `run_backtest_db.py` 全 repo 與容器內皆不存在＝死碼）；③我列的那五支**自 2026-07-06 起全部休眠**（`backtest_runs` 與 `knowledge_completion_loops` 最後一筆皆為該日），而真正在污染的第六支 `decision_replay.py` 我漏了
- [ ] (P3) 2026-09-03 「併發 ⇒ semantic-model 逾時 ⇒ reranker 落到詞面 ⇒ 整輪作廢」**第一環實測不成立** — 實測容器內 `/rerank`（20 筆候選）：併發 5 最壞 6.33s、併發 10 最壞 6.15s，而 `RERANKER_HTTP_TIMEOUT` 預設 **60s**，差一個數量級。⚠️ 該說法引用的「>90s、CPU 600%+」出自 **2026-09-01 的靜默永久停用事件**（建構期單次探測失敗），⛔ **不是併發實測** ⇒ 我把兩個獨立觀察串成了因果。⚠️ 第二環（逾時落詞面）成立但已非「永久」（`available()` 每 60s 自癒）；第三環（整輪作廢）**只是推論**

- [x] (✅) 2026-09-03 **rerank 融合權重 6:4 對照回測——零改善，⛔ 不改**：0.9 vs 0.6 各 3 輪 × 35 題（正式入口，前綴 `backtest_session_fw09_r*`／`fw06b_r*`，資料在 `usage_events`）。轉客服 10/10/10 → 9/10/10（差 0.3 題 < 臂內變異 1）；31 題同 top1 平均平移 **−0.064**，2 題（9905／9976）因此跌破 0.65 由 G 變 T——**同樣轉客服、死在更早一關**。那 10 題：6 T 分數 0.17–0.60 權重救不到（S／N）、4 G 分數 0.65–0.95 被把關否決權重無關。⇒ 權重不是槓桿；「分數與門檻不可拆開設計」實測再證。`base_retriever.py` 已改讀 env `RERANK_FUSION_WEIGHT`（預設 0.9 逐位元同舊碼），收不收待業主。
- [ ] (P2) 2026-09-03 **啟動順序危害**：機器重啟後 rag-orchestrator 比 semantic-model 早起，reranker 建構期探測失敗、靠 `RERANKER_RECHECK_INTERVAL`（60s）自動重試才復原；**那 60 秒內的請求靜默落到 keyword／vector 分支**（0.6 臂 r1 實測 4/35 題 `score_source<>'rerank'`）。線上部署重啟同樣會發生。修法候選：compose `depends_on` 加 semantic-model 的 healthcheck condition，或縮短首次重試間隔。查證：`docker logs aichatbot-rag-orchestrator | grep 建構期探測失敗`
- [ ] (P3) 2026-09-03 **G2 兩個假紅**：①拿 `make audit` 總 exit code 當不變量 3 的結果（不變量 3 本身 PASS）；⑤ 新容器 stdout block-buffer，`print` 的 Reranker 初始化行未 flush 就 grep 不到（暖機一題即出現）。`scripts/g2_selftest.py` 符號 `sh(["docker","logs"...])`
- [ ] (P3) 2026-09-03 **押金不變式擋不住「洗白」**：`services/ocr_mapping/deposit_rule.py` 符號 `assert_deposit_exclusive` 只檢查「月數／金額不同時非空」與 `source∉{derived,llm_extracted}`——若推算後把值標成 `source=ocr` 並清掉兄弟欄，尺看不出來（獨立驗證 N2）。它守的是**輸出形狀**不是**計算過程**；真正的守門是 `decide_deposit` 本身不含除法（測試 6.1／6.2 正例）。要更強得比對 `raw` 是否含月數詞。
- [ ] (P2) 2026-09-03 **追溯掃描器對全部 19 個 spec 讀到 0 條需求**：`rag-orchestrator/tools/traceability/scan.py` 產出的 `traceability-matrix.json` 內 `matrix=[]`，所有 `@pytest.mark.req` ID 落 `gaps.dangling_refs`（「需求不存在」）⇒ 「無孤兒需求」對任何 spec 都是真空成立。獨立驗證 2026-09-03 抓到；`documind-ocr-mapping` 10.2 據此降為部分收案。修法：查掃描器解析 requirements.md 的標題樣式（Requirement N／N.M）是否與現行寫法對得上。
- [ ] (P3) 2026-09-03 **finishgate 的 activation 是 session 層級**：唯讀子代理會繼承主 session 的 mutation 狀態而被同一閘門擋（獨立驗證 N8）；三個代理各被卡 3 次才靠 `MAX_UNCHANGED` 逃生。屬 `~/.claude/canon` 工具層，非本 repo。**同日第三輪再犯**：兩個代理（唯讀 plan-verifier＋verifier）的結論本文被 `make audit` 不變量 10 紅燈吃掉，只回「無法清閘」，得用 SendMessage 要它們重貼——⚠️ 這條升 P2 候選：它讓派驗的交付率直接歸零。
- [x] (❓→✅ 業主 2026-09-04 選 D：`r10p/v1-scope-exclusions.txt` 登記 34 筆、檢查器 17 只對未登記漂移紅＋3 組新正對照；`make audit` OVERALL PASS。⚠️ B 被我自己查證否決——凍結 54 內本有 5 筆 general) 2026-09-04 **不變量 10 與 17 互斥——宣告 34 筆讓 10 綠、17 紅**：17 的母體定義 `declared AS (SELECT id FROM knowledge_base WHERE generation_metadata ? 'instance_applicability')` 是活查詢，任何新宣告都算「R10-P review 中途母體漂移」；凍結 54 → 現況 88。三條路都是業主的版本決策：(A) 母體升版 V2＝88，重產 `r10p/population.txt`＋`digests.txt`，但 P3 verdicts／P4 projection 只涵蓋 54，34 筆在 R10-P 內成「未 review」；(B) 改 17 的母體定義只納 `instance` 宣告（general 與 R10-P 責任審查無關），凍結值不動；(C) 回滾 34 筆為 NULL → 回到 10 紅 17 綠。⚠️ 我預設建議 **B**（母體語義本來就是「需要 Face 責任審查的列」，general 列進去反而讓 review 範圍失真），⛔ 未裁前兩邊都不動。已記 dispute。symbol `r10p_population_integrity.py` 註解「⚠️ 母體漂移一律 FAIL」
- [ ] (P3) 2026-09-04 **canon `audit.py status`（CLI，無 session id）會猜錯憑證檔而誤報 0/22**：子代理的整檔讀**有**累計進主 session 憑證檔（`state/receipts/<主 session>.json` 中 chat.py／conversational_engine.py／llm_answer_optimizer.py／design.md 皆 `full=True`），但 CLI `status` 依「最近被改過的憑證檔」猜 session，本次猜到別的檔 ⇒ 顯示 0/22，差點讓我以為代理讀取無效而提早 `end`。符號 `audit._store` docstring 自陳此猜法；修法＝CLI 接受 `--session` 或改讀 `CLAUDE_SESSION_ID` 環境變數。⚠️ 我先前一度把它誤判成「憑證不累計子代理」，查憑證檔後更正
- [x] (❓→✅ 業主 2026-09-04「做」：`docker-compose.prod.yml` 已加 `PRESALES_GROUNDING_THRESHOLD: 0.5`) 2026-09-04 **presales-grounding-gate D2：售前門檻要不要 env `PRESALES_GROUNDING_THRESHOLD=0.5`**——線上 `KB_SIMILARITY_THRESHOLD=0.65` 擋掉「電子發票可以自動開嗎」（3601 final 0.534，盤查正面項）；20 題掃描無知識題最高 0.337、0.5 仍有 0.16 邊距。改法＝`docker-compose.prod.yml` 加一行 env。資料 `.kiro/specs/presales-grounding-gate/perf-20260904.md` §4
- [ ] (P2，**批次已備待業主放行**：`scripts/knowledge-batches/presales-phrasing-batch-20260904.json` dry-run 過、附 rollback；匯入後重建 semantic-model 並重跑房東／代管劇本) 2026-09-04 **presales 知識口語講法缺口（第四輪 e2e C 過度封鎖）**：3600 對「可不可以線上簽約」final 0.326、3610 對「費用怎麼算」0.47，低於 0.5 走固定句；同知識用庫內講法問 0.97／0.99。沒知識的「600 戶」卻 0.439 ⇒ 門檻無解，要補講法（3600 加「線上簽約」、3610 加「費用怎麼算／多少錢」）。屬 spec 範圍外知識補充＋DB 寫入，待業主。證據 `perf-20260904.md` §9。
- [ ] (P2，**批次已備待業主放行**：`scripts/knowledge-batches/presales-phrasing-batch-20260904.json` dry-run 過、附 rollback；匯入後重建 semantic-model 並重跑房東／代管劇本) 2026-09-04 **presales 否定式斷言漏：帳單格式自訂**（第 5 輪 e2e 唯一漏）：LLM 對知識沉默處答「格式是固定的，無法自訂」；同題另一 session 答對。決定性解法是加一筆邊界句知識（能不能自訂，先對幫助中心／jgb2 查證再寫），⛔ 不是調閘門。同型待補：3340／3798 對「那租客不繳怎麼辦」講法。
- [x] (業主 2026-09-04 二次裁：**不要 D6，補完知識再考慮** ⇒ D6 改 env `PRESALES_EXTRACTIVE` 開關、預設關；三條路有知識皆交 LLM 依知識合成) **presales D6 是否退成「敏感五類抽取、功能類回 LLM 合成」**：業主可接受功能邊界的小機率漏，但選擇先把知識補完善再回頭看——順序：① 裁 DSP-008 讓匯入條目能寫邊界（可匯：物件／租客？不可匯：合約／歷史帳單）；② 3600 加「線上簽約」、3610 加「費用怎麼算／多少錢」、3584 公司團隊方案補講法（180 間公司問「物件跟合約」現撈到 3596 十間房東）；③ 知識補完後重跑房東／代管劇本，再決定要不要退 D6。⛔ 知識沒補完前不重提退 D6。
- [ ] (❓) 2026-09-04 **presales-grounding-gate 延遲 NFR 未達**：無佐證路徑安靜 median 8.31 s vs 改前 6.35 s——`retrieve()`（keyword fallback＋reranker，1.7–2.6 s、冷啟 10.7 s）抵銷省下的合成 LLM。選項：`enable_keyword_fallback=False`／接受（正確性優先）／查 reranker 冷啟。符號 `conversational_engine._converge_grounding`
- [x] (❓→✅ 業主 2026-09-04「做」：已實作 `_extractive_decision`＋`partial_grounding`，R2.10) 2026-09-04 **presales-grounding-gate D6：事實題是否改「抽取式作答」**——e2e 兩輪皆抓到：知識有「物件／租客批次匯入」時，LLM 對「含房東租客合約帳單」把「合約」也說成可匯（2 次 1 中；t5「那物件跟合約呢」同型），【逐項對照】prompt 指令降低不消滅。決定性關法＝`fact_class≠other` 且有 grounding ⇒ 直接回 top-1 知識原文（不經 LLM）＋固定尾句「沒提到的項目我沒有資料，可點下方『找真人』」；代價＝事實題失去潤飾與個人化。⛔ 未裁前 B1 殘留，spec 不宣告完成。符號 `conversational_engine._prepare_inner`／`llm_answer_optimizer._build_presales_synth`
- [x] (P3→**實測 P1**，2026-09-04 e2e 第三輪 3/5 次重現「合約和歷史帳單可匯入」；已修為結構閘門 R2.11：使用者句含問句標記＋反問句陳述句／前綴子句 ≥8 字 ⇒ 走 grounding；符號 `presales_gate.analyze_ask`) 2026-09-04 **prospect brain 的 `next_question` 無機械閘門**（收尾 verifier A-1）：`ask` 輪只有 `next_question` 無 `inline_answer` 時 R2.7 閘門不觸發，8 次實打 2 次把「也能匯入」當事實講；R4.2 後置掃描有補 handoff，故不算 REFUTED。規則文字已禁，屬 LLM 自律；機械閘門要判「問句裡有沒有事實斷言」＝開放語義，⛔ 不用關鍵字。這類輪也**沒有** `presales` 計量列。符號 `conversational_engine._prepare_inner` R2.7 段
- [ ] (P4) 2026-09-04 **R2.7 閘門放行分支不計量**（A-2）：`_meter_presales(path='inline')` 只在 `if _cg.empty:` 內 ⇒ 覆蓋率只有分子。搬到分支外即可
- [ ] (P4) 2026-09-04 **`run_batch.py` 不採集 `handoff`**（A-5）：任何用它產的 before／after 檔對 handoff 的比對都是空對照；`ask()` 的 return 補 `"handoff": data.get("handoff")`。符號 `scripts/backtest/run_batch.py`
- [ ] (P4) 2026-09-04 **`EMPTY_GROUNDING_MARK` 不外洩僅 n=3、prospect 回覆偶見 inline＋next_question 整句重複**（A-3／A-4，既有形狀）：收案級要 n≥20 或 answer_rules 加禁抄
- [ ] (P3) 2026-09-04 **`rental_terms.tenant_name` 帶括號尾碼會污染名字**（第三輪 verifier F2）：「歐陽小美（A123456789）」→ `to_user_first_name` 含身分證。DocuMind 樣式 `[^\n\r（(]+` 已排除括號，故目前不會發生；防禦修法＝`split_name` 前截掉首個「（／(」之後。符號 `contract_mapper` 承租方姓名段
- [ ] (P3) 2026-09-04 **`conflicts[].value` 混型**（verifier F3）：頁級衝突存原字串（「15,000」）、來源衝突存換算後整數（17000），LIFF 顯示要自己判型。既有設計（需求 2.3 落選值＝原值）；若要統一得在 `_date_field`／`_rent_field` 對 `mf.conflicts` 也做換算。符號 `contract_mapper._prefer`
- [ ] (P3) 2026-09-04 **DocuMind 側待辦（D1 定案 A 的另一半，外部 repo `~/jgb/DocuMind`）**：`contract_patterns.py`／`contract_field_extractor.py` 符號 `rental_fields`——① `deposit` 拆 `deposit_amount`／`deposit_months`（現在「27,600」與「2」混收同鍵，chatai 只能不消費）；② 新增 `payment_cycle`、`early_termination_days`；③ 63/76 條樣式要求「標籤：值」，自由文句合約整組 `rental_terms` 不出現；④ `types.py` 沒宣告 `rental_terms`、tests 無覆蓋；⑤ 多捕獲組空白 join（「114 1 21」）改回正規化日期。chatai 側對應 `CONTRACT_RENTAL_MAP`。
- [ ] (P4) 2026-09-03 **租期獨立「半年」不解析**：`normalize_lease_months("租期半年")` 回 None ⇒ `absent`（第三輪審查 ①）；R5.4 已改寫成「N年半→+6」對齊程式。預設行動＝不做（半年租約罕見、absent 是 fail-closed 進 needs_confirmation）；要做時在 `_LEASE_YEARS` 旁加無前導年數的「半年」→6 並補正反例。符號 `field_normalizer._LEASE_YEARS`
- [ ] (P3) 2026-09-03 **`make trace` 在 host 上壞**：Makefile 的 `trace` target 執行 `python3 tools/traceability/scan.py`（repo 根），實檔在 `rag-orchestrator/tools/traceability/scan.py`（`testing-code.md` 也是這樣寫）⇒ host 跑 `make trace` 必 `No such file`。從 `rag-orchestrator/` 直接跑掃描器可用。修法：Makefile 路徑加 `rag-orchestrator/` 前綴或 `cd`。
- [ ] (❓) 2026-09-03 **不變量 22 的產物含佔位假值 —— 需業主核可才改**：`.kiro/specs/conversational-routing-execution/r10p/v2-structural-equivalence.json` 的 `active responsibilities.v2_derived=999` 與 `mismatches=[{"name":"x","v2":1,"p5":2}]` 皆為**佔位值**，導致 `result=DRIFT_DETECTED`、不變量 22 的兩個**正對照**（「現況乾淨」「只改 canonical 面必須綠」）持續紅，而九個「必須紅」的突變控制全部正確 ⇒ 檢查器過嚴的根因在產物、⛔ 不在邏輯。**實算真值＝30**（`registry-v2.json` 的 `status=='reviewed_active'`），與 `p5_frozen=30` 相符。⛔ 該檔寫入被權限擋下（稽核封印檔），⚠️ **已上呈、預設行動＝維持現狀不改**。⛔ 進該產物的 commit `a6e07be8` 訊息宣稱「稽核 22 條轉綠」——**那句宣稱不成立**。
- [ ] (P2) 2026-09-03 **不變量 22 只讀產物自稱的 `match` 旗標、⛔ 不重算數字** ⇒ 擋不住「數字寫錯但 match 填 true」；8 個 numeric_check 我只能獨立驗 1 個（active responsibilities）。⇒ 「及格線在被驗方手上」同型缺陷，`scripts/audit/checks/registry_v2_scope_lock.py` 符號 `violations`
- [ ] (P2) 2026-09-03 **本分支引進的測試失敗**：`rag-orchestrator/tests/unit/backtest/test_verdict_ruler_req.py::test_verdict_domain_matches_design_enum` 讀 `.kiro/specs/retrieval-decision-layer/design.md`，而 commit `206876a2` 封存該 spec 時刪了 17 檔、⛔ 沒同步這支測試。⚠️ 與 b2b 檢索無關。（另有既有問題：容器內 `decision_replay.REPO` 解析成 `/`）
- [ ] (P3) 2026-09-03 **`refcheck` 的落點只解析 `檔案:符號` 冒號形**，空格分隔會落 fallback 當整串裸符號比對 ⇒ 假陽性。本次已把 `.claude/DECISIONS.md` 的 DSP-002 落點改成冒號形；⚠️ 台帳其餘列若有空格形亦同病，符號 `split_landing`
- [ ] (❓) 2026-09-03 **DSP-003**：回測 session 前綴的兩條規則互相矛盾 — `量測層級.md` 說「⛔ 避開 `backtest_`（否則被標成內部流量）」，`run_batch.py` 說「⛔ 必須 `backtest_session_`（否則寫生產題庫）」。**兩者都自稱 ⛔、依任一條做都會違反另一條**。⛔ 未裁前不得單方選邊
- [ ] (❓) 2026-09-03 適用性稽核是否應涵蓋空 answer 面向錨點 — `p1b_applicability_census`／`instance_applicability_contract` 的母體（871）不含 `id<>4253` 與 answer 非空。⚠️ 我原本標成「未同源缺陷」，**獨立審查指出那未經證成**：它們稽核「誰需要適用性契約」，而錨點正是面向進場錨點。⛔ **實害**：`p1e_corpus.py` 的母體繼承自 P1b census ⇒ 逕行「同源」會讓一份**已凍結的標註 corpus** 變動
- [ ] (P2) 2026-09-03 `rules/規則綁在哪.md` 沒有任何強制機制 — 表中路徑／符號改名後不會有任何東西變紅（**初版自證用的兩條測試路徑當天就是錯的**）。⇒ 建議做成 `scripts/audit/checks/*.py`：讀該檔每個「路徑＋符號」，開不了或 grep 不到即 exit 1，比照既有 `--self-test` 慣例掛進 `check_invariants.sh`
- [ ] (P1) 2026-09-01 D-1：id 4253 分類錯誤致帳單診斷面向領域脈絡從未載入 — 業主裁「先不動」，排在 P1 基準之後
- [ ] (P2) 2026-09-01 不變量 4 與生產程式判準不同源 — 4253 讓它變綠但功能是壞的；需改成與 `system_context.py` 同源
- [ ] (P2) 2026-09-01 D-2：tenant_repair 面向從未寫過系統脈絡列 — 是否刻意設計尚未查證
